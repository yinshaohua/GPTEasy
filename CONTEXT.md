# GPTEasy

<!-- release-contract: desktop-consumer-control=prohibited -->

GPTEasy 帮助个人用户管理已验证供应商，并切换当前 Windows 用户默认 Codex 环境使用的 API 服务。

## 用户与环境

**主要用户**:
知道供应商、API Key 和模型的含义，并能从供应商网站取得这些信息，但不应被要求理解或手工编辑 Codex 配置的个人用户。
_Avoid_: 普通消费者、高级开发者、非技术用户

**当前用户 Codex 环境**:
当前 Windows 用户默认的 `~/.codex` 配置环境，是 GPTEasy 的核心受管对象。
_Avoid_: 原生 Codex 环境、宿主应用配置、自定义配置路径

**Codex 消费者**:
读取当前用户 Codex 环境的程序，包括 ChatGPT/Codex 桌面版和 Codex CLI。
_Avoid_: 宿主应用、客户端、受管环境

**ChatGPT/Codex 桌面版**:
OpenAI 面向 Windows 提供、以桌面进程形式读取当前用户 Codex 环境的统一程序；产品显示名、安装包身份和可执行文件名可能分别使用 ChatGPT 或 Codex。
_Avoid_: 桌面 Codex、统一 ChatGPT 桌面应用、宿主应用

**Codex CLI**:
从终端运行并使用当前用户 Codex 环境的 Codex 消费者。
_Avoid_: 本机原生 Codex CLI

## 供应商

**供应商**:
一组可切换的 AI 服务访问配置，包含名称、服务地址、API Key 和默认模型。
_Avoid_: 渠道、节点、代理

**候选供应商配置**:
用户正在新建或修改、但尚未完整通过供应商验证并明确保存的页面内配置；它不进入供应商目录，也不会跨页面或应用重启保留。
_Avoid_: 未验证供应商、供应商草稿、待验证供应商

**推荐供应商模板**:
由 GPTEasy 内置并在供应商管理中优先展示的配置入口，包含推荐供应商的固定身份信息和网站入口；用户完成配置、验证并保存前，它不是供应商目录中的供应商。
_Avoid_: 内置供应商、未验证供应商、默认供应商

**DayWay**:
内置的推荐供应商模板，固定展示名称为 DayWay，官网为 `https://dayway.site`，预填服务地址为 `https://dayway.site/v1`；它不预设默认模型，也不因内置而免除供应商验证。
_Avoid_: 默认供应商、免验证供应商

**供应商 ID**:
供应商首次验证并保存时获得、且不会因名称或访问配置变化而改变的 UUID。
_Avoid_: 供应商名称、地址指纹

**供应商名称**:
用户识别供应商的显示名称；去除首尾空白后，在供应商目录中不区分大小写地唯一。
_Avoid_: 别名、节点名称

**默认模型**:
供应商切换生效后 Codex 使用、并由供应商验证实际检验的模型；它必须来自本次模型发现结果。
_Avoid_: 测试模型、手工模型 ID

**供应商验证**:
确认供应商能发现默认模型，并以该模型完成 Responses API 流式工具调用闭环的过程。
_Avoid_: 测试、连通性测试、普通聊天测试、接口测试

**已验证供应商**:
当前服务地址、API Key 和默认模型已经完整通过供应商验证的供应商。
_Avoid_: 已保存草稿、可用节点

**供应商目录**:
当前用户保存的已验证供应商集合；每个供应商只保留当前已验证版本，不保存草稿、历史版本或软删除记录。
_Avoid_: 供应商列表、节点列表、草稿目录

**供应商顺序**:
供应商在供应商管理中的用户可调整顺序；DayWay 固定在首位，其他供应商默认按配置先后排列，验证、切换和改名不会自动改变顺序。
_Avoid_: 名称排序、最近使用排序、验证时间排序

**当前供应商**:
某个受管环境已经切换生效的已验证供应商；没有环境上下文时特指当前用户 Codex 环境，WSL2 操作中按所选发行版理解。
_Avoid_: 默认供应商、选中供应商、已启用供应商

**供应商配置有更新**:
受管环境中的当前供应商 ID仍存在于观察方的供应商目录或 Linux 供应商快照，但环境实际使用的名称、地址、API Key 或模型至少有一项不同。
_Avoid_: 当前供应商失效、自动同步、验证过期

**验证时间**:
供应商当前关键配置最近一次完整验证成功的时间；它不自动过期，也不表示持续健康状态。
_Avoid_: 在线状态、实时健康度、验证有效期

## 模式与配置

**供应商模式**:
当前用户 Codex 环境通过一个已验证供应商访问服务的模式。
_Avoid_: 代理 API 模式、第三方登录

**OpenAI 登录模式**:
当前用户 Codex 环境使用 Codex 自身已有 OpenAI 账户认证的模式；GPTEasy 不负责登录，也不把它建模为供应商。
_Avoid_: 原厂登录模式、官方模式、OAuth 模式

**环境实际状态**:
当前用户 Codex 环境的配置文件及目标 Codex 版本使用的凭据载体共同表达的新进程读取状态。
_Avoid_: 数据库当前状态、期望状态

**外部配置**:
当前用户 Codex 环境中没有有效 GPTEasy 供应商 ID、因而尚未由 GPTEasy 管理的供应商状态。
_Avoid_: 未知供应商、配置异常、自动匹配

**GPTEasy 管理区块**:
Codex 配置中由明确边界标识、带协议版本、供应商 ID和管理来源 ID，且只包含 GPTEasy 管理字段的唯一区域。
_Avoid_: 全文件重写、隐式配置区

**管理来源 ID**:
标识一次受管写入来自桌面管理协议或某份 Linux 供应商快照的非敏感值；它不决定当前供应商身份，也不参与供应商配置有更新的判断。
_Avoid_: 写入者 ID、供应商来源、配置指纹

**管理冲突**:
GPTEasy 管理区块的内容、边界或供应商 ID 被外部改动，或者区块重复、嵌套、残缺的状态。
_Avoid_: 外部配置、普通外部修改、自动修复

**旧版管理区块**:
由早期桌面 GPTEasy 写入、仍能识别当前供应商但缺少现行协议版本、管理来源 ID或命令式凭据的完整管理区块；它只在用户下次明确写入时迁移。
_Avoid_: 管理冲突、外部配置、自动迁移

## 恢复与运行

**配置备份**:
GPTEasy 修改当前用户 Codex 环境前，对每个受影响工件的完整内容、存在性和指纹所做的可恢复记录。
_Avoid_: 整个 `.codex` 副本、临时文件

**恢复上次配置**:
用户撤销 GPTEasy 最近一次已完成配置修改，并将环境实际状态协调到恢复结果的操作。
_Avoid_: 备份浏览器、任意文件恢复

**未完成配置操作**:
已经验证并由用户确认、但尚未跨 SQLite 与 Codex 磁盘状态完整提交的唯一配置变更。
_Avoid_: 操作日志、保存草稿、变更历史

**待重启**:
配置已经更新，但切换前的 Codex 消费者仍可能继续使用旧配置的状态。
_Avoid_: 未生效、切换失败

**托盘切换**:
用户从托盘菜单选择已验证供应商并切换当前用户 Codex 环境的快捷操作。
_Avoid_: OpenAI 登录切换、托盘配置、WSL2 快捷切换

**托盘驻留**:
设置窗口关闭后 GPTEasy 继续在系统托盘中运行，直到用户明确退出或 Windows 会话结束。
_Avoid_: 登录启动、关闭即退出

**桌面消费者控制（已移除）**:
GPTEasy 不启动、关闭、终止、激活或重启 ChatGPT/Codex 桌面版。桌面版和 Codex CLI 都只是消费者；配置切换后若旧消费者仍在运行，环境进入被动待重启，用户在原入口自然退出并重新运行后才读取新配置。
_Avoid_: 主动桌面控制、Codex 重启、消费者重启、CLI 重启

## WSL2 与 Linux 环境

**WSL2 发行版环境**:
WSL2 中可以独立拥有 Codex 配置、运行状态和当前供应商的一个 Linux 发行版；它与当前用户 Codex 环境分别管理。
_Avoid_: WSL 容器、Linux 宿主、所有发行版环境

**WSL2 供应商选择**:
用户为一个 WSL2 发行版明确选择已验证供应商，并将该供应商应用到该发行版默认用户 Codex 配置的操作；它不修改 Windows 当前用户 Codex 环境。
_Avoid_: WSL2 全局切换、批量切换、WSL2 登录模式

**WSL2 环境变化**:
WSL2 发行版的注册身份、默认用户、可访问性或生命周期状态相对于 GPTEasy 最近一次探测发生变化的状态；显示名变化本身不改变环境身份，注册身份变化则表示新环境。
_Avoid_: 普通刷新、供应商漂移、自动迁移

**WSL2 环境协调**:
GPTEasy 对 WSL2 发行版的探测快照、已应用证据和未完成操作进行比对，并在变化、消失或恢复后决定继续、停止或要求用户重新确认的过程。
_Avoid_: WSL2 自动修复、环境同步、后台接管

**Linux 脚本导出**:
把 Linux 供应商快照提供给独立 GNU/Linux 或 WSL2 shell 使用的导出操作；加载导出物本身不修改配置，只有用户显式选择供应商后才修改目标 Linux Codex 环境。
_Avoid_: Linux 初始化脚本、自动同步、Linux GUI

**WSL2 共同管理**:
桌面 GPTEasy 与 Linux 导出脚本共同管理同一个 WSL2 发行版环境；任一方完成的供应商切换都会成为该环境的实际状态，并能由另一方识别。
_Avoid_: 最后写入者获胜、两套独立管理、WSL2 外部配置

**Linux 供应商快照**:
一次 Linux 脚本导出时从供应商目录固化的全部已验证供应商集合；供应商目录之后的新增、删除、修改或重排不会自动改变它。
_Avoid_: Linux 供应商列表、同步目录、实时目录

**Linux 导出 ID**:
一次 Linux 脚本导出随机获得的非敏感标识，用于区分当前配置是否来自同一份 Linux 供应商快照。
_Avoid_: 供应商验证指纹、配置指纹、供应商 ID

**Linux 凭据工件**:
Linux 脚本在用户明确选择供应商后，为该供应商的已验证组合生成、并由目标 Linux Codex 环境或恢复点引用的私有不可变 API Key 文件。
_Avoid_: 完整凭据快照、Linux auth.json、共享 Key 文件

**Linux 恢复点**:
Linux 脚本成功切换前保存的目标 Codex 环境原状态；它用于经再次确认后一步覆盖恢复，并在恢复成功后被消费，但不证明恢复内容比当前配置更新。
_Avoid_: 备份浏览器、配置历史、同步记录
