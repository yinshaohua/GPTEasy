# GPTEasy

GPTEasy 是统一 ChatGPT 桌面应用的伴侣程序，帮助用户管理并切换当前用户原生 Codex 环境及 WSL2 环境所使用的 API 服务。

## Language

**宿主应用**:
由 GPTEasy 检测并配置的桌面应用。首版特指统一 ChatGPT 桌面应用中的 Codex/API Key 模式，不包括普通 Chat 登录模式。
_Avoid_: ChatGPT/Codex 桌面版、客户端

**原生 Codex 环境**:
Windows 或 macOS 当前用户的 Codex 配置环境，由统一 ChatGPT 桌面应用中的 Codex 和本机原生 Codex CLI 共同使用。托盘供应商切换作用于此环境。
_Avoid_: 仅桌面应用、宿主应用配置

**旧版宿主应用**:
ChatGPT Classic 或历史上的独立 Codex App。首版只检测其存在并提示迁移，不管理其供应商配置。
_Avoid_: 旧客户端

**供应商**:
一组可切换的 AI 服务访问配置，包含服务地址、凭据和默认模型。
_Avoid_: 渠道、节点、代理

**供应商 ID**:
供应商创建时获得且不会因显示名称、服务地址、凭据或默认模型修改而改变的身份标识。
_Avoid_: 供应商名称、地址指纹

**默认模型**:
供应商切换生效后原生 Codex 环境或 WSL2 环境使用的模型，也是该供应商接受可用性验证时使用的模型。
_Avoid_: 测试模型

**供应商验证**:
确认供应商能够发现默认模型，并能完成 Codex 所需的 Responses API 流式响应和工具调用闭环。
_Avoid_: 连通性测试、接口测试

**已验证供应商**:
当前服务地址、凭据和默认模型已经通过供应商验证的供应商。只有已验证供应商可以保存并参与切换。
_Avoid_: 可用节点、已测试供应商

**内置推荐供应商**:
由 GPTEasy 发布者提供并在设置界面的供应商列表中始终排在最前的供应商模板。它不会自动启用或绕过供应商验证，未完成配置和验证时不能被切换使用，也不显示在托盘菜单中；验证完成后在托盘供应商列表中排在最前。
_Avoid_: 默认供应商、首选供应商

**DayWay**:
首版内置推荐供应商，展示名称为 DayWay，服务地址为 `https://dayway.site/v1`，官网为 `https://dayway.site`，推荐说明为“推荐首选，稳定实惠。价格、模型和可用性以官网及实时验证结果为准。”模板不预设默认模型，用户必须获取当时可用的模型并选择一个完成供应商验证。

**未配置推荐供应商**:
尚未完成凭据、默认模型和供应商验证的内置推荐供应商状态。它仍然排在设置界面的供应商列表最前，但不显示在托盘菜单中，也不能作为原生 Codex 环境或 WSL2 环境的当前供应商。
_Avoid_: 不可用供应商、已保存供应商

**推荐模板更新**:
应用升级带来的 DayWay 模板变化。未配置模板可以直接更新；已验证 DayWay 的服务地址、凭据和默认模型不会被自动覆盖，采用新地址前必须由用户确认并重新验证。
_Avoid_: 推荐配置强制更新、静默覆盖

**供应商目录**:
原生 Codex 环境与 WSL2 环境共同使用的已验证供应商集合。供应商资料只维护一份，但各环境可以选择不同的当前供应商。
_Avoid_: 供应商列表、节点列表

**当前供应商**:
在某个受管环境中已经切换生效的供应商。原生 Codex 环境与 WSL2 环境各自拥有独立的当前供应商。
_Avoid_: 默认供应商、选中供应商

**WSL2 切换**:
在设置界面中为 WSL2 环境选择当前供应商的操作。它独立于原生 Codex 环境切换，不出现在托盘快捷菜单中。
_Avoid_: 同步切换、跟随切换

**WSL2 环境**:
一个独立安装的 WSL2 发行版。每个 WSL2 环境分别拥有当前供应商，也可以通过批量操作切换到同一供应商。
_Avoid_: WSL2、Linux 子系统

**未配置 WSL2 环境**:
已被 GPTEasy 检测到、但用户尚未在设置界面为其选择当前供应商的 WSL2 环境。检测本身不会启动发行版或修改其 Codex 配置。
_Avoid_: 自动同步、继承宿主配置

**WSL2 默认用户**:
一个 WSL2 环境配置的默认 Linux 用户。GPTEasy 首版只管理该用户的 Codex 配置，不扫描或管理同一发行版中的其他用户。
_Avoid_: root 用户、全部 Linux 用户

**WSL2 临时启动**:
用户主动切换一个已停止的 WSL2 环境时，GPTEasy 为完成配置修改而临时启动该发行版，并在成功或失败处理完成后恢复其原有停止状态。
_Avoid_: 后台常驻、自动启用

**WSL2 待重启**:
WSL2 环境的供应商配置已经更新，但该环境中仍有使用旧配置的 Codex 进程。GPTEasy 提示用户重启这些进程，但不主动终止它们。
_Avoid_: 切换失败、强制重启

**登录启动**:
用户登录操作系统后自动运行 GPTEasy 并进入托盘的可选行为。它默认关闭，首次使用时提示用户选择是否启用。
_Avoid_: 默认自启动、强制常驻

**托盘驻留**:
关闭设置窗口后 GPTEasy 继续在托盘中运行的默认行为。首次关闭时提示用户，只有托盘中的明确退出操作才结束程序。
_Avoid_: 后台自启动、关闭即退出

**托盘切换**:
用户从托盘菜单点击已验证供应商后立即切换原生 Codex 环境当前供应商的快捷操作，不进行二次确认，也不提供 WSL2 环境切换。
_Avoid_: 托盘确认、WSL2 快捷切换

**诊断日志**:
GPTEasy 保存在本地、默认保留最近七天的脱敏运行记录，不包含完整请求、模型输出或供应商凭据，也不会默认上传。
_Avoid_: 遥测、请求记录

**诊断导出**:
由用户主动生成的脱敏排障资料，包含应用和环境摘要及诊断日志，但不包含供应商凭据。
_Avoid_: 自动上报、崩溃上传

**更新检查**:
GPTEasy 启动后每天最多执行一次的新版本检查。发现更新时提示用户，由用户确认后才下载和安装，不进行静默安装。
_Avoid_: 强制更新、后台静默安装

**支持平台**:
首版支持 Windows 10 22H2 或更高版本的 x64/ARM64，以及 macOS 14 或更高版本的 Intel/Apple Silicon；Linux 只提供导出的 shell function，不提供独立 GUI 应用。
_Avoid_: 旧版 Windows、Linux 桌面版

**当前用户安装**:
GPTEasy 只为当前操作系统用户安装和更新，不提供整机所有用户安装；应用数据、登录启动、日志和备份也按当前用户隔离。
_Avoid_: 全局安装、管理员安装

**界面语言**:
首版提供简体中文和英语，首次使用时跟随操作系统语言并允许用户切换；Linux 切换脚本使用导出时选择的语言。
_Avoid_: 仅中文、仅英语

**本地模式**:
GPTEasy 不要求产品账户，也不把供应商目录、凭据或当前状态同步到云端；外部 Linux 系统通过用户手工复制的 Linux 切换脚本独立使用。
_Avoid_: 云同步、GPTEasy 账户

**跨设备迁移**:
将 GPTEasy 供应商目录导入另一台 Windows 或 macOS 设备的能力。首版不提供此能力，Linux 切换脚本也不是 GPTEasy 的回导格式。
_Avoid_: 配置同步、脚本导入

**受支持 Shell**:
首版 Linux 切换脚本分别支持 Bash 4 或更高版本和 Zsh 5 或更高版本，不支持 Fish、Nushell 或仅 POSIX sh，也不依赖 Python、Node.js 或 GPTEasy 可执行文件。
_Avoid_: 全部 Shell、通用 sh

**无运行时依赖脚本**:
Linux 切换脚本只依赖受支持 Shell 和常见基础命令，不要求安装 Python、Node.js、第三方解析器或辅助程序。
_Avoid_: Python 脚本、辅助二进制

**GPTEasy 管理区块**:
Codex 配置中由明确注释边界标识、只包含 GPTEasy 所管理供应商字段的区域。Linux function 只替换这些区块，发现区块损坏、重复或存在歧义时停止修改。
_Avoid_: 全文件重写、自动修复 TOML

**供应商凭据**:
供应商授权用户访问其服务的 API Key。为照顾非技术用户，它由 GPTEasy 明文保存和完整显示，并随 WSL2 配置及 Linux 切换脚本明文写出。
_Avoid_: 密钥引用、凭据句柄

**Linux 切换脚本**:
由 GPTEasy 导出的 shell function 集合，包含导出时全部已验证供应商及其明文凭据，不提供供应商子集选择。脚本本身不修改目标系统；用户调用其中的交互式选择 function 后，function 列出可用供应商和当前供应商，用户选择后才修改当前 Linux 用户的 Codex 配置，使脚本可以脱离 GPTEasy 长期独立使用。
_Avoid_: 无密钥脚本、同步脚本

**验证后替换**:
供应商的地址、凭据或默认模型修改后，必须重新通过供应商验证；验证成功才替换原来的已验证配置，失败则继续保留原配置。
_Avoid_: 直接保存、覆盖式编辑

**供应商配置传播**:
已验证供应商更新后，更新后的配置自动提供给所有当前使用该供应商的原生 Codex 环境和 WSL2 环境。
_Avoid_: 重新切换、手工同步

**待重启**:
原生 Codex 环境已经写入新的供应商配置，但仍有桌面 Codex 或本机 Codex CLI 进程使用旧配置，必须重新启动相应进程后才会读取新配置的状态。
_Avoid_: 未生效、切换失败

**宿主应用缺失**:
没有检测到统一 ChatGPT 桌面应用时的产品状态。GPTEasy 可以建议安装；如果本机 Codex CLI 已存在，原生 Codex 环境及托盘切换仍然可用。
_Avoid_: 原生环境缺失、安装失败

**原生 Codex 环境缺失**:
统一 ChatGPT 桌面应用和本机 Codex CLI 都未检测到的状态。此状态不阻止供应商管理、WSL2 管理和 Linux 切换脚本导出，但原生环境托盘切换暂时不可用。
_Avoid_: 宿主应用缺失、GPTEasy 不可用

**Linux 用户级配置**:
某个 Linux 用户自己的 Codex 配置状态，由 Linux 切换脚本中的 function 修改；它独立于 GPTEasy 后续运行状态，也独立于 Windows 原生 Codex 环境和 WSL2 环境。已经运行的 Codex 需要重启后才会读取新的状态。
_Avoid_: 系统级配置、临时环境变量

**Linux 供应商选择**:
Linux 切换脚本提供的交互操作，展示当前供应商和脚本中可用的供应商；用户可以选择一个供应商激活，也可以直接退出而不修改配置。
_Avoid_: 命令别名、自动切换

**配置保留**:
切换供应商只改变供应商相关配置，用户在 Codex 中的其他设置继续保留，不因供应商切换而被覆盖或删除。
_Avoid_: 重置配置、重新生成配置

**配置备份**:
宿主应用、每个 WSL2 环境和独立 Linux function 在实际修改 Codex 配置前自动保存带时间戳的副本，每个环境默认保留最近五份，以便在切换失败或结果不符合预期时恢复。
_Avoid_: 临时文件、配置快照

**供应商删除**:
从 GPTEasy 供应商目录移除供应商的操作。当前仍被宿主应用或任一 WSL2 环境使用的供应商不能直接删除，外部 Linux 脚本不受影响。
_Avoid_: 删除节点、清空配置

**验证时间**:
供应商最近一次完整供应商验证成功的时间。它用于说明已验证状态的新鲜程度，不代表 GPTEasy 会持续监控供应商健康状况。
_Avoid_: 在线状态、实时健康度

**标准供应商**:
只使用服务地址、API Key 和默认模型，并满足模型发现、Responses API 流式响应及工具调用闭环的供应商。首版不为它增加自定义 Header、组织或项目标识等高级认证字段。
_Avoid_: 通用 OpenAI 兼容服务、任意代理

**安全服务地址**:
远程供应商必须使用 HTTPS；只有 `localhost`、`127.0.0.1` 和 `[::1]` 回环地址允许使用 HTTP，且不提供绕过开关。
_Avoid_: 远程 HTTP、忽略传输风险

**直接配置模式**:
GPTEasy 通过修改各受管环境的 Codex 配置来切换供应商，不在请求链路中运行本地 API 代理网关。已经运行的 Codex 需要重新启动后读取新配置。
_Avoid_: 本地中转、内置网关

**当前用户配置**:
GPTEasy 只管理运行 GPTEasy 的当前操作系统用户对应的 Codex 配置，不管理机器级公共配置，也不支持在首版中指定其他配置路径。
_Avoid_: 全局配置、管理员配置、自定义配置路径

**首次自动切换**:
首次供应商验证成功后，GPTEasy 自动将该供应商设为原生 Codex 环境的当前供应商并切换到代理 API 模式；如果原生 Codex 环境正在运行，则进入待重启状态。
_Avoid_: 首次确认切换、手工启用

**后续供应商保存**:
已经存在已验证供应商时，新供应商验证成功后只加入供应商目录，不自动改变原生 Codex 环境或任何 WSL2 环境的当前供应商。
_Avoid_: 新增即切换、推荐供应商强制切换

**切换预确认**:
原生 Codex 环境正在运行时，用户主动切换供应商会在写入配置前选择立即重启、稍后重启或取消；取消不会修改配置。已经确认保存的供应商配置传播则在写入后只选择立即或稍后重启。
_Avoid_: 写入后取消、静默切换

**外部配置**:
默认 Codex 配置中存在但无法匹配 GPTEasy 已保存供应商的当前状态。GPTEasy 读取并展示它，不自动覆盖，也不将其视为已验证供应商。
_Avoid_: 未知供应商、配置异常

**代理 API 模式**:
原生 Codex 环境通过供应商目录中的服务地址、凭据和默认模型访问服务的认证模式。它是 GPTEasy 的默认模式。
_Avoid_: 第三方登录、原厂模式

**原厂登录模式**:
原生 Codex 环境直接使用 OpenAI 官方账户登录的认证模式。用户可以主动启用，但 GPTEasy 默认不启用，启用后供应商切换暂时不参与原生 Codex 环境运行。
_Avoid_: 官方模式、OAuth 模式
