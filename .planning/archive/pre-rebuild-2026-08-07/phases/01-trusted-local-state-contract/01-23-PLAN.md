---
phase: 01-trusted-local-state-contract
plan: 23
type: execute
wave: 15
depends_on: [01-16, 01-19, 01-22]
files_modified:
  - src-tauri/src/state/backup.rs
  - src-tauri/src/state/migrations/mod.rs
  - src-tauri/src/state/mod.rs
  - src-tauri/tests/migration_failure.rs
  - src-tauri/tests/backup_restore.rs
  - src-tauri/tests/state_concurrency.rs
autonomous: true
requirements: [STATE-03, STATE-04]
must_haves:
  truths:
    - "迁移前使用 SQLite Online Backup API 创建 snapshot，并由统一只读 validator 验证 identity。"
    - "只保留最近三份 verified backups；同一未变 source/target 的重复失败复用同一 snapshot。"
    - "全部 pending migrations 在一个 BEGIN IMMEDIATE 中执行；失败回到起始版本，并发进程不能交错 backup/migrate。"
  artifacts:
    - path: "src-tauri/src/state/backup.rs"
      provides: "BackupIdentity、Online Backup、reuse 与三份 retention"
    - path: "src-tauri/src/state/migrations/mod.rs"
      provides: "全 pending 单一 transaction 与受 policy 约束的 transforms"
    - path: "src-tauri/src/state/mod.rs"
      provides: "coordinator→backup→migration 固定 wiring"
    - path: "src-tauri/tests/migration_failure.rs"
      provides: "最后一步故障与全版本 rollback"
    - path: "src-tauri/tests/backup_restore.rs"
      provides: "snapshot identity、retention、reuse 与 replacement rejection"
    - path: "src-tauri/tests/state_concurrency.rs"
      provides: "两进程 migrate/backup 序列化与 non-mutation"
  key_links:
    - from: "src-tauri/src/state/backup.rs"
      to: "src-tauri/src/state/validation.rs"
      via: "snapshot 关闭后按 ExpectedSnapshotIdentity 只读验证"
      pattern: "ExpectedSnapshotIdentity"
  assumptions:
    - "FLAGGED_UNVERIFIED [STATE-03]: specless edge probe 为 unclassified；全 pending 单事务、事务内版本复核和 migration policy 回归作为 backstop。"
    - "FLAGGED_UNVERIFIED [STATE-04]: specless edge probe 为 unclassified；verified snapshot、三份 retention、reuse 与末步故障回滚作为 backstop。"
  prohibitions:
    - "FLAGGED_UNVERIFIED: 不得用普通文件复制代替 SQLite Online Backup API。"
    - "FLAGGED_UNVERIFIED: 迁移失败不得删除、清空或重建用户数据库继续运行。"
---

<objective>
实现批准后的 verified SQLite backup、三份 retention、全 pending 单事务 rollback 与并发协调。

Purpose: 完整交付 STATE-04，并把 STATE-03 的顺序迁移变成可恢复、不可交错的生产路径。
Output: BackupManager、migration transaction、故障/retention/reuse/并发测试。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0006-sqlite-application-state.md
@.planning/phases/01-trusted-local-state-contract/01-16-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-19-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-22-SUMMARY.md
</context>

## Artifacts this phase produces

- `src-tauri/src/state/backup.rs`：BackupIdentity、Online Backup、reuse 与三份 retention
- `src-tauri/src/state/migrations/mod.rs`：全 pending 单一 transaction 与受 policy 约束的 transforms
- `src-tauri/src/state/mod.rs`：coordinator→backup→migration 固定 wiring
- `src-tauri/tests/migration_failure.rs`：最后一步故障与全版本 rollback
- `src-tauri/tests/backup_restore.rs`：snapshot identity、retention、reuse 与 replacement rejection
- `src-tauri/tests/state_concurrency.rs`：两进程 migrate/backup 序列化与 non-mutation

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 verified BackupIdentity、reuse 与三份 retention</name>
  <read_first>
    @src-tauri/src/state/validation.rs
    @src-tauri/src/state/preflight.rs
    @src-tauri/src/state/coordination.rs
    @.planning/phases/01-trusted-local-state-contract/01-16-SUMMARY.md
    @docs/adr/0006-sqlite-application-state.md
  </read_first>
  <precondition>01-16-SUMMARY.md 记录 approve-db-backup-contract。</precondition>
  <reversibility rating="one-way">backup 文件名、sidecar identity 与 retention/reuse 规则会成为历史恢复兼容面。</reversibility>
  <files>src-tauri/src/state/backup.rs, src-tauri/tests/backup_restore.rs</files>
  <behavior>
    - Online Backup snapshot 关闭后由独立只读 connection 通过完整 validator。
    - 只有 verified backup 参与排序/裁剪，顺序不依赖文件 mtime。
    - 同 database UUID/source digest/from-to/fingerprint 可复用；任一变化必须新建。
  </behavior>
  <action>在持有 StateCoordinator 时用 rusqlite::backup::Backup 写同一 backup root 的临时 snapshot，sync/close 后计算 digest，并以 01-16 批准的 UTC+schema+database UUID+backup UUID 命名和 sidecar 原子发布。调用 validate_database_contract(ExpectedSnapshotIdentity) 后才标记 verified、参与 reuse 或 retention；按合同字段排序保留最近三份。红测覆盖第四份裁剪、重复失败复用、未变 source 复用、source 改变、sidecar/file 替换、digest/identity 错配与未验证文件排除。</action>
  <acceptance_criteria>backup 可独立打开并验证身份；只留最近三份 verified snapshots；重复失败不挤掉真实历史。</acceptance_criteria>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test backup_restore</automated></verify>
  <done>备份格式、身份、复用和 retention 与批准合同一致。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现全 pending rollback 与并发 migrate/backup 协调</name>
  <read_first>
    @src-tauri/src/state/migrations/mod.rs
    @src-tauri/src/state/mod.rs
    @src-tauri/src/state/coordination.rs
    @scripts/contracts/verify-migration-policy.ps1
    @src-tauri/tests/state_concurrency.rs
  </read_first>
  <files>src-tauri/src/state/migrations/mod.rs, src-tauri/src/state/mod.rs, src-tauri/tests/migration_failure.rs, src-tauri/tests/state_concurrency.rs</files>
  <behavior>
    - validate→coordinator→backup→BEGIN IMMEDIATE 顺序固定。
    - 最后一个 pending migration 注入故障后 schema/rows/ledger/metadata/user_version 全部保持起始版本。
    - 一个进程暂停在 backup 或 migrate 时，第二进程返回 StateBusy 且不产生额外 backup。
  </behavior>
  <action>StateCoordinator guard 覆盖 backup 与整个 migration。事务内重读 user_version/database identity，按 registry 顺序执行全部 pending SQL/constrained transforms，逐项写 ledger/checksum，最后更新 fingerprint/user_version，再运行 FK/quick_check 并 commit。红测从 v001 应用 test-only v2/v3，在最后一步故障；对 DB、行、ledger、metadata 与备份集合做前后比较。扩展两进程测试覆盖 pause-at-backup、pause-in-transaction、holder crash 和释放后正常升级。</action>
  <acceptance_criteria>任何 pending migration 失败都完整回到起始 schema；并发进程不交错 backup/migration；释放后可正常升级。</acceptance_criteria>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test migration_failure --test backup_restore --test state_concurrency</automated></verify>
  <done>STATE-03/04 的迁移前备份、完整回滚和多进程串行化全部成立。</done>
</task>

</tasks>

<threat_model>
Security level: ASVS L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 旧数据库 → backup/migration | 备份、迁移和并发进程共同影响唯一用户数据库。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-56 | Tampering | backup replacement | high | mitigate | snapshot digest、sidecar identity 与统一 validator。 |
| T-01-57 | Tampering | migration transaction | critical | mitigate | 单一 BEGIN IMMEDIATE、policy lint 与末步故障测试。 |
| T-01-58 | Denial of Service | concurrent migrate/backup | high | mitigate | StateCoordinator 与两进程矩阵。 |
</threat_model>

<verification>
- backup_restore、migration_failure、state_concurrency 返回 0。
</verification>

<success_criteria>
- STATE-04 的 snapshot/retention/reuse/rollback/concurrency 全部自动证明。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-23-SUMMARY.md`
</output>
