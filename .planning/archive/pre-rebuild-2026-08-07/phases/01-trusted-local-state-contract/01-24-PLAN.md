---
phase: 01-trusted-local-state-contract
plan: 24
type: execute
wave: 16
depends_on: [01-23]
files_modified:
  - src-tauri/src/state/recovery.rs
  - src-tauri/src/state/preflight.rs
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
  - src-tauri/tests/higher_schema_refusal.rs
  - src-tauri/tests/recovery_validation.rs
  - src-tauri/tests/installed_recovery_smoke.rs
  - src-tauri/tests/state_concurrency.rs
autonomous: true
requirements: [STATE-04, STATE-05]
must_haves:
  truths:
    - "higher schema 与任何 DB contract failure 在 RW 前进入 RecoveryOnly，相关文件完全不变。"
    - "restore 只接受后端枚举 opaque ID，先生成 verified quarantine，再原子恢复并重跑 preflight。"
    - "recovery smoke 使用与 state smoke 不同 run ID，prepare/refuse/restore/verify/cleanup 分离，verify 不清理。"
  artifacts:
    - path: "src-tauri/src/state/recovery.rs"
      provides: "compatible backup listing、verified quarantine 与 atomic restore"
    - path: "src-tauri/src/state/preflight.rs"
      provides: "higher/invalid database RecoveryOnly decision"
    - path: "src-tauri/src/commands.rs"
      provides: "list/restore narrow commands 与普通写拒绝"
    - path: "src-tauri/src/lib.rs"
      provides: "phase1-recovery-smoke 五模式 wiring"
    - path: "src-tauri/tests/higher_schema_refusal.rs"
      provides: "DB/WAL/SHM/backup/quarantine non-mutation tests"
    - path: "src-tauri/tests/recovery_validation.rs"
      provides: "replaced/truncated/incompatible backup 与 quarantine tests"
    - path: "src-tauri/tests/installed_recovery_smoke.rs"
      provides: "五模式、多进程、独立 run ID 与保留状态 tests"
    - path: "src-tauri/tests/state_concurrency.rs"
      provides: "两进程 restore/migrate/backup 协调测试"
  key_links:
    - from: "src-tauri/src/state/recovery.rs"
      to: "src-tauri/src/state/validation.rs"
      via: "restore 前验证 backup、替换前验证 quarantine、替换后重跑 preflight"
      pattern: "validate_database_contract"
  assumptions:
    - "FLAGGED_UNVERIFIED [STATE-04]: specless edge probe 为 unclassified；恢复前 verified quarantine 与失败保留当前 DB 作为显式 backstop。"
    - "FLAGGED_UNVERIFIED [STATE-05]: specless edge probe 为 unclassified；higher-schema 全文件 non-mutation、opaque backup restore 与新进程 verify 作为显式 backstop。"
  prohibitions:
    - "FLAGGED_UNVERIFIED: higher-schema/invalid DB 路径不得打开 RW、创建 journal/backup/quarantine 或调用普通写 command。"
    - "FLAGGED_UNVERIFIED: restore 不得接受任意文件路径，也不得在无法验证保全当前 DB 时继续替换。"
---

<objective>
实现 higher-schema non-mutating refusal、opaque backup restore、verified quarantine 与 installed recovery smoke。

Purpose: 完整交付 STATE-05，并提供最终平台工作流可组合、不会提前清理的真实恢复入口。
Output: recovery service/commands、拒写/恢复/并发 tests 与五个 headless modes。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0006-sqlite-application-state.md
@.planning/phases/01-trusted-local-state-contract/01-16-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-23-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
</context>

## Artifacts this phase produces

- `src-tauri/src/state/recovery.rs`：compatible backup listing、verified quarantine 与 atomic restore
- `src-tauri/src/state/preflight.rs`：higher/invalid database RecoveryOnly decision
- `src-tauri/src/commands.rs`：list/restore narrow commands 与普通写拒绝
- `src-tauri/src/lib.rs`：phase1-recovery-smoke 五模式 wiring
- `src-tauri/tests/higher_schema_refusal.rs`：DB/WAL/SHM/backup/quarantine non-mutation tests
- `src-tauri/tests/recovery_validation.rs`：replaced/truncated/incompatible backup 与 quarantine tests
- `src-tauri/tests/installed_recovery_smoke.rs`：五模式、多进程、独立 run ID 与保留状态 tests
- `src-tauri/tests/state_concurrency.rs`：两进程 restore/migrate/backup 协调测试

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 higher-schema 与合同失败的 non-mutating RecoveryOnly</name>
  <read_first>
    @src-tauri/src/state/preflight.rs
    @src-tauri/src/state/validation.rs
    @src-tauri/src/commands.rs
    @src-tauri/src/lib.rs
    @docs/adr/0006-sqlite-application-state.md
  </read_first>
  <files>src-tauri/src/state/preflight.rs, src-tauri/src/commands.rs, src-tauri/src/lib.rs, src-tauri/tests/higher_schema_refusal.rs</files>
  <behavior>
    - higher/application/integrity/ledger/fingerprint/identity failure 不进入 RW。
    - 普通写 commands 稳定返回脱敏拒绝错误。
    - 调用前后 DB/WAL/SHM/backup/quarantine 的存在性、hash、长度、mtime 不变。
  </behavior>
  <action>扩展 BootstrapState/PublicStoreError，保持 existing DB 第一连接只读；RecoveryOnly 只注册 bootstrap、compatible-backup list seam 和普通写拒绝，restore 实现由 Task 2 补齐。红测预先记录所有相关文件，调用 bootstrap 和每个普通写 command，证明没有 RW、journal、backup、quarantine 或清理副作用；错误只含 found/supported version 和稳定 code。</action>
  <acceptance_criteria>未来或无效数据库进入 RecoveryOnly，普通写不可用，相关文件逐项完全不变。</acceptance_criteria>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test higher_schema_refusal</automated></verify>
  <done>旧应用遇到更高或不可信 DB 时 fail-closed 且 non-mutating。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 opaque restore、verified quarantine 与跨进程协调</name>
  <read_first>
    @src-tauri/src/state/backup.rs
    @src-tauri/src/state/validation.rs
    @src-tauri/src/state/coordination.rs
    @.planning/phases/01-trusted-local-state-contract/01-16-SUMMARY.md
  </read_first>
  <precondition>01-16-SUMMARY.md 记录 approve-db-backup-contract。</precondition>
  <reversibility rating="one-way">restore/quarantine 消费已批准的历史 backup identity；改变语义需兼容已存在 snapshots。</reversibility>
  <files>src-tauri/src/state/recovery.rs, src-tauri/src/commands.rs, src-tauri/tests/recovery_validation.rs, src-tauri/tests/state_concurrency.rs</files>
  <behavior>
    - unknown opaque ID、任意路径、replaced/truncated/incompatible backup 被拒绝。
    - quarantine 创建或验证失败时当前 DB 保持原位。
    - restore 持有 StateCoordinator；并发 migrate/backup/restore 不能交错。
  </behavior>
  <action>list_compatible_backups 只返回后端生成的 opaque ID/脱敏摘要。restore 将 ID 映射到 canonical backup root，重新验证 sidecar、actual digest、database/application/schema identity；取得 coordinator 后用 Online Backup 保存当前 newer DB 为 quarantine并完整验证，随后以同目录临时文件和平台原子替换恢复 snapshot，最后重跑 preflight。红测覆盖路径注入、unknown ID、替换、截断、不兼容、quarantine 故障、替换故障及 restore 与 migrate/backup 竞争。</action>
  <acceptance_criteria>只有当前扫描并重新验证的兼容 backup 可恢复；当前较新 DB 在任何替换前有 verified quarantine；失败不丢唯一副本。</acceptance_criteria>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test recovery_validation --test state_concurrency</automated></verify>
  <done>降级恢复具有 opaque、validated、quarantine-first 与跨进程串行化语义。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 提供 installed recovery 五模式与显式 cleanup</name>
  <read_first>
    @src-tauri/src/lib.rs
    @src-tauri/tests/installed_state_smoke.rs
    @src-tauri/src/state/recovery.rs
    @src-tauri/tests/higher_schema_refusal.rs
  </read_first>
  <files>src-tauri/src/lib.rs, src-tauri/tests/installed_recovery_smoke.rs</files>
  <behavior>
    - prepare 创建 compatible backup 后构造固定 higher-schema DB；refuse 在新进程验证 non-mutation。
    - restore 与 verify 由不同新进程执行，verify 后状态/backup/quarantine 仍存在。
    - cleanup 是唯一删除入口，recovery run ID 与 state run ID 必须不同。
  </behavior>
  <action>增加 phase1-recovery-smoke prepare/refuse/restore/verify/cleanup，只接受 opaque recovery run ID。prepare 使用固定假数据与批准的 backup contract；restore 只消费后端枚举 ID。所有模式输出 allowlist 摘要和不可逆 digest，不在 prepare/refuse/restore/verify 中清理。测试组合 01-19 state smoke 的不同 run ID，并断言 cleanup 只有显式调用才删除对应 run-scoped 工件。</action>
  <acceptance_criteria>五个模式可由独立进程按序调用，verify 前无清理，两个 run ID 不同且只能清理各自工件。</acceptance_criteria>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test installed_recovery_smoke --test higher_schema_refusal --test recovery_validation</automated></verify>
  <done>最终平台 workflow 拥有 fixed、truthful、可组合的完整状态/恢复入口。</done>
</task>

</tasks>

<threat_model>
Security level: ASVS L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recovery command → 当前/备份数据库 | 降级恢复会替换唯一用户数据库并处理更高版本副本。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-59 | Tampering | higher-schema open | critical | mitigate | read-only full preflight 与全文件 non-mutation tests。 |
| T-01-60 | Tampering | backup ID/path | high | mitigate | opaque ID、canonical root 与 identity validation。 |
| T-01-61 | Tampering | quarantine/restore | critical | mitigate | verified quarantine before atomic replace；失败保留原 DB。 |
| T-01-62 | Denial of Service | concurrent restore | high | mitigate | StateCoordinator 与两进程 restore matrix。 |
</threat_model>

<verification>
- higher_schema_refusal、recovery_validation、installed_recovery_smoke、state_concurrency 返回 0。
</verification>

<success_criteria>
- STATE-05 拒写/恢复及最终 headless recovery 入口全部自动通过。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-24-SUMMARY.md`
</output>
