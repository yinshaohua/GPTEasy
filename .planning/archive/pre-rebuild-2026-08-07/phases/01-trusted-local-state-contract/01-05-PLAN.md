---
phase: 01-trusted-local-state-contract
plan: 05
type: execute
wave: 2
depends_on: [01-03]
files_modified:
  - tests/fixtures/contracts/gh-preflight-cases.json
  - scripts/contracts/preflight-gh-evidence.ps1
  - scripts/contracts/test-gh-preflight.ps1
autonomous: true
requirements:
  - STATE-02
  - STATE-03
  - STATE-04
  - STATE-05
must_haves:
  truths:
    - "gh 版本低于 2.49.0、未认证或目标仓库不可读时阻断。"
    - "Actions runs/artifacts 与 repository attestation endpoint 的 401/403 均阻断。"
    - "preflight 输出不显示 token、认证头或完整 gh 配置。"
  artifacts:
    - path: "tests/fixtures/contracts/gh-preflight-cases.json"
      provides: "版本、认证、repo/actions/attestation 权限正反 transcript"
    - path: "scripts/contracts/preflight-gh-evidence.ps1"
      provides: "固定最低版本与 read-only 权限检查"
    - path: "scripts/contracts/test-gh-preflight.ps1"
      provides: "mock gh 输出、脱敏与退出码测试"
  key_links:
    - from: "scripts/contracts/preflight-gh-evidence.ps1"
      to: "scripts/contracts/verify-evidence-provenance.ps1"
      via: "任何真实 gh 下载/验签之前必须返回 0"
      pattern: "2.49.0"
---

<objective>
实现固定最低版本、认证与仓库权限的 gh evidence preflight。

Purpose: 在任何 artifact 下载或 attestation 验证前显式阻断缺失工具/权限。
Output: preflight policy、脚本与注入式自测。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
@docs/agents/issue-tracker.md
</context>

## Artifacts this phase produces

- `tests/fixtures/contracts/gh-preflight-cases.json`：版本、认证、repo/actions/attestation 权限正反 transcript
- `scripts/contracts/preflight-gh-evidence.ps1`：固定最低版本与 read-only 权限检查
- `scripts/contracts/test-gh-preflight.ps1`：mock gh 输出、脱敏与退出码测试

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 固定 gh 版本/认证/权限 preflight</name>
  <files>tests/fixtures/contracts/gh-preflight-cases.json, scripts/contracts/preflight-gh-evidence.ps1</files>
  <behavior>
    - gh version 必须至少 2.49.0，且 gh attestation verify --help 可用。
    - gh auth status、repository read、Actions runs/artifacts read 均成功。
    - 对 attestation digest endpoint 的 404 视为有权限但对象不存在，401/403 视为阻断。
  </behavior>
  <action>实现只读 preflight，固定 repository 为 yinshaohua/GPTEasy；检查版本、认证、repo metadata、Actions runs/artifacts API 与 artifact attestations endpoint。任何输出先字段 allowlist，再写摘要；不得调用会显示 token 的选项。支持注入 GhFixture 以运行同一 parser。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/preflight-gh-evidence.ps1 -SelfTest</automated></verify>
  <done>工具、认证和最小权限有类型化 pass/blocked 结果。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 证明权限缺失和敏感输出 fail-closed</name>
  <files>scripts/contracts/test-gh-preflight.ps1</files>
  <behavior>
    - 旧版本、未认证、repo 403、Actions 403、attestation 403 分别失败。
    - 404 attestation probe 可证明权限存在但不声称 artifact 已验证。
    - token canary 不出现在摘要或错误。
  </behavior>
  <action>对 fixture transcript 逐项调用生产 parser；加入含 token/auth header 的恶意 stderr，断言只输出稳定 error code。自测不访问网络。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/test-gh-preflight.ps1</automated></verify>
  <done>真实 checkpoint 前已有完整可执行负例。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本机 gh → GitHub 仓库 | 本地工具身份与权限决定能否独立验证外部证据。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10 | Spoofing | gh authentication | high | mitigate | 固定 hostname、auth status 与 repository API 交叉检查。 |
| T-01-11 | Information Disclosure | gh token | high | mitigate | 禁止 token 输出选项并对 stderr 执行 canary/allowlist。 |
</threat_model>

<verification>
- preflight 自测返回 0。
- 所有缺失权限负例返回非零。
</verification>

<success_criteria>
- 真实 evidence 操作拥有明确的工具与权限前置条件。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-05-SUMMARY.md`
</output>
