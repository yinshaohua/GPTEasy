---
phase: 01-trusted-local-state-contract
plan: 10
type: execute
wave: 5
depends_on: [01-09]
files_modified:
  - src-tauri/src/path_smoke.rs
  - src-tauri/tests/path_smoke.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - STATE-01
  - STATE-02
must_haves:
  truths:
    - "调用者只能提供 1–64 位 ASCII 字母数字/连字符 opaque ID。"
    - "同一 ID 的两次独立运行得到 reopened=false 与 true，且不输出绝对用户路径。"
  artifacts:
    - path: "src-tauri/src/path_smoke.rs"
      provides: "固定状态根的非敏感 path smoke"
    - path: "src-tauri/tests/path_smoke.rs"
      provides: "opaque ID、越界与两次独立运行测试"
    - path: "src-tauri/src/lib.rs"
      provides: "真实应用 CLI path-smoke wiring"
  key_links:
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/src/path_smoke.rs"
      via: "phase1-path-smoke 只传 opaque ID 与 AppHandle"
      pattern: "phase1-path-smoke"
---

<objective>
用真实 Tauri AppHandle 建立固定 app_local_data_dir path smoke。

Purpose: 先证明当前用户状态根、opaque ID 与独立进程重开语义。
Output: path smoke module、应用 wiring 与 Rust tests。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trusted-local-state-contract/01-PATTERNS.md
@.planning/phases/01-trusted-local-state-contract/01-09-SUMMARY.md
</context>

## Artifacts this phase produces

- `src-tauri/src/path_smoke.rs`：固定状态根的非敏感 path smoke
- `src-tauri/tests/path_smoke.rs`：opaque ID、越界与两次独立运行测试
- `src-tauri/src/lib.rs`：真实应用 CLI path-smoke wiring

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现固定状态根的 path smoke</name>
  <files>src-tauri/src/path_smoke.rs, src-tauri/src/lib.rs</files>
  <behavior>
    - 合法 ID 只写 app_local_data_dir 下固定 contract-smoke/path 子目录。
    - 路径分隔符、点段、Unicode 或超长 ID 被拒绝。
    - 报告只含 run ID、OS/arch、schema 常量与 reopened。
  </behavior>
  <action>通过 AppHandle PathResolver 获取唯一状态根；文件名只由 opaque ID 派生。第一次原子写固定非敏感 JSON，后续读取并报告 reopened；生产 CLI 不接受路径、配置正文或环境值。</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --lib path_smoke</automated></verify>
  <done>path smoke 只能触达固定产品根。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 执行真实 AppHandle 与独立进程重开测试</name>
  <files>src-tauri/tests/path_smoke.rs</files>
  <behavior>
    - 第一次 reopened=false，第二次 reopened=true。
    - 临时根之外没有文件变化。
  </behavior>
  <action>使用 Tauri mock app/runtime 构造真实 AppHandle；测试用当前 integration-test executable 的两个子进程运行同一 opaque ID，记录临时目录树并覆盖无效 ID。验证命令只读断言已生成的临时状态，不修改 tracked 文件。</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test path_smoke</automated></verify>
  <done>Rust 测试实际证明 fixed-root 与跨进程 reopen。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI smoke → 当前用户状态根 | 测试入口若接受路径会越权读写用户文件。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-20 | Tampering | opaque ID | high | mitigate | 严格字符/长度校验与固定子目录。 |
| T-01-21 | Information Disclosure | path report | medium | mitigate | 不输出绝对路径、环境值或用户内容。 |
</threat_model>

<verification>
- path_smoke integration test 返回 0。
</verification>

<success_criteria>
- 当前用户状态根和重开语义可被平台包复用。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-10-SUMMARY.md`
</output>
