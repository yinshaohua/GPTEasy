---
phase: 01-trusted-local-state-contract
plan: 12
type: execute
wave: 3
depends_on: [01-04]
files_modified:
  - scripts/contracts/probe-codex-macos.zsh
  - scripts/contracts/probe-macos-host.zsh
  - scripts/contracts/test-macos-contract-probes.zsh
  - .github/workflows/phase1-macos-wave0.yml
  - scripts/contracts/test-macos-wave0-contract.ps1
autonomous: true
requirements:
  - STATE-02
  - STATE-03
  - STATE-05
must_haves:
  truths:
    - "macOS Wave 0 在 Intel 与 Apple Silicon 上执行 zsh -n 和 zsh verifier tests。"
    - "后续 macOS evidence jobs 以 needs 依赖 Wave 0，不可跳过。"
    - "CLI 与正式宿主 bundled Codex 对同一默认用户层 canary 的摘要一致。"
  artifacts:
    - path: "scripts/contracts/probe-codex-macos.zsh"
      provides: "macOS Codex 0.146.1 app-server/config-read probe"
    - path: "scripts/contracts/probe-macos-host.zsh"
      provides: "正式 bundle/bundled Codex 与 CLI parity probe"
    - path: "scripts/contracts/test-macos-contract-probes.zsh"
      provides: "原生 zsh syntax、控制流和 parity 正反测试"
    - path: ".github/workflows/phase1-macos-wave0.yml"
      provides: "Intel/Apple Silicon 可复用 zsh Wave 0 workflow"
    - path: "scripts/contracts/test-macos-wave0-contract.ps1"
      provides: "读取 workflow 与全部 zsh consumer 的静态 dependency 契约测试"
  key_links:
    - from: ".github/workflows/phase1-macos-evidence.yml"
      to: ".github/workflows/phase1-macos-wave0.yml"
      via: "job-level uses 与 needs: wave0"
      pattern: "needs: wave0"
---

<objective>
建立 macOS 原生 zsh Wave 0 与 CLI/正式宿主共享配置 contract。

Purpose: 让 zsh 语法/控制流在任何 macOS evidence job 前真实执行。
Output: 两个 probe、zsh tests、可复用 Wave 0 workflow 与本地契约检查。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
@.codex/skills/spike-findings-gpteasy/references/macos-host-contract.md
</context>

## Artifacts this phase produces

- `scripts/contracts/probe-codex-macos.zsh`：macOS Codex 0.146.1 app-server/config-read probe
- `scripts/contracts/probe-macos-host.zsh`：正式 bundle/bundled Codex 与 CLI parity probe
- `scripts/contracts/test-macos-contract-probes.zsh`：原生 zsh syntax、控制流和 parity 正反测试
- `.github/workflows/phase1-macos-wave0.yml`：Intel/Apple Silicon 可复用 zsh Wave 0 workflow
- `scripts/contracts/test-macos-wave0-contract.ps1`：读取 workflow 与全部 zsh consumer 的静态 dependency 契约测试

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 macOS CLI/宿主 probes、原生 zsh tests 与必须先行的 Wave 0</name>
  <files>scripts/contracts/probe-codex-macos.zsh, scripts/contracts/probe-macos-host.zsh, scripts/contracts/test-macos-contract-probes.zsh, .github/workflows/phase1-macos-wave0.yml, scripts/contracts/test-macos-wave0-contract.ps1</files>
  <behavior>
    - Darwin 14+ 与目标原生 arch 才能产生 native verdict。
    - official CLI 与正式 Codex.app/ChatGPT.app bundled binary 读取同一默认 ~/.codex canary。
    - zsh tests 覆盖 host 缺失、架构错、root/origin/provider/carrier 错配。
  </behavior>
  <action>实现 zsh probe，输出字段与 Windows manifest schema 对齐；使用固定非敏感 canary，禁止原始 config/read、完整命令行和用户绝对路径。test script 首先对自身及两个 probe 执行 zsh -n，再运行注入 fixture 的正反 predicate。创建 workflow_call + workflow_dispatch 的 Intel/Apple Silicon 原生 matrix；每个 job exact checkout 后运行 zsh -n 与 zsh contract tests，并在 package verifier 文件出现后执行其 zsh tests。PowerShell 契约测试读取本任务全部脚本与 workflow，验证 Wave 0 自身可复用、双架构、语法与测试步骤完整；证据 workflow 对 Wave 0 的消费由 01-14 在其文件创建后验证。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/test-macos-wave0-contract.ps1 -Scripts scripts/contracts/probe-codex-macos.zsh,scripts/contracts/probe-macos-host.zsh,scripts/contracts/test-macos-contract-probes.zsh -Workflow .github/workflows/phase1-macos-wave0.yml</automated></verify>
  <done>修改的 zsh 文件、PowerShell 契约测试与原生 Wave 0 在同一任务内创建；后续 evidence 有可执行先行依赖。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macOS zsh/宿主 → native evidence | Windows synthetic 聚合不能证明 zsh 语法或真实宿主共享边界。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-25 | Spoofing | non-native macOS verdict | high | mitigate | Darwin/version/arch 校验与双架构原生 Wave 0。 |
| T-01-26 | Tampering | zsh control flow | high | mitigate | zsh -n 与原生 fixture tests 先于 evidence jobs。 |
| T-01-27 | Information Disclosure | macOS config evidence | high | mitigate | 固定 canary 与 allowlist 摘要。 |
</threat_model>

<verification>
- 本地 dependency contract 通过；原生 evidence workflow 必须实际运行 Wave 0。
</verification>

<success_criteria>
- macOS 语法/单元执行不再由 Windows synthetic 代替。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-12-SUMMARY.md`
</output>
