---
phase: 01-trusted-local-state-contract
plan: 21
type: execute
wave: 13
depends_on: [01-20]
files_modified:
  - src-tauri/tests/migration_matrix.rs
  - tests/fixtures/databases/history-lock.json
  - scripts/contracts/verify-migration-history.ps1
  - scripts/contracts/verify-migration-policy.ps1
  - tests/fixtures/migrations/forbidden-migration-cases.json
autonomous: true
requirements: [STATE-03]
must_haves:
  truths:
    - "migration_matrix 从 manifest 枚举所有已存在 fixture，复制到 TempDir 后升级，仓库原件保持只读。"
    - "history lock 固定 migration SQL、fixture 与 schema fingerprint，并与可用 release tags 交叉比较。"
    - "VACUUM、ATTACH、DETACH、PRAGMA journal_mode 及 Rust transform 外部文件/进程副作用由生产 lint 拒绝。"
  artifacts:
    - path: "src-tauri/tests/migration_matrix.rs"
      provides: "manifest-driven 全历史顺序迁移矩阵"
    - path: "tests/fixtures/databases/history-lock.json"
      provides: "migration SQL/fixture/schema fingerprint 不可变 lock"
    - path: "scripts/contracts/verify-migration-history.ps1"
      provides: "工作树与 release tag 只读漂移 gate"
    - path: "scripts/contracts/verify-migration-policy.ps1"
      provides: "SQL token 与 Rust transform capability 禁止项 lint"
    - path: "tests/fixtures/migrations/forbidden-migration-cases.json"
      provides: "SQL 与外部文件/进程 side-effect 正反 fixture"
  key_links:
    - from: "tests/fixtures/databases/manifest.json"
      to: "src-tauri/tests/migration_matrix.rs"
      via: "矩阵先验证 manifest/file identity，再复制每个历史 fixture"
      pattern: "manifest"
  assumptions:
    - "FLAGGED_UNVERIFIED [STATE-03]: specless edge probe 为 unclassified；manifest-driven 全历史矩阵、tag drift 与禁止项 lint 共同作为显式 backstop。"
  prohibitions:
    - "FLAGGED_UNVERIFIED: migration 内不得执行非事务 SQL、外部文件、子进程或网络副作用。"
    - "FLAGGED_UNVERIFIED: 不得在 Rust 源码手写历史 fixture 版本列表或在 verify 中调用 generator。"
---

<objective>
建立 manifest-driven 历史迁移矩阵、history lock/tag drift gate 与迁移禁止项 lint。

Purpose: 证明所有正式历史数据库可以顺序升级，并阻断破坏单事务回滚的副作用。
Output: 迁移矩阵、历史锁、只读 drift verifier、policy verifier 与正反 fixture。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0006-sqlite-application-state.md
@.planning/phases/01-trusted-local-state-contract/01-20-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
</context>

## Artifacts this phase produces

- `src-tauri/tests/migration_matrix.rs`：manifest-driven 全历史顺序迁移矩阵
- `tests/fixtures/databases/history-lock.json`：migration SQL/fixture/schema fingerprint 不可变 lock
- `scripts/contracts/verify-migration-history.ps1`：工作树与 release tag 只读漂移 gate
- `scripts/contracts/verify-migration-policy.ps1`：SQL token 与 Rust transform capability 禁止项 lint
- `tests/fixtures/migrations/forbidden-migration-cases.json`：SQL 与外部文件/进程 side-effect 正反 fixture

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 manifest-driven historical migration matrix</name>
  <read_first>
    @tests/fixtures/databases/manifest.json
    @tests/fixtures/databases/v001/state.sqlite3
    @src-tauri/src/state/migrations/mod.rs
    @src-tauri/tests/fixture_generation.rs
  </read_first>
  <files>src-tauri/tests/migration_matrix.rs</files>
  <behavior>
    - Rust 测试不手写 fixture 版本数组。
    - 每个 fixture 先校验 hash/application_id/user_version/fingerprint，再复制到 TempDir。
    - test-only v2/v3 顺序应用后 ledger/checksum/fingerprint/FK/quick_check 一致。
  </behavior>
  <action>读取 01-20 已创建的 manifest 并枚举 fixtures；每项验证身份后只在 TempDir 副本上打开。通过仅测试注册的 v2/v3 migrations 验证从 v001 跨多版本顺序升级、每个 migration 恰好一次、最终 user_version/ledger/checksum/fingerprint/foreign_key_check/quick_check 一致。verify 不运行 generator，不修改 committed DB/manifest。</action>
  <acceptance_criteria>manifest 中每个正式历史样本均从原版本顺序升级到 current，仓库原件 hash/mtime 不变。</acceptance_criteria>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test migration_matrix</automated></verify>
  <done>所有已提交历史数据库由同一矩阵覆盖。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 固定 history lock 与 release-tag drift gate</name>
  <read_first>
    @src-tauri/src/state/migrations/0001_initial.sql
    @tests/fixtures/databases/manifest.json
    @.planning/STATE.md
  </read_first>
  <files>tests/fixtures/databases/history-lock.json, scripts/contracts/verify-migration-history.ps1</files>
  <action>history-lock 记录每个 migration SQL SHA、fixture file/logical SHA、application ID、user_version 与 schema fingerprint。verifier 只读重算工作树并枚举本地可用的已合并 v* release tags；tag 中存在相同历史版本时比较摘要，任何已发布内容改写非零。仓库尚无匹配 tag 时仍要求本地 lock 非空、连续且与 manifest/registry 一致，不把“无 tag”当成跳过。</action>
  <acceptance_criteria>当前历史身份与 lock 一致；任一 SQL/fixture/fingerprint 漂移或 tag 历史改写均失败。</acceptance_criteria>
  <verify><automated>powershell -NoProfile -File scripts/contracts/verify-migration-history.ps1</automated></verify>
  <done>永久历史不能被静默改写。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 禁止非事务 migration 与外部副作用</name>
  <read_first>
    @src-tauri/src/state/migrations/mod.rs
    @src-tauri/src/state/migrations/0001_initial.sql
    @.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
  </read_first>
  <files>scripts/contracts/verify-migration-policy.ps1, tests/fixtures/migrations/forbidden-migration-cases.json</files>
  <behavior>
    - SQL lexer 在注释/字符串外拒绝 VACUUM、ATTACH、DETACH 与 PRAGMA journal_mode。
    - Rust transform 只能接收 Transaction；migrations 模块不得引用 fs/File/OpenOptions/Path/Command/network API。
    - 字符串字面量与注释中的词汇不误报，正常 DDL/DML 正控制通过。
  </behavior>
  <action>实现 production policy verifier，词法扫描全部 migration SQL，并对 migration registry/transform module 执行 capability allowlist。fixture 覆盖大小写、空白、注释绕过、字符串正控制、文件、子进程和网络 snippets；SelfTest 把 fixture 写到临时目录并调用同一 lexer/capability predicate，真实 scan 读取 repository root。</action>
  <acceptance_criteria>研究列出的每类破坏事务边界的 SQL/Rust 副作用均有失败负例，正常迁移不误报。</acceptance_criteria>
  <verify><automated>powershell -NoProfile -File scripts/contracts/verify-migration-policy.ps1 -SelfTest; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/verify-migration-policy.ps1 -RepositoryRoot .</automated></verify>
  <done>migration 的全部操作保持在单一 SQLite transaction 能回滚的边界内。</done>
</task>

</tasks>

<threat_model>
Security level: ASVS L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration source → SQLite transaction/外部系统 | 非事务 SQL 或文件/进程副作用会破坏完整回滚。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-51 | Tampering | migration history | high | mitigate | history lock、fixture hash 与 tag drift gate。 |
| T-01-52 | Tampering | non-transactional migration | critical | mitigate | SQL lexer 与 Rust capability lint/负例。 |
</threat_model>

<verification>
- migration_matrix、history verifier、policy self-test 与真实 scan 返回 0。
</verification>

<success_criteria>
- STATE-03 的历史升级链和迁移安全约束均可执行验证。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-21-SUMMARY.md`
</output>
