---
phase: 01-trusted-local-state-contract
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/contracts/runner-cli-matrix.json
  - scripts/contracts/run-phase1-contracts.ps1
  - scripts/contracts/test-run-phase1-cli.ps1
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04
  - STATE-05
must_haves:
  truths:
    - "所有 runner 调用都显式提供 Scope、Target、Mode，并且只使用声明组合。"
    - "AllowBlocked 永远输出 strict_gate_eligible=false，不能被 Freeze 或 PhaseComplete 接受。"
    - "退出码 0/2/3/4/5/64 的含义固定且由 parser tests 覆盖。"
  artifacts:
    - path: "tests/fixtures/contracts/runner-cli-matrix.json"
      provides: "Scope/Target/Mode 合法组合、退出码与下游 dispatch 表"
    - path: "scripts/contracts/run-phase1-contracts.ps1"
      provides: "Phase 1 唯一合同/打包/冻结/完成 runner"
    - path: "scripts/contracts/test-run-phase1-cli.ps1"
      provides: "参数 parser、非法组合、AllowBlocked 与 PLAN consumer 扫描测试"
  key_links:
    - from: "scripts/contracts/test-run-phase1-cli.ps1"
      to: ".planning/phases/01-trusted-local-state-contract/01-PLAN 集合"
      via: "提取所有 runner invocation 并对照 matrix"
      pattern: "-Scope + -Target + -Mode"
---

<objective>
定义 Phase 1 唯一 runner CLI、合法调用矩阵与稳定退出码。

Purpose: 消除跨计划 Scope/Target/Mode 漂移，并让所有消费者可机器扫描。
Output: canonical runner、CLI matrix 与 parser/consumer contract tests。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-trusted-local-state-contract/01-VALIDATION.md
</context>

## Artifacts this phase produces

- `tests/fixtures/contracts/runner-cli-matrix.json`：Scope/Target/Mode 合法组合、退出码与下游 dispatch 表
- `scripts/contracts/run-phase1-contracts.ps1`：Phase 1 唯一合同/打包/冻结/完成 runner
- `scripts/contracts/test-run-phase1-cli.ps1`：参数 parser、非法组合、AllowBlocked 与 PLAN consumer 扫描测试

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 固定 Scope/Target/Mode 与退出码合同</name>
  <files>tests/fixtures/contracts/runner-cli-matrix.json, scripts/contracts/run-phase1-contracts.ps1</files>
  <behavior>
    - Scope 仅为 RunnerSelfTest、ProvenanceSelfTest、ContractSelfTest、PackagingSelfTest、Freeze、PhaseComplete。
    - Target 仅为 Local、WindowsX64、WindowsArm64、MacIntel、MacAppleSilicon、Wsl2。
    - Mode 仅为 Strict、AllowBlocked；Freeze/PhaseComplete 只允许 Local+Strict。
  </behavior>
  <action>实现参数声明与 matrix-driven dispatch。退出码固定为：0 表示按请求模式完成；2 表示行为或断言失败；3 表示 Strict 前置资源阻断；4 表示 provenance/attestation 不可信；5 表示安全、canary 或生命周期边界失败；64 表示参数或组合错误。AllowBlocked 只允许 ContractSelfTest/PackagingSelfTest 的具体目标；资源缺失时可返回 0，但 JSON 必须同时记录 outcome=blocked 与 strict_gate_eligible=false，且任何行为失败、provenance 失败或安全失败仍返回 2/4/5。RunnerSelfTest 不依赖尚未创建的下游脚本。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope RunnerSelfTest -Target Local -Mode Strict</automated></verify>
  <done>runner parser 与 matrix 可独立执行，非法组合稳定返回 64。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 锁定 parser 与所有计划消费者合同</name>
  <files>scripts/contracts/test-run-phase1-cli.ps1</files>
  <behavior>
    - 每个合法组合命中唯一 dispatch。
    - 缺参数、旧参数、未声明 Scope/Target/Mode 和 Strict/AllowBlocked 混用被拒绝。
    - 所有 PLAN.md 中的 runner 调用都可由 matrix 解析。
  </behavior>
  <action>创建 parser/consumer 测试，包含旧式 freeze/phase 开关、未声明 scope、遗漏 Target 与遗漏 Mode 负例；只扫描 Phase 1 全部 PLAN.md 的 automated runner 命令，任何调用不符合 matrix 立即失败。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/test-run-phase1-cli.ps1 -ScanPlans .planning/phases/01-trusted-local-state-contract</automated></verify>
  <done>计划命令接口只有一个事实源，旧调用形状无法通过。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PLAN 消费者 → runner CLI | 跨计划命令字符串决定执行哪个门禁及其严格性。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | runner mode selection | high | mitigate | matrix-driven parser 与 PLAN consumer 扫描。 |
| T-01-06 | Elevation of Privilege | AllowBlocked | high | mitigate | Freeze/PhaseComplete 拒绝 AllowBlocked，blocked 结果不可 strict eligible。 |
</threat_model>

<verification>
- RunnerSelfTest 与 consumer scan 返回 0。
- 所有后续计划命令只使用声明组合。
</verification>

<success_criteria>
- CLI 合同无歧义。
- AllowBlocked 不可升级为严格通过。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-03-SUMMARY.md`
</output>
