---
phase: 01-trusted-local-state-contract
plan: 08
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - package.json
  - package-lock.json
  - vite.config.ts
  - tsconfig.json
  - index.html
  - src/main.tsx
  - src/App.tsx
  - src/global.css
autonomous: true
requirements:
  - STATE-01
  - STATE-02
must_haves:
  truths:
    - "package.json 与 lockfile 精确使用已批准/已审计版本。"
    - "React 只提供可构建 root，不保存权威状态或实现后续阶段产品功能。"
  artifacts:
    - path: "package.json"
      provides: "exact npm scripts 与 dependencies"
    - path: "package-lock.json"
      provides: "可重复 npm 依赖图"
    - path: "vite.config.ts"
      provides: "Tauri/Vite build 配置"
    - path: "tsconfig.json"
      provides: "严格 TypeScript 配置"
    - path: "index.html"
      provides: "单一 React mount"
    - path: "src/main.tsx"
      provides: "React bootstrap"
    - path: "src/App.tsx"
      provides: "无业务状态的应用 root"
    - path: "src/global.css"
      provides: "仅 mount 所需基础样式"
  key_links:
    - from: "package.json"
      to: "scripts/contracts/verify-npm-package-allowlist.ps1"
      via: "install 前重跑隔离 metadata gate"
      pattern: "exact versions"
---

<objective>
创建 exact、可重复构建且无业务范围的 React/TypeScript 前端壳。

Purpose: 为 Tauri 与状态 command 提供静态入口，同时不提前实现 Phase 7 UI。
Output: npm lock、Vite/TypeScript 配置和最小 React root。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0002-tauri-rust-react.md
@.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
@.planning/phases/01-trusted-local-state-contract/01-02-SUMMARY.md
</context>

## Artifacts this phase produces

- `package.json`：exact npm scripts 与 dependencies
- `package-lock.json`：可重复 npm 依赖图
- `vite.config.ts`：Tauri/Vite build 配置
- `tsconfig.json`：严格 TypeScript 配置
- `index.html`：单一 React mount
- `src/main.tsx`：React bootstrap
- `src/App.tsx`：无业务状态的应用 root
- `src/global.css`：仅 mount 所需基础样式

<tasks>

<task type="auto">
  <name>Task 1: 安装批准后的 exact npm 工具链</name>
  <precondition>01-02-SUMMARY.md 明确记录 approved。</precondition>
  <reversibility rating="costly">lockfile 与主版本将被后续全部桌面构建消费。</reversibility>
  <files>package.json, package-lock.json, vite.config.ts, tsconfig.json, index.html</files>
  <action>先重跑 01-01 verifier；按 RESEARCH 的 exact versions 创建 private package，禁止 caret、tilde 与 latest。包含官方 @tauri-apps/api/cli 及已批准 React/Vite/TypeScript 组合；不引入账户、遥测、云同步或 Tauri fs/shell/http/SQL/updater plugin。build 必须执行 tsc --noEmit 与 vite build。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/verify-npm-package-allowlist.ps1 -Allowlist tests/fixtures/contracts/npm-package-allowlist.json; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm ci</automated></verify>
  <done>干净 install 精确复现 lockfile，tracked package 文件无二次漂移。</done>
</task>

<task type="auto">
  <name>Task 2: 建立无业务范围的 React root</name>
  <files>src/main.tsx, src/App.tsx, src/global.css</files>
  <action>创建单一 root mount；App 只显示内部构建标识与可访问容器，不添加供应商表单、恢复页面、托盘、网络调用或浏览器持久化。普通文本保持 LF。</action>
  <verify><automated>npm run build</automated></verify>
  <done>production build 通过且前端不是第二状态源。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm 依赖 → WebView | 前端依赖和 capability 可能扩大本地数据访问面。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-16 | Tampering | npm dependency graph | high | mitigate | 双重 legitimacy、exact pins、lockfile 与 npm ci。 |
| T-01-17 | Elevation of Privilege | frontend plugins | medium | mitigate | 不安装高权限 Tauri plugin 或网络/账户 SDK。 |
</threat_model>

<verification>
- npm ci 与 npm run build 返回 0。
</verification>

<success_criteria>
- 前端工具链可重复。
- 没有后续阶段 UI/网络范围。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-08-SUMMARY.md`
</output>
