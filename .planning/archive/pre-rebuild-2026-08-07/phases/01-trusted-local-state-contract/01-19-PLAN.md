---
phase: 01-trusted-local-state-contract
plan: 19
type: execute
wave: 11
depends_on: [01-18]
files_modified:
  - src-tauri/src/lib.rs
  - src-tauri/tests/local_only_boundary.rs
  - src-tauri/tests/installed_state_smoke.rs
  - src-tauri/src/state/coordination.rs
  - src-tauri/src/state/mod.rs
  - src-tauri/tests/state_concurrency.rs
autonomous: true
requirements: [STATE-01, STATE-02, STATE-03, STATE-04, STATE-05]
must_haves:
  truths:
    - "headless full-state smoke 使用 seed、verify、cleanup 三个独立模式，verify 不删除状态。"
    - "应用无产品账户、云同步、默认上传或浏览器持久化；状态根只来自 app_local_data_dir。"
    - "StateCoordinator 使用 OS 级 File lock；进程崩溃后锁由 OS 释放，owner metadata 不能自证占用或 disposable。"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "phase1-state-smoke seed/verify/cleanup 固定入口"
    - path: "src-tauri/tests/local_only_boundary.rs"
      provides: "当前用户路径、依赖/capability/DTO 与无账户/上传边界"
    - path: "src-tauri/tests/installed_state_smoke.rs"
      provides: "seed/verify 保留状态与显式 cleanup 的两进程测试"
    - path: "src-tauri/src/state/coordination.rs"
      provides: "OS exclusive File lock 与脱敏 owner metadata"
    - path: "src-tauri/src/state/mod.rs"
      provides: "StateStore 写路径对 coordinator 的 wiring"
    - path: "src-tauri/tests/state_concurrency.rs"
      provides: "并发 open、busy、崩溃释放与 non-mutation 两进程测试"
  key_links:
    - from: "src-tauri/src/state/mod.rs"
      to: "src-tauri/src/state/coordination.rs"
      via: "Create/Ready/Migrate/Backup/Restore 写路径在任何写入前持有 exclusive File lock"
      pattern: "StateCoordinator"
  assumptions:
    - "FLAGGED_UNVERIFIED [STATE-01]: specless edge probe 为 unclassified；installed smoke 用完整状态 digest 和新进程 verify 作为 backstop。"
    - "FLAGGED_UNVERIFIED [STATE-02]: specless edge probe 为 unclassified；依赖/capability/commands/COVERAGE 静态扫描与路径测试作为 local-only backstop。"
    - "FLAGGED_UNVERIFIED [STATE-03]: specless edge probe 为 unclassified；本计划只建立所有未来迁移写路径必须使用的跨进程锁。"
    - "FLAGGED_UNVERIFIED [STATE-04]: specless edge probe 为 unclassified；backup/rollback 行为仍由 01-23 验证，但先锁定协调边界。"
    - "FLAGGED_UNVERIFIED [STATE-05]: specless edge probe 为 unclassified；restore 行为仍由 01-24 验证，但同一 coordinator 必须覆盖它。"
  prohibitions:
    - "FLAGGED_UNVERIFIED: 不得用 lock 文件存在或 owner metadata 代替 OS lock ownership。"
    - "FLAGGED_UNVERIFIED: verify 模式不得清理数据库、备份或 run-scoped 状态。"
---

<objective>
提供 truthful installed full-state smoke、完全本地边界与跨进程状态协调合同。

Purpose: 让后续迁移、备份、恢复和最终签名工作流共享不会提前清理、不会并发写坏的真实入口。
Output: state smoke modes、local-only tests、OS file lock coordinator 与并发测试。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0007-local-only-product.md
@.planning/phases/01-trusted-local-state-contract/COVERAGE.md
@.planning/phases/01-trusted-local-state-contract/01-18-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
</context>

## Artifacts this phase produces

- `src-tauri/src/lib.rs`：phase1-state-smoke seed/verify/cleanup 固定入口
- `src-tauri/tests/local_only_boundary.rs`：当前用户路径、依赖/capability/DTO 与无账户/上传边界
- `src-tauri/tests/installed_state_smoke.rs`：seed/verify 保留状态与显式 cleanup 的两进程测试
- `src-tauri/src/state/coordination.rs`：OS exclusive File lock 与脱敏 owner metadata
- `src-tauri/src/state/mod.rs`：StateStore 写路径对 coordinator 的 wiring
- `src-tauri/tests/state_concurrency.rs`：并发 open、busy、崩溃释放与 non-mutation 两进程测试

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现不提前清理的 full-state installed smoke 与 local-only gate</name>
  <read_first>
    @src-tauri/src/lib.rs
    @src-tauri/src/commands.rs
    @src-tauri/tests/state_persistence.rs
    @src-tauri/capabilities/default.json
    @src-tauri/Cargo.toml
    @package.json
    @.planning/phases/01-trusted-local-state-contract/COVERAGE.md
  </read_first>
  <files>src-tauri/src/lib.rs, src-tauri/tests/local_only_boundary.rs, src-tauri/tests/installed_state_smoke.rs</files>
  <behavior>
    - seed 写固定假全量状态，verify 在新进程逐项核对 digest 并保留状态。
    - cleanup 只删除匹配 opaque run ID 的固定 run-scoped 状态。
    - local-only gate 拒绝账户/云同步/默认上传/浏览器 storage/高权限 Tauri plugin 或公开 secret DTO。
  </behavior>
  <action>在 production run_with_args 增加 phase1-state-smoke seed、verify、cleanup 固定入口，只接受 1–64 位 opaque run ID，不接受路径。seed/verify 输出 provider/verification/native+WSL/current-provider/settings 计数与 expected digest；verify 不删除 DB 或 backup。cleanup 先验证 run ID、固定产品根与 run marker，再删除对应 run-scoped 状态。local_only_boundary 检查 app_local_data_dir、Cargo/package 依赖、capability、commands、COVERAGE.md 与 DTO 字段，证明没有产品账户、云同步、默认上传、Web Storage 或外部 API surface。</action>
  <acceptance_criteria>seed→新进程 verify 成功且状态仍存在；只有显式 cleanup 删除匹配 run 的状态；local-only gate 全绿。</acceptance_criteria>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test installed_state_smoke --test local_only_boundary</automated></verify>
  <done>installed workflows 可在恢复场景前保留完整状态，且产品保持当前用户完全本地。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立由 OS 释放的跨进程 StateCoordinator</name>
  <read_first>
    @src-tauri/src/state/mod.rs
    @src-tauri/tests/state_command_restart.rs
    @.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
  </read_first>
  <files>src-tauri/src/state/coordination.rs, src-tauri/src/state/mod.rs, src-tauri/tests/state_concurrency.rs</files>
  <behavior>
    - 一个进程持有 File exclusive lock 时第二个写 open 返回 StateBusy 且不改 DB/WAL/backup。
    - kill holder 后 OS 释放锁，第二进程可取得锁并覆盖旧 owner metadata。
    - owner metadata 只含 PID/start token/run ID 摘要，不决定 lock ownership。
  </behavior>
  <action>使用 Rust 1.97 稳定 File::try_lock/lock 在固定 app_local_data_dir/state.lock 上协调；取得 OS lock 后才原子写 owner metadata，正常 Drop/unwind 释放，进程终止依赖 OS 释放。StateStore 的 Create/Ready 以及后续 Migrate/Backup/Restore seam 均从同一 guard 进入。两进程测试覆盖 concurrent open、busy timeout、kill holder、再次 open、stale metadata 与相关文件 non-mutation，不删除不可信 lock path。</action>
  <acceptance_criteria>锁所有权仅由 OS primitive 决定；崩溃释放可重现；竞争失败不修改任何状态/恢复工件。</acceptance_criteria>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test state_concurrency</automated></verify>
  <done>跨进程状态写被序列化，且 crash 后不需要伪造 cleanup marker 解锁。</done>
</task>

</tasks>

<threat_model>
Security level: ASVS L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installed CLI smoke → 当前用户状态根 | headless 入口必须固定根、脱敏且不提前删除证据。 |
| 多个 GPTEasy 进程 → 单一状态根 | 并发写可能交错迁移、备份与恢复。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-45 | Denial of Service | concurrent state access | high | mitigate | OS exclusive File lock、busy timeout 与两进程测试。 |
| T-01-46 | Tampering | smoke cleanup | high | mitigate | opaque run ID、verify 不清理、显式 cleanup。 |
| T-01-47 | Spoofing | lock metadata | medium | mitigate | metadata 仅诊断，ownership 由 OS lock 决定。 |
| T-01-48 | Information Disclosure | local-only output | high | mitigate | DTO/capability/dependency allowlist 与 secret canary。 |
</threat_model>

<verification>
- installed_state_smoke、local_only_boundary、state_concurrency 返回 0。
</verification>

<success_criteria>
- installed state 可被后续 recovery smoke 继续消费。
- 完全本地与多进程串行化边界可自动证明。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-19-SUMMARY.md`
</output>
