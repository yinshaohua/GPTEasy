---
phase: 01-trusted-local-state-contract
plan: 27
type: execute
wave: 18
depends_on: [01-25]
files_modified:
  - tests/fixtures/contracts/codex/macos-intel/manifest.json
  - tests/fixtures/contracts/host/macos-intel/manifest.json
  - tests/fixtures/contracts/packaging/macos-intel/manifest.json
  - tests/fixtures/contracts/codex/macos-apple-silicon/manifest.json
  - tests/fixtures/contracts/host/macos-apple-silicon/manifest.json
  - tests/fixtures/contracts/packaging/macos-apple-silicon/manifest.json
autonomous: false
requirements: [STATE-01, STATE-02, STATE-03, STATE-04, STATE-05]
user_setup:
  - service: Apple Developer ID 与 notarization
    why: 生成真实 macOS Intel/Apple Silicon signed/notarized installed evidence
    env_vars:
      - name: APPLE_CERTIFICATE
        source: 正式 Developer ID 证书的离线保管位置；配置为 GitHub Actions secret
      - name: APPLE_CERTIFICATE_PASSWORD
        source: 正式证书密码的离线保管位置；配置为 GitHub Actions secret
      - name: APPLE_SIGNING_IDENTITY
        source: Apple Developer 账户的精确 Developer ID Application identity
      - name: APPLE_API_ISSUER
        source: App Store Connect API issuer
      - name: APPLE_API_KEY
        source: App Store Connect notarization private key
      - name: APPLE_API_KEY_ID
        source: App Store Connect API key ID
    dashboard_config:
      - task: 提供原生 Intel/Apple Silicon runner、allowlisted 正式宿主与 Codex 0.146.1
        location: GitHub repository Actions runner/settings
must_haves:
  truths:
    - "Intel 与 Apple Silicon jobs 都先通过原生 Wave 0，再获得 Developer ID、notary stapling、Gatekeeper 与 ~/Applications 证据。"
    - "两架构均执行 CLI/正式宿主 parity、完整状态重开、higher refusal、backup/quarantine restore 与 uid/home cleanup。"
    - "没有正式 Apple 凭据时计划在 precondition 停止；不创建 ad-hoc identity、占位 secrets 或 fake notarization。"
  artifacts:
    - path: "tests/fixtures/contracts/codex/macos-intel/manifest.json"
      provides: "macOS Intel Codex contract evidence"
    - path: "tests/fixtures/contracts/host/macos-intel/manifest.json"
      provides: "macOS Intel 正式宿主 parity evidence"
    - path: "tests/fixtures/contracts/packaging/macos-intel/manifest.json"
      provides: "macOS Intel notarized full-state/recovery evidence"
    - path: "tests/fixtures/contracts/codex/macos-apple-silicon/manifest.json"
      provides: "macOS Apple Silicon Codex contract evidence"
    - path: "tests/fixtures/contracts/host/macos-apple-silicon/manifest.json"
      provides: "macOS Apple Silicon 正式宿主 parity evidence"
    - path: "tests/fixtures/contracts/packaging/macos-apple-silicon/manifest.json"
      provides: "macOS Apple Silicon notarized full-state/recovery evidence"
  key_links:
    - from: ".github/workflows/phase1-macos-evidence.yml"
      to: ".github/workflows/phase1-macos-wave0.yml"
      via: "所有 native evidence jobs 依赖 Wave 0 且 manifest 记录 wave0_passed"
      pattern: "needs: wave0"
  assumptions:
    - "FLAGGED_UNVERIFIED [STATE-01]: specless edge probe 为 unclassified；双架构 installed full-state restart assertions 为平台 backstop。"
    - "FLAGGED_UNVERIFIED [STATE-02]: specless edge probe 为 unclassified；当前用户 ~/Applications/profile 与 canary-clean evidence 为平台 backstop。"
    - "FLAGGED_UNVERIFIED [STATE-03]: specless edge probe 为 unclassified；signed/notarized app 的 current-schema open/migration assertions 为平台 backstop。"
    - "FLAGGED_UNVERIFIED [STATE-04]: specless edge probe 为 unclassified；双架构 backup/rollback assertions 为平台 backstop。"
    - "FLAGGED_UNVERIFIED [STATE-05]: specless edge probe 为 unclassified；双架构 refusal/quarantine restore assertions 为平台 backstop。"
  prohibitions:
    - "FLAGGED_UNVERIFIED: 不得使用 ad-hoc 签名、未公证 app、伪 stapling/Gatekeeper、非原生或 strict fixture 作为正式证据。"
---

<objective>
获取 macOS Intel/Apple Silicon 真实 Developer ID、公证与 installed 状态/恢复证据。

Purpose: 在所有非签名功能完成后，用两个原生架构和 Apple 信任链证明 macOS 最终行为。
Output: 六份由真实 native run、签名公证工件与 attestation 支持的 manifests。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trusted-local-state-contract/01-06-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-14-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-25-SUMMARY.md
@.codex/skills/spike-findings-gpteasy/references/macos-host-contract.md
</context>

## Artifacts this phase produces

- `tests/fixtures/contracts/codex/macos-intel/manifest.json`：macOS Intel Codex contract evidence
- `tests/fixtures/contracts/host/macos-intel/manifest.json`：macOS Intel 正式宿主 parity evidence
- `tests/fixtures/contracts/packaging/macos-intel/manifest.json`：macOS Intel notarized full-state/recovery evidence
- `tests/fixtures/contracts/codex/macos-apple-silicon/manifest.json`：macOS Apple Silicon Codex contract evidence
- `tests/fixtures/contracts/host/macos-apple-silicon/manifest.json`：macOS Apple Silicon 正式宿主 parity evidence
- `tests/fixtures/contracts/packaging/macos-apple-silicon/manifest.json`：macOS Apple Silicon notarized full-state/recovery evidence

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: 核验 macOS 正式凭据与原生宿主前置条件</name>
  <precondition>01-25 的 macOS Wave 0/full-state/recovery workflow 接线合同已通过，且尚未调度任何远程 evidence job。</precondition>
  <action>只核验 GitHub Actions secret 名称、正式 Developer ID/notarization 凭据的离线可用性、原生 macos-15 与 macos-15-intel runner、allowlisted 正式宿主和 Codex 0.146.1 的可用性。不得读取、打印或记录证书、密码、API key 或 private key 值，不得创建 ad-hoc、占位或 synthetic 材料；任一前置条件缺失时停止并保持后续证据任务 fail-closed。</action>
  <what-built>01-25 已完成 macOS Wave 0、full-state/recovery/lifecycle workflow 的本地接线，正式签名与公证运行仍等待真实环境。</what-built>
  <instructions>由凭据保管人确认正式 Developer ID 与 notarization 凭据可用，由仓库管理员确认六个 Actions secret 名称和 macos-15/macos-15-intel 原生 runner 可用，由证据负责人确认 allowlisted 正式宿主与 Codex 0.146.1 可调度；全程不得展示或复制证书、密码、API key 或 private key 值。</instructions>
  <how-to-verify>
    1. 使用只读 GitHub CLI 查询 Actions secrets，确认六个名称均存在。
    2. 由凭据保管人确认正式 Developer ID、notarization 与 App Store Connect key 可用，且不向本地输出值。
    3. 确认 macos-15 与 macos-15-intel 原生 runner、allowlisted 正式宿主和 Codex 0.146.1 可调度。
  </how-to-verify>
  <verify><automated>$required = @('APPLE_CERTIFICATE','APPLE_CERTIFICATE_PASSWORD','APPLE_SIGNING_IDENTITY','APPLE_API_ISSUER','APPLE_API_KEY','APPLE_API_KEY_ID'); $configured = @(gh secret list --repo yinshaohua/GPTEasy --app actions --json name --jq '.[].name'); if ($LASTEXITCODE -ne 0) { exit 3 }; $missing = @($required | Where-Object { $_ -notin $configured }); if ($missing.Count -gt 0) { Write-Error ('缺少 Actions secret 名称: ' + ($missing -join ', ')); exit 3 }</automated><human-check>确认正式 Developer ID/notarization 凭据、原生双架构 runner 与 allowlisted 正式宿主均可用；不提供或打印凭据值。</human-check></verify>
  <verification>六个 secret 名称存在，凭据保管人确认正式 Developer ID/notarization 可用，原生双架构 runner 与正式宿主/Codex 前置条件均被确认；没有任何凭据值、ad-hoc、占位或 synthetic 材料进入输出。</verification>
  <done>仅在 secret 名称、正式凭据可用性、原生 runner 与正式宿主前置条件全部确认后，才允许远程调度 macOS evidence。</done>
  <resume-signal>输入 macos-evidence-prerequisites-ready，或描述任一缺失前置条件。</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 获取并导入 macOS Intel 正式证据</name>
  <read_first>
    @.github/workflows/phase1-macos-wave0.yml
    @.github/workflows/phase1-macos-evidence.yml
    @scripts/contracts/run-macos.zsh
    @scripts/contracts/verify-evidence-provenance.ps1
    @.planning/phases/01-trusted-local-state-contract/01-25-SUMMARY.md
  </read_first>
  <precondition>Task 1 已返回 macos-evidence-prerequisites-ready；正式 Developer ID 与 notarization credentials 已存在于离线/Apple 保管位置并配置 secrets，原生 Intel runner、Codex 0.146.1 与 allowlisted 正式宿主可用。</precondition>
  <files>tests/fixtures/contracts/codex/macos-intel/manifest.json, tests/fixtures/contracts/host/macos-intel/manifest.json, tests/fixtures/contracts/packaging/macos-intel/manifest.json</files>
  <action>只读核验 secret 名称后调度当前 commit 的 MacIntel job；要求 Wave 0 原生 zsh syntax/tests 成功，再执行 Developer ID build、公证/stapling、Gatekeeper、~/Applications、host parity、完整状态/恢复和 lifecycle。统一 finally 成功后按数值 run/job/artifact ID 重新下载/验签、重算 archive/app/commit digest。仅在全部 strict assertions 为 true 时导入三份 manifest；blocked/failed 不写 pass 文件。</action>
  <acceptance_criteria>Intel 三份 manifest 由同一真实 native run 与 notarized archive 支持，并包含 wave0/full-state/recovery/cleanup/provenance 事实。</acceptance_criteria>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope ContractSelfTest -Target MacIntel -Mode Strict; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope PackagingSelfTest -Target MacIntel -Mode Strict</automated></verify>
  <done>macOS Intel 正式签名公证 installed 全范围证据 strict pass。</done>
</task>

<task type="auto">
  <name>Task 3: 获取并导入 macOS Apple Silicon 正式证据</name>
  <read_first>
    @.github/workflows/phase1-macos-wave0.yml
    @.github/workflows/phase1-macos-evidence.yml
    @scripts/contracts/run-macos.zsh
    @scripts/contracts/verify-evidence-provenance.ps1
  </read_first>
  <precondition>Task 1 已返回 macos-evidence-prerequisites-ready；同一正式 Apple credentials 可用，且原生 Apple Silicon runner、Codex 0.146.1 与 allowlisted 正式宿主可用。</precondition>
  <files>tests/fixtures/contracts/codex/macos-apple-silicon/manifest.json, tests/fixtures/contracts/host/macos-apple-silicon/manifest.json, tests/fixtures/contracts/packaging/macos-apple-silicon/manifest.json</files>
  <action>独立调度 MacAppleSilicon job 并执行与 Intel 相同的 Wave 0、签名公证、current-user、状态/恢复、cleanup 与 provenance gates。要求 runner/uname/bundle executable 均为 arm64；禁止 universal/cross-build、自报架构或复用 Intel evidence。全绿后才写固定三份 manifests。</action>
  <acceptance_criteria>Apple Silicon evidence 独立、原生、Developer ID signed/notarized、可取回并覆盖完整状态/恢复。</acceptance_criteria>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope ContractSelfTest -Target MacAppleSilicon -Mode Strict; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope PackagingSelfTest -Target MacAppleSilicon -Mode Strict</automated></verify>
  <done>macOS Apple Silicon 正式签名公证 installed 全范围证据 strict pass。</done>
</task>

</tasks>

<threat_model>
Security level: ASVS L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apple credentials/native runner → notarized app | 外部凭据与真实宿主决定 Apple 信任链。 |
| Actions artifacts → committed manifests | 远程结果只有独立取回、验签和 archive correlation 后可信。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-69 | Spoofing | macOS architecture/signing | critical | mitigate | native arch、Developer ID、notary/stapling、Gatekeeper 与 attestation。 |
| T-01-70 | Tampering | Wave 0/archive import | high | mitigate | needs wave0、archive/app correlation 与重新取回 digest。 |
| T-01-71 | Information Disclosure | Apple/profile evidence | critical | mitigate | credentials 只在 CI secrets/private keychain；allowlist output。 |
</threat_model>

<verification>
- MacIntel/MacAppleSilicon 的 ContractSelfTest 与 PackagingSelfTest Strict 均返回 0。
</verification>

<success_criteria>
- macOS 双架构正式 Developer ID/notary evidence 真实覆盖 STATE-01..05。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-27-SUMMARY.md`
</output>
