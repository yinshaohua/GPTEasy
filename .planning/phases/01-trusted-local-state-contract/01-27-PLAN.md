---
phase: 01-trusted-local-state-contract
plan: 27
type: execute
wave: 17
depends_on: [01-14, 01-25]
files_modified:
  - .github/workflows/phase1-macos-evidence.yml
  - tests/fixtures/contracts/packaging/macos-intel/manifest.json
  - tests/fixtures/contracts/packaging/macos-apple-silicon/manifest.json
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04
  - STATE-05
user_setup:
  - service: GitHub Actions macOS 原生签名 runners
    why: 重跑 Intel/Apple Silicon 最终 installed full-state/recovery matrix
    dashboard_config:
      - task: 保持 native runners、Developer ID 与 notary secrets 可用
        location: GitHub repository Actions settings
must_haves:
  truths:
    - "两个 macOS job 在 Wave 0 之后运行不同 state/recovery run ID。"
    - "签名公证 app 在 ~/Applications 中执行 full-state、higher refusal、backup/quarantine restore。"
    - "统一 finally 完成两个 run cleanup、app cleanup 与 uid/home cleanup 后才允许 attestation。"
  artifacts:
    - path: ".github/workflows/phase1-macos-evidence.yml"
      provides: "macOS 最终 Wave0/full-state/recovery/lifecycle wiring"
    - path: "tests/fixtures/contracts/packaging/macos-intel/manifest.json"
      provides: "macOS Intel 最终 full-state/recovery evidence"
    - path: "tests/fixtures/contracts/packaging/macos-apple-silicon/manifest.json"
      provides: "macOS Apple Silicon 最终 full-state/recovery evidence"
  key_links:
    - from: ".github/workflows/phase1-macos-evidence.yml"
      to: ".github/workflows/phase1-macos-wave0.yml"
      via: "最终 native jobs 仍 needs wave0"
      pattern: "needs: wave0"
---

<objective>
把 full-state 与 recovery smoke 接入 macOS Intel/Apple Silicon installed workflow，并刷新最终 attested packaging evidence。

Purpose: 证明签名公证安装版在 Wave 0 与一次性用户生命周期内完成完整状态和恢复。
Output: 更新的 macOS workflow 与两份最终 packaging manifests。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trusted-local-state-contract/01-14-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-25-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-VALIDATION.md
</context>

## Artifacts this phase produces

- `.github/workflows/phase1-macos-evidence.yml`：macOS 最终 Wave0/full-state/recovery/lifecycle wiring
- `tests/fixtures/contracts/packaging/macos-intel/manifest.json`：macOS Intel 最终 full-state/recovery evidence
- `tests/fixtures/contracts/packaging/macos-apple-silicon/manifest.json`：macOS Apple Silicon 最终 full-state/recovery evidence

<tasks>

<task type="auto">
  <name>Task 1: 接线 macOS installed full-state/recovery 与统一 finally</name>
  <files>.github/workflows/phase1-macos-evidence.yml</files>
  <action>保留 job-level Wave 0 依赖；Intel/Apple Silicon jobs 分别生成不同 opaque state/recovery run ID，按 Windows 相同六步进程序列执行签名公证 app。任何中间步骤不得清理；统一 finally 依次执行 recovery cleanup、state cleanup、停止/移除 app、删除 uid/home 或 baseline restore，再生成 cleanup_attested。codesign/notary/Gatekeeper 与 CLI/host parity 仍为同 job 前置。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/test-macos-wave0-contract.ps1 -Scripts scripts/contracts/assert-macos-job-lifecycle.zsh,scripts/contracts/run-macos.zsh,scripts/contracts/test-macos-package-verifier.zsh -Workflow .github/workflows/phase1-macos-wave0.yml -EvidenceWorkflow .github/workflows/phase1-macos-evidence.yml; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope PackagingSelfTest -Target Local -Mode Strict</automated></verify>
  <done>workflow contract 保留 Wave 0、distinct run IDs 与统一 finally。</done>
</task>

<task type="auto">
  <name>Task 2: 重跑双架构并刷新最终 macOS packaging evidence</name>
  <precondition>macOS Intel/Apple Silicon native runners、签名公证资源与 01-06 gh preflight 仍可用。</precondition>
  <files>tests/fixtures/contracts/packaging/macos-intel/manifest.json, tests/fixtures/contracts/packaging/macos-apple-silicon/manifest.json</files>
  <action>调度两个 native jobs，要求 Wave 0 与 unified finally 成功；主工作区重新下载、验签并导入与 Windows 相同的 full-state/recovery/distinct-run/cleanup/provenance assertions，另要求 codesign/notary/Gatekeeper/current-user path 全绿。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope PackagingSelfTest -Target MacIntel -Mode Strict; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope PackagingSelfTest -Target MacAppleSilicon -Mode Strict</automated></verify>
  <done>macOS 双架构最终 native evidence 与实际完整测试范围一致。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installed macOS app → run-scoped DB/recovery/profile | 最终签名公证 app 与用户目录、恢复工件和账户生命周期交互。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-63 | Tampering | Wave0/final cleanup bypass | critical | mitigate | needs wave0、六步 smoke 与 unified finally assertions。 |
| T-01-64 | Spoofing | final macOS manifests | high | mitigate | native arch、codesign/notary/Gatekeeper、attestation 与 predicate fields。 |
</threat_model>

<verification>
- PackagingSelfTest MacIntel/MacAppleSilicon Strict 返回 0。
</verification>

<success_criteria>
- macOS 最终 evidence 覆盖真实 zsh、完整状态、恢复与账户清理。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-27-SUMMARY.md`
</output>
