# Phase 1 续作点：01-15 Windows freeze evidence

## 当前进度

- 计划：`01-15-PLAN.md`
- 任务：Task 1，Windows x64 evidence 调度前
- 已完成：真实 `gh` preflight 通过；修复 job 级 `runner.temp` 导致的 workflow 解析失败
- 修复提交：`455ca04`（已推送到 `origin/main`）

## 人工操作门禁

在 GitHub 仓库 `yinshaohua/GPTEasy` 的 Actions secrets 中安全配置以下名称，不要把值写入仓库或对话：

- `WINDOWS_AUTHENTICODE_PFX_BASE64`
- `WINDOWS_AUTHENTICODE_PFX_PASSWORD`

位置：GitHub repository → Settings → Secrets and variables → Actions。

## 恢复步骤

1. 用 `gh secret list --repo yinshaohua/GPTEasy --app actions` 只核验两个名称存在。
2. 调度 `.github/workflows/phase1-windows-evidence.yml` 的当前 `main` revision。
3. 等待 Windows x64 与 ARM64 job 成功；任何一项 blocked/failed 都不得创建 pass fixture。
4. 按数值 run/job/artifact ID 下载到临时目录，验证 attestation、commit、digest、canary、parity 与 lifecycle。
5. 仅在 Strict 合同通过后导入八份固定 manifest，并继续 `01-15` 收尾。

## Critical Anti-Patterns

| severity | anti-pattern | prevention |
|----------|--------------|------------|
| blocking | 缺少正式签名 secrets 时仍调度并把失败或 synthetic fixture 当作 freeze evidence | 调度前只读核验两个 secret 名称；取回后必须执行 schema/provenance/Strict 门禁，失败不写 pass fixture |
