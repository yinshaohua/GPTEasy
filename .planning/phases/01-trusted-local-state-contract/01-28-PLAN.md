---
phase: 01-trusted-local-state-contract
plan: 28
type: execute
wave: 18
depends_on: [01-07, 01-26, 01-27]
files_modified:
  []
autonomous: false
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04
  - STATE-05
must_haves:
  truths:
    - "PhaseComplete 只允许 Local+Strict，并执行 runner consumer tests、只读 source audit、npm build、Cargo all-targets、migration policy/history 与所有本地状态/恢复 tests。"
    - "四目标最终 packaging/contract/provenance/lifecycle evidence 均为实时 strict pass。"
    - "人工不能覆盖非零退出、digest drift、blocking reason 或 high/critical 未 mitigate。"
  artifacts:
    []
  key_links:
    - from: "scripts/contracts/run-phase1-contracts.ps1"
      to: "scripts/contracts/audit-phase1-plan-source.ps1"
      via: "PhaseComplete 强制执行 read-only audit 与 digest lock"
      pattern: "-Scope PhaseComplete -Target Local -Mode Strict"
---

<objective>
执行 Phase 1 最终只读统一门禁并由用户确认 STATE-01..STATE-05。

Purpose: 只有本地套件、机器来源审计、四目标 attested evidence 和安全威胁全部通过才完成阶段。
Output: 可重复 PhaseComplete 结果与人工批准记录。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-trusted-local-state-contract/01-VALIDATION.md
@.planning/phases/01-trusted-local-state-contract/01-SOURCE-AUDIT.md
@.planning/phases/01-trusted-local-state-contract/01-07-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-26-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-27-SUMMARY.md
</context>

## Artifacts this phase produces

- 本计划不创建或修改仓库文件；只记录门禁结论。

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 批准 Phase 1 最终统一门禁</name>
  <precondition>01-26 与 01-27 的四目标 PackagingSelfTest Strict 全部返回 0。</precondition>
  <action>运行唯一 PhaseComplete 命令；它必须执行 CLI consumer scan、gh preflight、只读 plan/source/digest audit、npm build、Cargo all-targets、path/command/full-state/concurrency/migration/lint/validator/backup/recovery tests，以及四目标 contract/package provenance。输出只读，不写规划文件或 manifests。人工逐条核对 ROADMAP Phase 1 Success Criteria 与 STATE-01..05；任何非零、drift、blocked 或 high/critical 未 mitigate 都拒绝批准。</action>
  <what-built>可信本地状态、永久迁移、verified backup/recovery、外部合同 freeze、四目标 signed installed smoke 与机器来源审计。</what-built>
  <how-to-verify>
    1. 运行 PhaseComplete Local Strict。
    2. 确认 STATE-01..05 均 pass，blocking_reasons 为空。
    3. 确认 source audit 重新解析 28 个计划、runner CLI、路径、波次和 digests。
    4. 确认 Windows/macOS 四目标 provenance、lifecycle、full-state/recovery 均实时 verified。
  </how-to-verify>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope PhaseComplete -Target Local -Mode Strict</automated><human-check>逐条核对 ROADMAP Phase 1 Success Criteria 与 STATE-01..05，无任何人工覆盖红结果。</human-check></verify>
  <done>仅输入 approved 后 Phase 1 可标记完成。</done>
  <resume-signal>输入 approved，或描述未满足项。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 全部实现/证据 → phase completion | 最终聚合决定阶段是否可以被后续 Phase 2–8 消费。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-65 | Spoofing | phase completion | high | mitigate | Local+Strict 唯一组合、实时 provenance 与机器 source audit。 |
| T-01-66 | Tampering | planning/evidence drift | high | mitigate | digest lock、read-only reparse 与四目标 strict manifests。 |
| T-01-67 | Repudiation | final approval | medium | mitigate | 可重复命令与 SUMMARY 中的机器结果/人工结论。 |
</threat_model>

<verification>
- PhaseComplete Local Strict 返回 0。
</verification>

<success_criteria>
- STATE-01..05、本地安全合同与四目标外部证据全部通过。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-28-SUMMARY.md`
</output>
