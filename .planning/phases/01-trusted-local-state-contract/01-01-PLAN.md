---
phase: 01-trusted-local-state-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/fixtures/contracts/npm-package-allowlist.json
  - scripts/contracts/verify-npm-package-allowlist.ps1
  - scripts/contracts/test-npm-package-allowlist.ps1
autonomous: true
requirements:
  - STATE-01
  - STATE-02
must_haves:
  truths:
    - "7 个 SUS package@version 只能从固定公开 registry、空 user/global config 的隔离工作目录查询。"
    - "恶意项目 .npmrc、私有 registry 和 token 环境不能改变结果或进入输出。"
    - "错包名、版本、repository、缺字段、重复项或查询失败均返回非零。"
  artifacts:
    - path: "tests/fixtures/contracts/npm-package-allowlist.json"
      provides: "7 个精确 package@version 与官方 repository 允许清单"
    - path: "scripts/contracts/verify-npm-package-allowlist.ps1"
      provides: "隔离 npm 配置与公开 registry 的只读 metadata verifier"
    - path: "scripts/contracts/test-npm-package-allowlist.ps1"
      provides: "正例、伪造 metadata、恶意 npm 配置与泄漏自测"
  key_links:
    - from: "scripts/contracts/verify-npm-package-allowlist.ps1"
      to: "tests/fixtures/contracts/npm-package-allowlist.json"
      via: "逐项规范化并精确比较 name/version/repository"
      pattern: "registry.npmjs.org"
---

<objective>
建立首次 npm install 前可重复、隔离且不会受本机或仓库 npm 配置污染的包身份门禁。

Purpose: 关闭供应链 legitimacy 与恶意 .npmrc/token 环境影响风险。
Output: 固定 allowlist、生产 verifier 与正反自测。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
@.planning/phases/01-trusted-local-state-contract/01-VALIDATION.md
</context>

## Artifacts this phase produces

- `tests/fixtures/contracts/npm-package-allowlist.json`：7 个精确 package@version 与官方 repository 允许清单
- `scripts/contracts/verify-npm-package-allowlist.ps1`：隔离 npm 配置与公开 registry 的只读 metadata verifier
- `scripts/contracts/test-npm-package-allowlist.ps1`：正例、伪造 metadata、恶意 npm 配置与泄漏自测

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 固定隔离 npm metadata verifier</name>
  <files>tests/fixtures/contracts/npm-package-allowlist.json, scripts/contracts/verify-npm-package-allowlist.ps1</files>
  <behavior>
    - 正确公开 metadata 全部匹配时返回 0。
    - 任一 identity 字段、重复项、额外项或查询错误返回非零。
    - 输出不包含 npm token、Authorization 或 npm 配置正文。
  </behavior>
  <action>创建 7 项 exact allowlist。verifier 为每次运行创建临时空 userconfig/globalconfig 和无 .npmrc 的临时工作目录，固定 https://registry.npmjs.org/，显式清除 token/registry 相关环境变量后调用 npm view；不得执行 install、修改 package 文件或从当前仓库目录启动 npm。fixture 模式必须复用同一 schema 与精确比较函数。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/verify-npm-package-allowlist.ps1 -Allowlist tests/fixtures/contracts/npm-package-allowlist.json</automated></verify>
  <done>真实公开 metadata 与 allowlist 一致，隔离配置和脱敏输出均由脚本强制。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 证明恶意 npm 配置和伪造 metadata 不能绕过门禁</name>
  <files>scripts/contracts/test-npm-package-allowlist.ps1</files>
  <behavior>
    - 正控制通过同一生产 predicate。
    - 错名、错版本、错 repository、缺字段、重复/额外项分别失败。
    - 恶意 .npmrc、私有 registry 与 token canary 不影响查询且不泄漏。
  </behavior>
  <action>在临时目录生成正反 metadata，并额外构造指向私有 registry 的 .npmrc、伪 token 环境和项目级 auth 配置；调用生产 verifier，断言真实请求仍只指向固定公开 registry，输出与临时文件不含 canary。自测不得创建或修改仓库 package.json、lockfile 或 node_modules。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/test-npm-package-allowlist.ps1</automated></verify>
  <done>正控制通过，所有伪造与配置污染场景 fail-closed，canary 扫描为零。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → 本地 verifier | 外部 package metadata 与本机 npm 配置共同影响后续可执行代码身份。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing | npm package identity | high | mitigate | exact package/version/repository allowlist 与固定公开 registry。 |
| T-01-02 | Information Disclosure | npm auth configuration | high | mitigate | 临时空 user/global config、隔离 cwd、清除 token 环境并执行 canary 测试。 |
| T-01-SC | Tampering | npm installs | high | mitigate | 本计划禁止安装；01-02 人工批准后才允许首次 install。 |
</threat_model>

<verification>
- 运行 package verifier 自测与真实 registry 校验。
- 确认 git diff 中没有 package/lock/node_modules 变更。
</verification>

<success_criteria>
- 7 项身份可机器复核。
- 恶意 npm 配置不能影响或泄漏。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-01-SUMMARY.md`
</output>
