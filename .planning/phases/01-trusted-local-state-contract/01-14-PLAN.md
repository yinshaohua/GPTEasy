---
phase: 01-trusted-local-state-contract
plan: 14
type: execute
wave: 6
depends_on: [01-04, 01-06, 01-10, 01-12]
files_modified:
  - scripts/contracts/assert-macos-job-lifecycle.zsh
  - scripts/contracts/run-macos.zsh
  - scripts/contracts/test-macos-package-verifier.zsh
  - tests/fixtures/contracts/packaging/macos-positive-control.json
  - .github/workflows/phase1-macos-evidence.yml
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04
  - STATE-05
must_haves:
  truths:
    - "macOS package predicate 有同一生产 predicate 的 test-only 正控制且 strict_gate_eligible=false。"
    - "Intel/Apple Silicon native jobs 必须 needs wave0，并在 per-job OS user 中运行。"
    - "codesign、notary/stapling、Gatekeeper、~/Applications 与账户/profile cleanup 全部通过才有 strict pass。"
  artifacts:
    - path: "scripts/contracts/assert-macos-job-lifecycle.zsh"
      provides: "runner/job/uid/profile 创建与销毁/基线恢复证明"
    - path: "scripts/contracts/run-macos.zsh"
      provides: "codesign/notary/Gatekeeper/current-user/path predicate"
    - path: "scripts/contracts/test-macos-package-verifier.zsh"
      provides: "生产 predicate 正控制、负例与 success aggregation"
    - path: "tests/fixtures/contracts/packaging/macos-positive-control.json"
      provides: "test-only 有效签名/arch/current-user/profile metadata 正控制"
    - path: ".github/workflows/phase1-macos-evidence.yml"
      provides: "macOS Intel/Apple Silicon Wave0-dependent attested workflow"
  key_links:
    - from: ".github/workflows/phase1-macos-evidence.yml"
      to: ".github/workflows/phase1-macos-wave0.yml"
      via: "job-level reusable workflow 与 needs wave0"
      pattern: "needs: wave0"
---

<objective>
建立 macOS 双架构 package predicate、一次性 OS 用户生命周期证据与 Wave 0 依赖 workflow。

Purpose: 让 zsh、签名、公证、Gatekeeper 与账户清理均进入 native evidence。
Output: macOS lifecycle guard、package runner、自测 fixture 与 evidence workflow。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trusted-local-state-contract/01-04-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-06-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-10-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-12-SUMMARY.md
@.codex/skills/spike-findings-gpteasy/references/macos-host-contract.md
</context>

## Artifacts this phase produces

- `scripts/contracts/assert-macos-job-lifecycle.zsh`：runner/job/uid/profile 创建与销毁/基线恢复证明
- `scripts/contracts/run-macos.zsh`：codesign/notary/Gatekeeper/current-user/path predicate
- `scripts/contracts/test-macos-package-verifier.zsh`：生产 predicate 正控制、负例与 success aggregation
- `tests/fixtures/contracts/packaging/macos-positive-control.json`：test-only 有效签名/arch/current-user/profile metadata 正控制
- `.github/workflows/phase1-macos-evidence.yml`：macOS Intel/Apple Silicon Wave0-dependent attested workflow

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 macOS 生命周期与 package 生产 predicate</name>
  <files>scripts/contracts/assert-macos-job-lifecycle.zsh, scripts/contracts/run-macos.zsh, scripts/contracts/test-macos-package-verifier.zsh, tests/fixtures/contracts/packaging/macos-positive-control.json</files>
  <behavior>
    - 正控制通过同一 predicate，但 strict_gate_eligible=false。
    - non-Darwin、wrong-arch、missing codesign/notary/Gatekeeper、system install、marker-only、cleanup 缺失分别失败。
    - native pass 需要 ~/Applications、目标 arch 与 attested lifecycle。
  </behavior>
  <action>guard 绑定 run/attempt/job、runner identity、临时 OS 用户 uid/home；finally 停止 run-scoped app，删除账户/home并复核，或在 ephemeral runner 上验证完整 baseline restore。run-macos 复用同一 predicate 处理 fixture/real facts；fixture_mode 永不 strict eligible。test script 对全部 zsh 文件执行 zsh -n，再跑正控制和拒绝分支。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/test-macos-wave0-contract.ps1 -Scripts scripts/contracts/assert-macos-job-lifecycle.zsh,scripts/contracts/run-macos.zsh,scripts/contracts/test-macos-package-verifier.zsh -Workflow .github/workflows/phase1-macos-wave0.yml</automated></verify>
  <done>macOS 正控制、负例与语法入口完整。</done>
</task>

<task type="auto">
  <name>Task 2: 创建 Wave 0 依赖的 macOS attested workflow</name>
  <files>.github/workflows/phase1-macos-evidence.yml</files>
  <action>workflow 先以 job-level uses 调用 phase1-macos-wave0.yml；Intel/Apple Silicon native jobs 均 needs wave0。每个 job exact checkout、创建 per-job OS user，运行 CLI/正式宿主 canary parity、Developer ID build、公证/stapling、Gatekeeper 与 current-user path smoke；统一 finally 清理 app 与账户/profile，随后上传 app/evidence 并生成 attestation。Wave 0 或 cleanup 失败时不得上传 strict pass manifest。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/test-macos-wave0-contract.ps1 -Scripts scripts/contracts/probe-codex-macos.zsh,scripts/contracts/probe-macos-host.zsh,scripts/contracts/test-macos-contract-probes.zsh,scripts/contracts/assert-macos-job-lifecycle.zsh,scripts/contracts/run-macos.zsh,scripts/contracts/test-macos-package-verifier.zsh -Workflow .github/workflows/phase1-macos-wave0.yml -EvidenceWorkflow .github/workflows/phase1-macos-evidence.yml; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope PackagingSelfTest -Target Local -Mode Strict</automated></verify>
  <done>macOS workflow 的语法、依赖与 predicate contract 全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macOS runner/package → 当前用户 profile | 签名 app 在真实用户目录执行并依赖 zsh/OS 生命周期。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-31 | Spoofing | macOS bundle | high | mitigate | native arch、codesign/notary/Gatekeeper 与 attestation。 |
| T-01-32 | Tampering | disposable account claim | critical | mitigate | runner/job/uid identity 与 attested delete/restore。 |
| T-01-33 | Information Disclosure | Apple credentials | critical | mitigate | 只在 CI secret store 消费，证据 allowlist/canary。 |
</threat_model>

<verification>
- macOS Wave0 contract 与 PackagingSelfTest Local 返回 0。
</verification>

<success_criteria>
- 真实 macOS evidence 依赖 zsh Wave 0 与可证明账户清理。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-14-SUMMARY.md`
</output>
