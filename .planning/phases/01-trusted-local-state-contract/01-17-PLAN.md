---
phase: 01-trusted-local-state-contract
plan: 17
type: execute
wave: 9
depends_on: [01-16]
files_modified:
  - src-tauri/src/state/mod.rs
  - src-tauri/src/state/migrations/0001_initial.sql
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
  - src-tauri/tests/state_command_restart.rs
autonomous: true
requirements: [STATE-01, STATE-02]
must_haves:
  truths:
    - "第一个生产状态能力是已注册 Tauri command→SQLite→新 OS 进程 bootstrap 的真实竖切。"
    - "两个子进程均按 command 名经过 Tauri mock IPC；测试不直接调用 StateStore 绕过接线。"
    - "生产状态路径只来自 app_local_data_dir；测试 root 不进入 release CLI。"
  artifacts:
    - path: "src-tauri/src/state/mod.rs"
      provides: "初始 StateStore、schema metadata 与 settings repository"
    - path: "src-tauri/src/state/migrations/0001_initial.sql"
      provides: "批准后的永久 schema version 1"
    - path: "src-tauri/src/commands.rs"
      provides: "update_app_settings 与 bootstrap_state commands"
    - path: "src-tauri/src/lib.rs"
      provides: "生产 invoke handler 与状态初始化 wiring"
    - path: "src-tauri/tests/state_command_restart.rs"
      provides: "两进程 Tauri IPC tracer"
  key_links:
    - from: "src-tauri/tests/state_command_restart.rs"
      to: "src-tauri/src/commands.rs"
      via: "current_exe 子进程构造 mock app 并按 command 名 invoke"
      pattern: "update_app_settings|bootstrap_state"
  assumptions:
    - "FLAGGED_UNVERIFIED [STATE-01]: specless edge probe 为 unclassified；本 tracer 只证明一个设置字段的真实重开路径，完整状态由 01-18 扩展。"
    - "FLAGGED_UNVERIFIED [STATE-02]: specless edge probe 为 unclassified；本 tracer 必须证明固定当前用户路径与公开 DTO 脱敏，完整 local-only 静态边界由 01-19 验证。"
  prohibitions:
    - "FLAGGED_UNVERIFIED: 不得让测试直接调用 StateStore 冒充 Tauri command 端到端路径。"
    - "FLAGGED_UNVERIFIED: 不得把测试路径参数暴露给 production CLI 或 release package。"
---

<objective>
交付 Phase 1 生产 tracer：Tauri command 写入 SQLite，进程退出后由全新进程 bootstrap 读回。

Purpose: 在 schema/backup 决策批准后立即证明 command、磁盘与跨进程架构闭环。
Output: 0001 schema、StateStore、窄 commands、composition wiring 与真实两进程测试。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0002-tauri-rust-react.md
@docs/adr/0006-sqlite-application-state.md
@.planning/phases/01-trusted-local-state-contract/01-10-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-16-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-PATTERNS.md
</context>

## Artifacts this phase produces

- `src-tauri/src/state/mod.rs`：初始 StateStore、schema metadata 与 settings repository
- `src-tauri/src/state/migrations/0001_initial.sql`：批准后的永久 schema version 1
- `src-tauri/src/commands.rs`：update_app_settings 与 bootstrap_state commands
- `src-tauri/src/lib.rs`：生产 invoke handler 与状态初始化 wiring
- `src-tauri/tests/state_command_restart.rs`：两进程 Tauri IPC tracer

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 贯通 Tauri command 的两进程设置重开路径</name>
  <read_first>
    @src-tauri/src/lib.rs
    @src-tauri/src/path_smoke.rs
    @src-tauri/tests/path_smoke.rs
    @docs/adr/0002-tauri-rust-react.md
    @docs/adr/0006-sqlite-application-state.md
    @.planning/phases/01-trusted-local-state-contract/01-16-SUMMARY.md
  </read_first>
  <precondition>01-16-SUMMARY.md 同时记录 freeze-approved、approve-schema-version-1 与 approve-db-backup-contract。</precondition>
  <reversibility rating="one-way">0001_initial.sql 与 application identity 一经后续 fixture/用户数据库消费便只能通过追加 migration 演进。</reversibility>
  <files>src-tauri/src/state/mod.rs, src-tauri/src/state/migrations/0001_initial.sql, src-tauri/src/commands.rs, src-tauri/src/lib.rs, src-tauri/tests/state_command_restart.rs</files>
  <behavior>
    - RED：write 子进程按 command 名 invoke 后，read 子进程尚无法 bootstrap 同一设置。
    - GREEN：write 子进程写 theme=dark 与固定 run ID 后退出，read 子进程重建 app 并读回。
    - 测试源码不导入 StateStore，也不直接调用 open/write/read。
  </behavior>
  <action>先提交失败 integration test，再按 01-16 批准的六张 STRICT 表、APPLICATION_ID、user_version、database UUID、schema fingerprint 与 migration ledger 实现 0001。StateStore 使用 prepared SQL；build_app 注册 update_app_settings/bootstrap_state 两个 typed command；公开 BootstrapState 与错误不含 API Key 或绝对路径。测试主进程通过 current_exe 启动两个精确过滤的子进程，测试专用 root 只能由 integration-test 子进程环境读取，production run_with_args 不接受该参数。</action>
  <acceptance_criteria>同一测试在 RED 阶段因未实现 command/disk reopen 失败，GREEN 后由两个 OS 进程通过已注册 command 完成设置 round-trip。</acceptance_criteria>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test state_command_restart -- --nocapture</automated></verify>
  <done>真实 Tauri command、SQLite 与新进程 bootstrap 形成后续状态扩展的生产骨架。</done>
</task>

</tasks>

<threat_model>
Security level: ASVS L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri command → SQLite | WebView 意图跨入 Rust 权威状态和永久磁盘 schema。 |
| integration test → 状态路径 | 测试隔离能力不能进入生产 CLI。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-40 | Tampering | command input | high | mitigate | typed DTO、DB CHECK/FK 与 prepared SQL。 |
| T-01-41 | Elevation of Privilege | test root | high | mitigate | 仅 integration-test 子进程消费，production CLI 无路径参数。 |
| T-01-42 | Tampering | permanent schema | high | mitigate | 01-16 one-way approval、application ID、ledger/checksum/fingerprint。 |
</threat_model>

<verification>
- state_command_restart 返回 0。
- 测试源码的 import/call 扫描证明没有 StateStore 直连路径。
</verification>

<success_criteria>
- 生产 tracer 在 command、SQLite、关闭和全新进程重开四层真实贯通。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-17-SUMMARY.md`
</output>
