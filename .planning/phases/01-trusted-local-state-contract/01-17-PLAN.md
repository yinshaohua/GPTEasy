---
phase: 01-trusted-local-state-contract
plan: 17
type: execute
wave: 8
depends_on: [01-15, 01-16]
files_modified:
  []
autonomous: false
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04
  - STATE-05
must_haves:
  truths:
    - "Freeze 只消费四目标 independently attested manifests、gh preflight 与 strict predicates。"
    - "永久 schema version 1 与 backup/recovery 合同分别在实现前获得明确批准。"
    - "AllowBlocked、static COVERED、synthetic fixture 或缺 lifecycle cleanup 均不能解除 checkpoint。"
  artifacts:
    []
  key_links:
    - from: "01-15/01-16 target manifests"
      to: "scripts/contracts/run-phase1-contracts.ps1"
      via: "Freeze Local Strict 聚合全部 provenance/contract/package 条件"
      pattern: "-Scope Freeze -Target Local -Mode Strict"
---

<objective>
在独立 Windows/macOS attested manifests 全绿后冻结外部事实，并批准 schema 与 backup 两个 one-way 合同。

Purpose: 把不可逆磁盘/备份合同放在生产实现之前，并禁止人工覆盖 Strict 红结果。
Output: freeze 批准、schema version 1 决策与 backup/recovery 决策记录。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONTEXT.md
@docs/adr/0001-plaintext-provider-credentials.md
@docs/adr/0005-immutable-provider-identity.md
@docs/adr/0006-sqlite-application-state.md
@.planning/phases/01-trusted-local-state-contract/01-15-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-16-SUMMARY.md
</context>

## Artifacts this phase produces

- 本计划不创建或修改仓库文件；只记录门禁结论。

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 批准外部实现契约 freeze</name>
  <precondition>Windows x64/ARM64 与 macOS Intel/Apple Silicon 的 strict contract/package 命令全部返回 0。</precondition>
  <action>运行唯一 Freeze 命令，核对四目标 Codex 0.146.1、Windows/macOS 正式宿主共享用户层、Windows WSL2、签名/current-user package、runner/account lifecycle 与 provenance。人工不得把 blocked、AllowBlocked 或 static audit 文本改判为通过。</action>
  <what-built>01-15 与 01-16 已分别导入 Windows/macOS attested evidence。</what-built>
  <how-to-verify>
    1. 运行 Freeze Local Strict。
    2. 确认四目标 strict_gate_eligible=true 且 blocking_reasons 为空。
    3. 确认每份 evidence 的 run/job/artifact/commit/digest 与 cleanup attestation 实时验证。
  </how-to-verify>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope Freeze -Target Local -Mode Strict</automated><human-check>四目标事实与 ROADMAP 风险理由逐项一致。</human-check></verify>
  <done>输入 freeze-approved 后才进入两个 one-way 决策。</done>
  <resume-signal>输入 freeze-approved，或描述未满足项。</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: 批准永久 schema version 1</name>
  <precondition>Freeze Local Strict 返回 0 且 Task 1 已批准。</precondition>
  <reversibility rating="one-way">0001 migration 发布后只能追加演进，修改历史需要迁移所有用户数据库。</reversibility>
  <decision>是否批准 APPLICATION_ID、schema version 1、database UUID/schema fingerprint、双账本及 providers/provider_verifications/managed_environments/app_settings/state_metadata/schema_migrations 六张 STRICT 表？</decision>
  <context>API Key 按 ADR 明文入库；运行中进程、配置 Saga、WSL 切换和日志仍不进入本阶段 schema。</context>
  <options><option id="approve-schema-version-1"><name>批准 schema version 1</name><pros>满足 STATE-01/03 与永久历史要求。</pros><cons>后续只能新增 migration。</cons></option><option id="stop-and-revise-schema"><name>停止并修订</name><pros>避免固化错误磁盘合同。</pros><cons>生产 tracer 保持阻断。</cons></option></options>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope Freeze -Target Local -Mode Strict</automated></verify>
  <done>仅 approve-schema-version-1 允许 01-18 创建 0001。</done>
  <resume-signal>选择 approve-schema-version-1 或 stop-and-revise-schema。</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: 批准数据库 backup identity 与 quarantine 合同</name>
  <precondition>schema version 1 已批准。</precondition>
  <reversibility rating="one-way">备份文件名/sidecar/identity 会被已发布恢复流程长期读取。</reversibility>
  <decision>是否批准 UTC+schema+database UUID+backup UUID 命名、sidecar digest/identity、三份 retention、同源复用、兼容判定与 verified quarantine 协议？</decision>
  <context>只有完整 validator 通过的 snapshot 可参与 retention/reuse；恢复前无法保全当前唯一数据库时必须阻断。</context>
  <options><option id="approve-db-backup-contract"><name>批准 backup 合同</name><pros>STATE-04/05 获得稳定恢复格式。</pros><cons>格式变化需要兼容适配。</cons></option><option id="stop-and-revise-backup"><name>停止并修订</name><pros>避免固化不完整恢复协议。</pros><cons>backup/recovery 实现保持阻断。</cons></option></options>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope Freeze -Target Local -Mode Strict</automated></verify>
  <done>仅 approve-db-backup-contract 允许 01-24/01-25 实现备份与恢复。</done>
  <resume-signal>选择 approve-db-backup-contract 或 stop-and-revise-backup。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| attested evidence → one-way disk contracts | 外部事实和人工批准共同决定永久 schema/backup 格式。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-38 | Tampering | freeze decision | high | mitigate | 唯一 Strict runner、独立 manifests 与阻断 checkpoint。 |
| T-01-39 | Repudiation | one-way approval | high | mitigate | 两个独立 decision ID 记录于 SUMMARY。 |
</threat_model>

<verification>
- Freeze Local Strict 返回 0。
- SUMMARY 记录 freeze-approved、approve-schema-version-1、approve-db-backup-contract。
</verification>

<success_criteria>
- 所有不可逆决定均先于生产实现。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-17-SUMMARY.md`
</output>
