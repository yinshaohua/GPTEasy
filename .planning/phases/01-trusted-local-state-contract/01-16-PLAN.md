---
phase: 01-trusted-local-state-contract
plan: 16
type: execute
wave: 8
depends_on: [01-15]
files_modified: []
autonomous: false
requirements: [STATE-01, STATE-03, STATE-04, STATE-05]
must_haves:
  truths:
    - "用户只在 Freeze Local Strict 的非签名检查全绿后批准永久 schema version 1 与 backup/recovery 格式。"
    - "批准记录明确声明正式 Windows/macOS 签名、公证和工件 evidence 延期到 01-26/01-27，且不构成 PhaseComplete。"
    - "任何非签名合同红项都不能由人工文字覆盖。"
  artifacts: []
  key_links:
    - from: "01-15 Freeze Local Strict"
      to: "01-17 production tracer"
      via: "freeze-approved、approve-schema-version-1 与 approve-db-backup-contract 三个明确结论"
      pattern: "approve-schema-version-1|approve-db-backup-contract"
  assumptions:
    - "FLAGGED_UNVERIFIED [STATE-01]: specless edge probe 为 unclassified；schema 中完整状态字段的充分性仍需 01-17/01-18 round-trip 验证。"
    - "FLAGGED_UNVERIFIED [STATE-03]: specless edge probe 为 unclassified；append-only 历史语义仍需 01-20/01-21 验证。"
    - "FLAGGED_UNVERIFIED [STATE-04]: specless edge probe 为 unclassified；backup identity/retention/rollback 仍需 01-23 验证。"
    - "FLAGGED_UNVERIFIED [STATE-05]: specless edge probe 为 unclassified；higher-schema 与 restore 仍需 01-24 验证。"
  prohibitions:
    - "FLAGGED_UNVERIFIED: 不得以签名缺失为理由跳过 schema/backup 技术审查，也不得把技术批准写成签名已完成。"
---

<objective>
在诚实的非签名合同冻结后，明确批准两个 one-way 本地磁盘合同，并记录发布签名延期边界。

Purpose: 让生产 SQLite tracer 可以开始，同时保留单向决策的人类审查与最终发布证据硬门。
Output: freeze、schema version 1 与 backup/recovery 合同的明确决策记录。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONTEXT.md
@docs/adr/0001-plaintext-provider-credentials.md
@docs/adr/0005-immutable-provider-identity.md
@docs/adr/0006-sqlite-application-state.md
@.planning/phases/01-trusted-local-state-contract/01-15-SUMMARY.md
</context>

## Artifacts this phase produces

None — 本计划只记录阻断式技术决策，不创建或修改仓库文件。

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: 批准非签名合同冻结范围</name>
  <read_first>
    @.planning/phases/01-trusted-local-state-contract/01-15-SUMMARY.md
    @.planning/phases/01-trusted-local-state-contract/COVERAGE.md
    @.planning/ROADMAP.md
  </read_first>
  <precondition>Freeze Local Strict 返回 0，且输出 freeze_kind=non_signing_contract、release_ready=false 与四目标 deferred_evidence。</precondition>
  <action>核对 Freeze 实际执行的 path、process、Codex/host/WSL、provenance self-test 与 Windows/macOS install-workflow predicates；确认结果只支持本地 schema/backup 决策。记录本次用户决定：Windows Authenticode PFX、macOS Developer ID 与 notarization 在 01-26/01-27 才要求，当前不创建替代材料。任何非签名检查非零时停止。</action>
  <what-built>01-15 已把 Freeze 固定为非签名技术合同门，并把真实平台签名/公证保留为 PhaseComplete 的独立硬门。</what-built>
  <how-to-verify>
    1. 运行 Freeze Local Strict。
    2. 确认 freeze_kind=non_signing_contract、release_ready=false。
    3. 确认 deferred_evidence 明列 Windows x64/ARM64 Authenticode 与 macOS Intel/Apple Silicon Developer ID/notary。
    4. 确认 blocking_reasons 为空，且没有 formal platform manifest 被伪造或要求。
  </how-to-verify>
  <acceptance_criteria>人工结论为 freeze-approved，并明确列出签名/公证仍 deferred、PhaseComplete 仍 blocked。</acceptance_criteria>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope Freeze -Target Local -Mode Strict</automated><human-check>Freeze 只冻结非签名技术合同，没有声称平台发布身份已经验证。</human-check></verify>
  <done>freeze-approved 只允许进入两个本地磁盘 one-way 决策。</done>
  <resume-signal>输入 freeze-approved，或描述任一非签名证据缺口。</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: 批准永久 schema version 1</name>
  <read_first>
    @docs/adr/0001-plaintext-provider-credentials.md
    @docs/adr/0005-immutable-provider-identity.md
    @docs/adr/0006-sqlite-application-state.md
    @.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
  </read_first>
  <precondition>Task 1 已记录 freeze-approved。</precondition>
  <reversibility rating="one-way">0001 migration 一经用户数据库消费便只能追加演进，改写需要迁移所有已存在数据库。</reversibility>
  <decision>是否批准固定 APPLICATION_ID、schema version 1、database UUID、schema fingerprint、双账本，以及 providers/provider_verifications/managed_environments/app_settings/state_metadata/schema_migrations 六张 STRICT 表？</decision>
  <context>API Key 按 ADR 明文入库；运行中进程、配置 Saga、WSL 切换和诊断日志不属于本阶段 schema。签名延期与该磁盘合同无技术依赖。</context>
  <options><option id="approve-schema-version-1"><name>批准 schema version 1</name><pros>可立即实现完整状态与永久历史链。</pros><cons>发布后只能追加 migration。</cons></option><option id="stop-and-revise-schema"><name>停止并修订</name><pros>避免固化错误磁盘模型。</pros><cons>01-17 生产 tracer 保持阻断。</cons></option></options>
  <action>逐表核对主键、不可变 provider/environment identity、明文 Key 内部边界、FK、CHECK、application_id、user_version、ledger checksum 与 fingerprint；选择批准或停止，不能用后续迁移作为省略当前字段的理由。</action>
  <acceptance_criteria>仅 approve-schema-version-1 解除 01-17 创建 0001_initial.sql 的前置条件。</acceptance_criteria>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope Freeze -Target Local -Mode Strict</automated></verify>
  <done>SUMMARY 精确记录 schema 选择与 one-way 理由。</done>
  <resume-signal>选择 approve-schema-version-1 或 stop-and-revise-schema。</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: 批准数据库 backup identity 与 quarantine 合同</name>
  <read_first>
    @docs/adr/0006-sqlite-application-state.md
    @.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
    @.codex/skills/spike-findings-gpteasy/references/safe-config-editing.md
  </read_first>
  <precondition>Task 2 已选择 approve-schema-version-1。</precondition>
  <reversibility rating="one-way">备份命名、sidecar identity 与 quarantine 协议会被已发布恢复流程长期读取，变更需兼容历史备份。</reversibility>
  <decision>是否批准 UTC+schema+database UUID+backup UUID 命名、sidecar digest/identity、三份 verified retention、同源复用、兼容判定与恢复前 verified quarantine？</decision>
  <context>只有统一只读 DB validator 通过的 snapshot 可参与 retention/reuse；当前唯一数据库无法先被验证保全时 restore 必须阻断。</context>
  <options><option id="approve-db-backup-contract"><name>批准 backup 合同</name><pros>STATE-04/05 获得稳定恢复格式。</pros><cons>格式变化必须兼容已存在 sidecar 与 snapshot。</cons></option><option id="stop-and-revise-backup"><name>停止并修订</name><pros>避免固化不完整恢复协议。</pros><cons>01-23/01-24 保持阻断。</cons></option></options>
  <action>核对 Online Backup API、snapshot 完整验证、三份 retention、source/target identity reuse、quarantine 先于替换、opaque backup ID 和同目录原子恢复；选择批准或停止，不接受普通文件复制或任意路径恢复。</action>
  <acceptance_criteria>仅 approve-db-backup-contract 解除 01-23/01-24 的 backup/recovery 实现前置条件。</acceptance_criteria>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope Freeze -Target Local -Mode Strict</automated></verify>
  <done>SUMMARY 同时记录 freeze-approved、approve-schema-version-1、approve-db-backup-contract 与四目标签名延期。</done>
  <resume-signal>选择 approve-db-backup-contract 或 stop-and-revise-backup。</resume-signal>
</task>

</tasks>

<threat_model>
Security level: ASVS L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 非签名机器证据 → 人工 one-way 决策 | 技术合同足以决定磁盘格式，但不能决定发布身份。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-37 | Tampering | schema approval | high | mitigate | Freeze 前置、逐表 decision checkpoint 与明确 resume signal。 |
| T-01-38 | Tampering | backup approval | high | mitigate | 独立 checkpoint、Online Backup/identity/quarantine 必选合同。 |
| T-01-39 | Spoofing | signing deferral | high | mitigate | 决策记录 release_ready=false，01-28 仍要求真实平台证据。 |
</threat_model>

<verification>
- Freeze Local Strict 返回 0。
- SUMMARY 记录三个批准结论和未完成签名/公证清单。
</verification>

<success_criteria>
- 两个 one-way 合同均经过明确人工决策。
- 发布签名延期没有被解释为完成或替代证据。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-16-SUMMARY.md`
</output>
