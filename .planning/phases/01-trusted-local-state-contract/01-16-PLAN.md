---
phase: 01-trusted-local-state-contract
plan: 16
type: execute
wave: 7
depends_on: [01-06, 01-14]
files_modified:
  - tests/fixtures/contracts/codex/macos-intel/manifest.json
  - tests/fixtures/contracts/host/macos-intel/manifest.json
  - tests/fixtures/contracts/packaging/macos-intel/manifest.json
  - tests/fixtures/contracts/codex/macos-apple-silicon/manifest.json
  - tests/fixtures/contracts/host/macos-apple-silicon/manifest.json
  - tests/fixtures/contracts/packaging/macos-apple-silicon/manifest.json
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04
  - STATE-05
user_setup:
  - service: GitHub Actions macOS 原生签名 runners
    why: 提供 Intel/Apple Silicon、Developer ID、公证、正式 Codex/ChatGPT host
    dashboard_config:
      - task: 配置 native ephemeral runner labels、Developer ID 与 notary secrets
        location: GitHub repository Actions settings；秘密不得写入仓库或对话
must_haves:
  truths:
    - "Intel 与 Apple Silicon evidence jobs 都先通过可复用 zsh Wave 0。"
    - "两架构分别证明 official CLI 与正式宿主 bundled Codex 读取同一用户层 canary。"
    - "codesign/notary/Gatekeeper、~/Applications 与 uid/home cleanup 全部通过。"
  artifacts:
    - path: "tests/fixtures/contracts/codex/macos-intel/manifest.json"
      provides: "macOS Intel Codex 0.146.1 contract evidence"
    - path: "tests/fixtures/contracts/host/macos-intel/manifest.json"
      provides: "macOS Intel 正式宿主与 CLI parity evidence"
    - path: "tests/fixtures/contracts/packaging/macos-intel/manifest.json"
      provides: "macOS Intel signed/notarized current-user package evidence"
    - path: "tests/fixtures/contracts/codex/macos-apple-silicon/manifest.json"
      provides: "macOS Apple Silicon Codex 0.146.1 contract evidence"
    - path: "tests/fixtures/contracts/host/macos-apple-silicon/manifest.json"
      provides: "macOS Apple Silicon 正式宿主与 CLI parity evidence"
    - path: "tests/fixtures/contracts/packaging/macos-apple-silicon/manifest.json"
      provides: "macOS Apple Silicon signed/notarized current-user package evidence"
  key_links:
    - from: "macOS evidence jobs"
      to: ".github/workflows/phase1-macos-wave0.yml"
      via: "needs wave0 后才运行 native contract/package"
      pattern: "wave0_passed"
---

<objective>
获取 macOS Intel 与 Apple Silicon 独立 attested contract/package freeze evidence。

Purpose: 把双架构 zsh、CLI/宿主 parity、签名公证与账户生命周期纳入 freeze。
Output: 六份经主工作区重新取回和验签的 macOS manifests。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trusted-local-state-contract/01-06-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-14-SUMMARY.md
</context>

## Artifacts this phase produces

- `tests/fixtures/contracts/codex/macos-intel/manifest.json`：macOS Intel Codex 0.146.1 contract evidence
- `tests/fixtures/contracts/host/macos-intel/manifest.json`：macOS Intel 正式宿主与 CLI parity evidence
- `tests/fixtures/contracts/packaging/macos-intel/manifest.json`：macOS Intel signed/notarized current-user package evidence
- `tests/fixtures/contracts/codex/macos-apple-silicon/manifest.json`：macOS Apple Silicon Codex 0.146.1 contract evidence
- `tests/fixtures/contracts/host/macos-apple-silicon/manifest.json`：macOS Apple Silicon 正式宿主与 CLI parity evidence
- `tests/fixtures/contracts/packaging/macos-apple-silicon/manifest.json`：macOS Apple Silicon signed/notarized current-user package evidence

<tasks>

<task type="auto">
  <name>Task 1: 获取并导入 macOS Intel freeze evidence</name>
  <precondition>01-06 gh preflight 已 approved，Intel native runner、Developer ID/notary 与正式宿主可用。</precondition>
  <files>tests/fixtures/contracts/codex/macos-intel/manifest.json, tests/fixtures/contracts/host/macos-intel/manifest.json, tests/fixtures/contracts/packaging/macos-intel/manifest.json</files>
  <action>调度 MacIntel workflow；必须先看到 reusable Wave 0 成功并实际执行 zsh -n 与 zsh tests。主工作区重新下载/验签后，只导入 CLI/host parity、codesign/notary/Gatekeeper、~/Applications 与 cleanup_attested 全绿的三份 manifest。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope ContractSelfTest -Target MacIntel -Mode Strict; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope PackagingSelfTest -Target MacIntel -Mode Strict</automated></verify>
  <done>macOS Intel native evidence 通过 Wave 0、provenance 与 package predicates。</done>
</task>

<task type="auto">
  <name>Task 2: 获取并导入 macOS Apple Silicon freeze evidence</name>
  <precondition>01-06 gh preflight 已 approved，Apple Silicon native runner、Developer ID/notary 与正式宿主可用。</precondition>
  <files>tests/fixtures/contracts/codex/macos-apple-silicon/manifest.json, tests/fixtures/contracts/host/macos-apple-silicon/manifest.json, tests/fixtures/contracts/packaging/macos-apple-silicon/manifest.json</files>
  <action>按与 Intel 相同的独立流程调度 MacAppleSilicon；禁止 universal/cross-build 或 Intel 结果替代。导入前要求 Wave 0、host-vs-CLI canary、native arch、签名公证、Gatekeeper 与 uid/home cleanup 全绿。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope ContractSelfTest -Target MacAppleSilicon -Mode Strict; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope PackagingSelfTest -Target MacAppleSilicon -Mode Strict</automated></verify>
  <done>macOS Apple Silicon 独立 native evidence 可取回、可验签。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macOS Actions → committed fixtures | 远程 zsh/宿主/签名证据进入冻结输入。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-36 | Spoofing | macOS target architecture | high | mitigate | 双架构独立 native runner、uname/bundle arch 与 attestation。 |
| T-01-37 | Tampering | Wave 0 bypass | high | mitigate | workflow needs 与 manifest wave0_passed 硬门禁。 |
</threat_model>

<verification>
- 两个目标的 ContractSelfTest 与 PackagingSelfTest Strict 均返回 0。
</verification>

<success_criteria>
- macOS freeze evidence 真实执行 zsh 与正式宿主共享边界。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-16-SUMMARY.md`
</output>
