---
phase: 01-trusted-local-state-contract
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  []
autonomous: false
requirements:
  - STATE-01
  - STATE-02
must_haves:
  truths:
    - "机器隔离校验与人工官方来源确认都在首次 install 前完成。"
    - "任一 package@version 不一致时后续依赖安装保持阻断。"
  artifacts:
    []
  key_links:
    - from: "01-01 package verifier"
      to: "01-02 人工批准"
      via: "先执行机器校验再核对官方页面与模板"
      pattern: "approved"
---

<objective>
在首次 npm install 前完成人工官方来源批准。

Purpose: 把 SUS 包的发布者与官方模板来源交给不可自动替代的人工门禁。
Output: 7 项 exact package 的批准或阻断记录。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
@.planning/phases/01-trusted-local-state-contract/01-01-SUMMARY.md
@tests/fixtures/contracts/npm-package-allowlist.json
</context>

## Artifacts this phase produces

- 本计划不创建或修改仓库文件；只记录门禁结论。

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: 批准官方 npm package 身份</name>
  <precondition>scripts/contracts/test-npm-package-allowlist.ps1 与真实 registry 校验均返回 0。</precondition>
  <action>人工确认 React、Vite、TypeScript 与 DefinitelyTyped 的精确版本、发布者、repository 及官方 create-tauri-app React TypeScript 模板来源；不得粘贴 token、.npmrc 或个人身份信息。</action>
  <what-built>01-01 已提供隔离 registry verifier 与污染/泄漏负例，尚未安装依赖。</what-built>
  <how-to-verify>
    1. 运行自动校验并确认 7/7 identity 匹配。
    2. 在 npm 官方页面核对发布者与 repository。
    3. 对照官方 Tauri React TypeScript 模板。
    4. 全部一致时输入 approved；否则列出不一致项。
  </how-to-verify>
  <verify><automated>powershell -NoProfile -File scripts/contracts/test-npm-package-allowlist.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/verify-npm-package-allowlist.ps1 -Allowlist tests/fixtures/contracts/npm-package-allowlist.json</automated><human-check>7 个 package@version 的官方身份与模板来源一致。</human-check></verify>
  <done>仅 approved 解除 01-08 的 npm install 前置条件。</done>
  <resume-signal>输入 approved，或列出不一致的 package@version。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 自动 metadata → 人工批准 | 机器结果不能替代发布者与官方模板来源确认。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Spoofing | package approval | high | mitigate | 机器 exact allowlist 与人工官方来源双重门禁。 |
| T-01-04 | Repudiation | approval record | low | accept | SUMMARY 记录精确包清单与结论；单人主干流程风险可接受。 |
</threat_model>

<verification>
- 自动命令返回 0。
- checkpoint 明确记录 approved 或阻断原因。
</verification>

<success_criteria>
- 首次 install 之前完成双重 legitimacy 门禁。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-02-SUMMARY.md`
</output>
