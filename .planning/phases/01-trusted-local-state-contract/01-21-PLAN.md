---
phase: 01-trusted-local-state-contract
plan: 21
type: execute
wave: 12
depends_on: [01-20]
files_modified:
  - src-tauri/src/state/migrations/mod.rs
  - src-tauri/src/state/mod.rs
  - src-tauri/src/bin/generate_v001_fixture.rs
  - src-tauri/tests/fixture_generation.rs
  - tests/fixtures/databases/v001/state.sqlite3
  - tests/fixtures/databases/manifest.json
autonomous: true
requirements:
  - STATE-03
must_haves:
  truths:
    - "v001 fixture 在 migration_matrix 运行前已存在且 hash/application ID/user_version 可验证。"
    - "生成器使用固定数据/时间/SQLite 配置，默认拒绝覆盖。"
    - "StateStore 使用 append-only registry 而非分散 include。"
  artifacts:
    - path: "src-tauri/src/state/migrations/mod.rs"
      provides: "连续版本、名称、checksum、schema fingerprint 的 append-only registry"
    - path: "src-tauri/src/state/mod.rs"
      provides: "StateStore 对 migration registry 的 wiring"
    - path: "src-tauri/src/bin/generate_v001_fixture.rs"
      provides: "确定性 create-once fixture generator"
    - path: "src-tauri/tests/fixture_generation.rs"
      provides: "确定性、no-overwrite 与 fixed-data tests"
    - path: "tests/fixtures/databases/v001/state.sqlite3"
      provides: "永久 schema version 1 SQLite fixture"
    - path: "tests/fixtures/databases/manifest.json"
      provides: "fixture path、digest、application ID、user_version 与 test-data 声明"
  key_links:
    - from: "src-tauri/src/bin/generate_v001_fixture.rs"
      to: "tests/fixtures/databases/v001/state.sqlite3"
      via: "仅目标不存在且 initialize 明确时创建"
      pattern: "initialize"
---

<objective>
创建 append-only migration registry 与确定性、create-once 的 v001 历史数据库 fixture。

Purpose: 先生成并锁定真实历史样本，再让矩阵消费它，修复 create-before-verify。
Output: registry、generator、fixture test、v001 DB 与 manifest。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0006-sqlite-application-state.md
@.planning/phases/01-trusted-local-state-contract/01-20-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-PATTERNS.md
</context>

## Artifacts this phase produces

- `src-tauri/src/state/migrations/mod.rs`：连续版本、名称、checksum、schema fingerprint 的 append-only registry
- `src-tauri/src/state/mod.rs`：StateStore 对 migration registry 的 wiring
- `src-tauri/src/bin/generate_v001_fixture.rs`：确定性 create-once fixture generator
- `src-tauri/tests/fixture_generation.rs`：确定性、no-overwrite 与 fixed-data tests
- `tests/fixtures/databases/v001/state.sqlite3`：永久 schema version 1 SQLite fixture
- `tests/fixtures/databases/manifest.json`：fixture path、digest、application ID、user_version 与 test-data 声明

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 生成确定性 create-once v001 fixture</name>
  <files>src-tauri/src/state/migrations/mod.rs, src-tauri/src/bin/generate_v001_fixture.rs, src-tauri/tests/fixture_generation.rs, tests/fixtures/databases/v001/state.sqlite3, tests/fixtures/databases/manifest.json</files>
  <behavior>
    - 两个 TempDir 生成结果的 logical schema/data digest 一致。
    - 已存在 DB 或 manifest 时默认失败。
    - committed fixture 只包含固定假凭据与固定 UTC 时间。
  </behavior>
  <action>先创建 registry、generator 与测试，再在 action 中仅对缺失目标执行一次 initialize，生成 v001 DB 与 manifest；generator 固定 SQLite pragmas、时间、UUID 与假数据。测试后续只生成到 TempDir并比较，永不覆盖 committed fixture。</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test fixture_generation</automated></verify>
  <done>migration_matrix 尚未运行前，v001 fixture/manifest 已实际存在且通过 generator tests。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把 StateStore 接到 append-only registry</name>
  <files>src-tauri/src/state/mod.rs</files>
  <action>移除 tracer 中分散的 migration metadata，统一从 migrations::MIGRATIONS 读取 version/name/sql/checksum/fingerprint；验证连续版本与唯一名称。现有 tracer/full-state tests 必须继续通过。</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test state_command_restart --test state_persistence --test fixture_generation</automated></verify>
  <done>生产 StateStore 与 fixture generator 使用同一 registry。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration source → committed binary fixture | 生成器和 registry 决定永久历史数据库身份。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-48 | Tampering | historical fixture | high | mitigate | create-once/no-overwrite、fixed generator 与 manifest digest。 |
| T-01-49 | Information Disclosure | fixture data | medium | mitigate | 仅固定假凭据并在 manifest 声明。 |
</threat_model>

<verification>
- fixture_generation 与既有 tracer/full-state tests 返回 0。
</verification>

<success_criteria>
- 所有消费 fixture 的后续任务拥有已创建输入。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-21-SUMMARY.md`
</output>
