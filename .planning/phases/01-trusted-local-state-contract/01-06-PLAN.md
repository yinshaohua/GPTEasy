---
phase: 01-trusted-local-state-contract
plan: 06
type: execute
wave: 3
depends_on: [01-05]
files_modified:
  []
autonomous: false
requirements:
  - STATE-02
  - STATE-03
  - STATE-04
  - STATE-05
must_haves:
  truths:
    - "当前 gh 满足固定最低版本、登录与 repo/actions/attestation read 权限。"
    - "任何 preflight 非零都阻断 01-13 之后的真实 evidence。"
  artifacts:
    []
  key_links:
    - from: "01-06 preflight approval"
      to: "01-13/01-14 workflows 与 01-15/01-16 evidence"
      via: "这些计划显式 depends_on 01-06"
      pattern: "preflight-gh-evidence"
---

<objective>
在首次真实 artifact verification 前完成阻断式 gh 环境确认。

Purpose: 把安装、登录和仓库权限缺失变成显式执行前置条件。
Output: gh preflight 的人工确认记录。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trusted-local-state-contract/01-05-SUMMARY.md
</context>

## Artifacts this phase produces

- 本计划不创建或修改仓库文件；只记录门禁结论。

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: 确认 gh 安装、认证与仓库权限</name>
  <precondition>scripts/contracts/test-gh-preflight.ps1 返回 0。</precondition>
  <action>运行真实只读 preflight。若 gh 缺失或版本过低，先安装或升级到至少 2.49.0；若未登录或权限不足，由用户完成 GitHub CLI 登录或授权后重试。不得在回复或 SUMMARY 中记录 token。</action>
  <what-built>01-05 已实现固定版本、认证、repo/actions/attestation 权限检查及脱敏负例。</what-built>
  <how-to-verify>
    1. 运行真实 preflight。
    2. 确认 version/auth/repository/actions/attestations 五项均 pass。
    3. 确认输出不含 token 或 Authorization。
  </how-to-verify>
  <verify><automated>powershell -NoProfile -File scripts/contracts/preflight-gh-evidence.ps1 -Repository yinshaohua/GPTEasy -MinimumVersion 2.49.0</automated><human-check>当前 gh 身份被允许读取仓库、Actions 与 attestations。</human-check></verify>
  <done>输入 approved 后才允许真实 evidence 下载与验签。</done>
  <resume-signal>输入 approved，或描述非敏感阻断原因。</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 人工授权 → 自动 evidence | 用户认证决定自动脚本能否访问可信远程事实。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Elevation of Privilege | repository permissions | high | mitigate | 只要求 read 权限并由脚本逐项验证。 |
| T-01-13 | Repudiation | preflight approval | low | accept | SUMMARY 记录版本与 pass 项，不记录凭据。 |
</threat_model>

<verification>
- 真实 preflight 返回 0。
</verification>

<success_criteria>
- artifact verification 不再依赖隐含 gh 环境。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-06-SUMMARY.md`
</output>
