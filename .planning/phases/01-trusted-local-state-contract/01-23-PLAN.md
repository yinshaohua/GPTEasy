---
phase: 01-trusted-local-state-contract
plan: 23
type: execute
wave: 14
depends_on: [01-22]
files_modified:
  - src-tauri/src/state/validation.rs
  - src-tauri/src/state/preflight.rs
  - src-tauri/src/state/mod.rs
  - src-tauri/tests/db_contract_validation.rs
  - src-tauri/tests/ready_preflight.rs
autonomous: true
requirements:
  - STATE-03
  - STATE-04
  - STATE-05
must_haves:
  truths:
    - "所有检查在 SQLITE_OPEN_READ_ONLY connection 完成。"
    - "validator 检查 application_id、user_version、quick_check、foreign_key_check、ledger/checksum、schema fingerprint、database identity 与可选 snapshot identity。"
    - "任何失败都进入 recovery-only，验证前后 DB/WAL/SHM/backup/quarantine 不变。"
  artifacts:
    - path: "src-tauri/src/state/validation.rs"
      provides: "统一只读 DB contract validator"
    - path: "src-tauri/src/state/preflight.rs"
      provides: "任何 RW open 前的 PreflightDecision"
    - path: "src-tauri/src/state/mod.rs"
      provides: "StateStore Ready/Migrate 对 validator 的 wiring"
    - path: "src-tauri/tests/db_contract_validation.rs"
      provides: "截断、ledger/FK/schema/identity 负例与 non-mutation"
    - path: "src-tauri/tests/ready_preflight.rs"
      provides: "Ready/Migrate/higher schema 分支 gate tests"
  key_links:
    - from: "src-tauri/src/state/preflight.rs"
      to: "src-tauri/src/state/validation.rs"
      via: "existing DB 第一连接只读运行完整合同"
      pattern: "validate_database_contract"
---

<objective>
实现 Ready、Migrate source、backup 与 recovery candidate 共用的只读数据库合同 validator。

Purpose: 阻止仅凭 application_id/user_version 打开损坏、伪造或漂移数据库。
Output: validator、preflight wiring 与 non-mutating 负例。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0006-sqlite-application-state.md
@.planning/phases/01-trusted-local-state-contract/01-22-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
</context>

## Artifacts this phase produces

- `src-tauri/src/state/validation.rs`：统一只读 DB contract validator
- `src-tauri/src/state/preflight.rs`：任何 RW open 前的 PreflightDecision
- `src-tauri/src/state/mod.rs`：StateStore Ready/Migrate 对 validator 的 wiring
- `src-tauri/tests/db_contract_validation.rs`：截断、ledger/FK/schema/identity 负例与 non-mutation
- `src-tauri/tests/ready_preflight.rs`：Ready/Migrate/higher schema 分支 gate tests

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义统一只读 DB contract validator</name>
  <files>src-tauri/src/state/validation.rs, src-tauri/tests/db_contract_validation.rs</files>
  <behavior>
    - 任一 integrity/ledger/schema/identity 不一致返回类型化错误。
    - ExpectedSnapshotIdentity 同时比较 sidecar 与实际 file digest。
    - 验证前后所有相关文件存在性、hash、长度、mtime 不变。
  </behavior>
  <action>按已知 schema version 查找 compiled migration rows 与 schema fingerprint；执行 quick_check、foreign_key_check、ledger version/name/checksum、state_metadata database UUID/fingerprint。测试构造截断、伪造 ledger、FK 损坏、schema SQL 与 user_version 不一致、identity 缺失和 snapshot 替换。</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test db_contract_validation</automated></verify>
  <done>全部 DB contract 负例 fail-closed 且不修改文件。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把完整 validator 接入 Ready/Migrate preflight</name>
  <files>src-tauri/src/state/preflight.rs, src-tauri/src/state/mod.rs, src-tauri/tests/ready_preflight.rs</files>
  <behavior>
    - existing DB 第一连接只读。
    - 只有完整合同通过才取得 StateCoordinator 后进入 RW/WAL。
    - failure/higher schema 返回 recovery-only，普通写 command 不可用。
  </behavior>
  <action>PreflightDecision 区分 Create、Ready、Migrate、RecoveryOnly；Ready/Migrate source 均先 consume validator，再取得 coordinator并打开 RW。兼容前不得设置 journal_mode、写 ledger 或创建 backup。</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test ready_preflight --test db_contract_validation</automated></verify>
  <done>Ready/Migrate 的 RW 入口由统一完整合同保护。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 现有 SQLite 文件 → RW connection | 损坏或未来数据库若进入写连接会造成不可逆篡改。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-52 | Tampering | forged ledger/schema | critical | mitigate | compiled checksum/fingerprint 与 read-only validator。 |
| T-01-53 | Denial of Service | truncated DB | high | mitigate | 类型化 recovery-only，不 reset 或 RW。 |
</threat_model>

<verification>
- db_contract_validation 与 ready_preflight 返回 0。
</verification>

<success_criteria>
- 所有 DB 消费路径共享同一只读合同。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-23-SUMMARY.md`
</output>
