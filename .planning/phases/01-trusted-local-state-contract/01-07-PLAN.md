---
phase: 01-trusted-local-state-contract
plan: 07
type: execute
wave: 2
depends_on: [01-03]
files_modified:
  - scripts/contracts/audit-phase1-plan-source.ps1
  - scripts/contracts/test-phase1-plan-source-audit.ps1
  - tests/fixtures/contracts/phase1-plan-audit-lock.json
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04
  - STATE-05
must_haves:
  truths:
    - "审计器交叉检查 requirement mapping、计划/任务/路径、runner CLI、依赖波次、同波文件冲突与 artifacts 等式。"
    - "审计只读取规划/合同文件，不修改 PLAN、ROADMAP、PATTERNS、VALIDATION 或 SOURCE-AUDIT。"
    - "digest lock 漂移、缺计划、静态假 COVERED、通配关键路径或未声明 runner 调用均失败。"
  artifacts:
    - path: "scripts/contracts/audit-phase1-plan-source.ps1"
      provides: "只读多来源、拓扑、接口、路径与 digest 审计"
    - path: "scripts/contracts/test-phase1-plan-source-audit.ps1"
      provides: "缺映射、错命令、错波次、路径漂移与静态文本负例"
    - path: "tests/fixtures/contracts/phase1-plan-audit-lock.json"
      provides: "ROADMAP/REQUIREMENTS/PATTERNS/VALIDATION/SOURCE-AUDIT/PLAN/runner matrix 当前 SHA-256"
  key_links:
    - from: "scripts/contracts/run-phase1-contracts.ps1"
      to: "scripts/contracts/audit-phase1-plan-source.ps1"
      via: "PhaseComplete 必须执行只读审计并要求 digest lock 一致"
      pattern: "PhaseComplete"
---

<objective>
建立只读、机器可执行的 Phase 1 计划/来源/接口审计。

Purpose: 最终门禁不再依赖 SOURCE-AUDIT 中的静态 COVERED 文本。
Output: 审计脚本、负例自测与当前规划 artifact digest lock。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-trusted-local-state-contract/01-PATTERNS.md
@.planning/phases/01-trusted-local-state-contract/01-VALIDATION.md
@.planning/phases/01-trusted-local-state-contract/01-SOURCE-AUDIT.md
</context>

## Artifacts this phase produces

- `scripts/contracts/audit-phase1-plan-source.ps1`：只读多来源、拓扑、接口、路径与 digest 审计
- `scripts/contracts/test-phase1-plan-source-audit.ps1`：缺映射、错命令、错波次、路径漂移与静态文本负例
- `tests/fixtures/contracts/phase1-plan-audit-lock.json`：ROADMAP/REQUIREMENTS/PATTERNS/VALIDATION/SOURCE-AUDIT/PLAN/runner matrix 当前 SHA-256

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现只读多来源与计划拓扑审计</name>
  <files>scripts/contracts/audit-phase1-plan-source.ps1, tests/fixtures/contracts/phase1-plan-audit-lock.json</files>
  <behavior>
    - STATE-01..05 每项至少一个计划且不得出现 Phase 2–8 requirement ID。
    - 每个 files_modified 路径为具体路径，Artifacts 集合与 frontmatter 完全相等，task files 为其子集。
    - depends_on 存在且 wave 严格晚于依赖，同 wave 无文件重叠。
  </behavior>
  <action>实现 read-only parser，读取 ROADMAP、REQUIREMENTS、全部 PLAN、PATTERNS、VALIDATION、SOURCE-AUDIT 与 runner-cli-matrix；检查 28 个顺序计划、requirement/goal/research/context 映射、任务标签、runner invocation、源路径引用、关键链接、ASVS high/critical disposition、无关键 glob、无旧命令参数。生成 lock 时排除 lock 自身；普通运行只重算并比较，不写任何文件。SOURCE-AUDIT 的静态状态不能直接作为 pass，必须验证映射和当前 digest。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/audit-phase1-plan-source.ps1 -PhaseDir .planning/phases/01-trusted-local-state-contract -ReadOnly</automated></verify>
  <done>当前规划拓扑与来源映射具有机器可重复摘要。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 证明静态文本和漂移不能关闭审计</name>
  <files>scripts/contracts/test-phase1-plan-source-audit.ps1</files>
  <behavior>
    - 在临时副本中删除 requirement、改 runner 参数、制造同波冲突、替换 digest 分别失败。
    - 仅把 SOURCE-AUDIT 写成 COVERED 仍失败。
    - 生产规划文件在自测前后 hash 不变。
  </behavior>
  <action>把审计输入复制到临时目录，构造每类负例并调用生产审计器；测试前后比较真实规划文件 hash，证明 verifier 不修改源文件。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/test-phase1-plan-source-audit.ps1</automated></verify>
  <done>最终 gate 依赖实时解析与 digest，不依赖声明文本。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 规划文档 → 最终 gate | 静态文档、命令和路径映射决定执行覆盖是否真实。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-14 | Tampering | plan/source mapping | high | mitigate | 实时 parser、digest lock 与负例副本。 |
| T-01-15 | Repudiation | static COVERED claim | medium | mitigate | 忽略声明结果，只接受机器重算。 |
</threat_model>

<verification>
- 只读 audit 与负例自测返回 0。
</verification>

<success_criteria>
- 最终 PhaseComplete 拥有机器可执行来源审计依赖。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-07-SUMMARY.md`
</output>
