---
phase: 01-trusted-local-state-contract
plan: 18
type: execute
wave: 10
depends_on: [01-17]
files_modified:
  - src-tauri/src/domain/mod.rs
  - src-tauri/src/state/mod.rs
  - src-tauri/src/state/repositories.rs
  - src-tauri/src/commands.rs
  - src-tauri/tests/state_persistence.rs
autonomous: true
requirements: [STATE-01, STATE-02]
must_haves:
  truths:
    - "两个供应商、明文假 Key、验证记录、native+WSL 环境、不同当前供应商和完整设置在新进程重开后逐字段一致。"
    - "供应商和受管环境使用不可变 UUID；显示名、地址、Key、模型或 WSL 用户名都不是身份。"
    - "公开 bootstrap DTO、Debug、错误与测试输出不包含 API Key。"
  artifacts:
    - path: "src-tauri/src/domain/mod.rs"
      provides: "provider/verification/environment/settings 领域类型与不变量"
    - path: "src-tauri/src/state/mod.rs"
      provides: "完整 StateStore 事务与内部 snapshot"
    - path: "src-tauri/src/state/repositories.rs"
      provides: "参数化 typed repositories"
    - path: "src-tauri/src/commands.rs"
      provides: "完整状态写入与脱敏 bootstrap commands"
    - path: "src-tauri/tests/state_persistence.rs"
      provides: "两个 OS 进程的完整状态 round-trip 深比较"
  key_links:
    - from: "src-tauri/src/commands.rs"
      to: "src-tauri/src/state/repositories.rs"
      via: "typed DTO 进入 Rust repository，公开 snapshot 只返回脱敏投影"
      pattern: "StateSnapshot"
  assumptions:
    - "FLAGGED_UNVERIFIED [STATE-01]: specless edge probe 为 unclassified；本计划用包含 native+WSL 和不同 current provider 的深比较作为显式 backstop。"
    - "FLAGGED_UNVERIFIED [STATE-02]: specless edge probe 为 unclassified；明文 Key 只在 SQLite/internal model，公开投影与输出使用 canary backstop。"
  prohibitions:
    - "FLAGGED_UNVERIFIED: 不得照搬 Spike 012 的无 Key schema；ADR-0001/0006 要求 Key 明文持久化。"
    - "FLAGGED_UNVERIFIED: 不得用显示名、地址、模型或 WSL username 作为供应商或环境主键。"
---

<objective>
把 tracer 扩展为完整供应商目录、验证状态、受管环境当前供应商与应用设置的权威持久状态。

Purpose: 完整交付 STATE-01 的全字段跨进程重开语义，同时守住 STATE-02 的公开脱敏边界。
Output: 领域类型、typed repositories、commands 与完整两进程深比较测试。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONTEXT.md
@docs/adr/0001-plaintext-provider-credentials.md
@docs/adr/0005-immutable-provider-identity.md
@docs/adr/0006-sqlite-application-state.md
@.planning/phases/01-trusted-local-state-contract/01-17-SUMMARY.md
</context>

## Artifacts this phase produces

- `src-tauri/src/domain/mod.rs`：provider/verification/environment/settings 领域类型与不变量
- `src-tauri/src/state/mod.rs`：完整 StateStore 事务与内部 snapshot
- `src-tauri/src/state/repositories.rs`：参数化 typed repositories
- `src-tauri/src/commands.rs`：完整状态写入与脱敏 bootstrap commands
- `src-tauri/tests/state_persistence.rs`：两个 OS 进程的完整状态 round-trip 深比较

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展完整权威状态与跨进程深比较</name>
  <read_first>
    @src-tauri/src/state/mod.rs
    @src-tauri/src/state/migrations/0001_initial.sql
    @src-tauri/src/commands.rs
    @src-tauri/tests/state_command_restart.rs
    @CONTEXT.md
    @docs/adr/0001-plaintext-provider-credentials.md
    @docs/adr/0005-immutable-provider-identity.md
  </read_first>
  <files>src-tauri/src/domain/mod.rs, src-tauri/src/state/mod.rs, src-tauri/src/state/repositories.rs, src-tauri/src/commands.rs, src-tauri/tests/state_persistence.rs</files>
  <behavior>
    - provider UUID、API Key、verification、environment current provider 与 settings 重开后一致。
    - native 与 WSL 受管环境独立关联不同 provider。
    - public bootstrap snapshot、Debug、panic 与错误不泄漏 Key canary。
  </behavior>
  <action>先扩写 state_persistence 红测，再实现领域不变量、内部 secret-bearing model 和参数化 repositories。测试写入两个固定 UUID 供应商及不同假 Key、组合指纹/验证时间、一个 native 与一个 WSL 受管环境、不同 current provider、locale/theme/login/notice/onboarding/update-check 设置；write 子进程提交后退出，read 子进程重建 app 并通过 commands 深比较内部测试摘要。公开 DTO 只返回 identity/status/计数与不可逆摘要；Key 不能进入 Serialize、Debug、panic、error 或 stdout/stderr。</action>
  <acceptance_criteria>完整状态的每个稳定字段均跨进程保持，两个环境关联独立，公开输出 canary 扫描为零。</acceptance_criteria>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test state_persistence</automated></verify>
  <done>供应商目录、验证状态、各受管环境当前供应商和设置均从 SQLite 权威恢复。</done>
</task>

</tasks>

<threat_model>
Security level: ASVS L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 领域 DTO → SQLite repositories | 明文凭据与关联状态进入本地权威数据库。 |
| SQLite internal model → public bootstrap DTO | secret-bearing 内部数据必须被脱敏投影隔离。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-43 | Information Disclosure | API Key | high | mitigate | 仅 internal model/SQLite，DTO/Debug/error/output canary tests。 |
| T-01-44 | Tampering | environment/provider relation | high | mitigate | immutable UUID、UNIQUE platform identity 与 FK。 |
</threat_model>

<verification>
- state_persistence 返回 0。
- canary 未出现在测试捕获的公开 JSON、Debug 与错误文本。
</verification>

<success_criteria>
- STATE-01 完整 round-trip 自动通过。
- STATE-02 的明文保存与公开脱敏边界同时成立。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-18-SUMMARY.md`
</output>
