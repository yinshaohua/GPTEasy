---
phase: 01-trusted-local-state-contract
plan: 18
type: execute
wave: 9
depends_on: [01-17]
files_modified:
  - src-tauri/src/state/mod.rs
  - src-tauri/src/state/migrations/0001_initial.sql
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
  - src-tauri/tests/state_command_restart.rs
autonomous: true
requirements:
  - STATE-01
  - STATE-02
must_haves:
  truths:
    - "两个 OS 进程都通过 Tauri mock IPC 按 command 名调用已注册 handler，不直接调用 StateStore。"
    - "第一个进程写固定非敏感设置后退出；第二个新进程 bootstrap 读回。"
    - "生产应用始终使用 app_local_data_dir，测试路径只存在 integration-test 子进程。"
  artifacts:
    - path: "src-tauri/src/state/mod.rs"
      provides: "初始 StateStore、schema metadata 与 settings repository"
    - path: "src-tauri/src/state/migrations/0001_initial.sql"
      provides: "批准后的永久 schema version 1"
    - path: "src-tauri/src/commands.rs"
      provides: "update_app_settings 与 bootstrap_state commands"
    - path: "src-tauri/src/lib.rs"
      provides: "生产 invoke handler 与状态初始化 wiring"
    - path: "src-tauri/tests/state_command_restart.rs"
      provides: "同一 test executable 的两子进程 Tauri IPC tracer"
  key_links:
    - from: "src-tauri/tests/state_command_restart.rs"
      to: "src-tauri/src/commands.rs"
      via: "子进程构造 mock app 并按 command 名 invoke"
      pattern: "update_app_settings"
---

<objective>
交付 Phase 1 第一个生产级 tracer：测试进程经已注册 Tauri command 写 SQLite，退出后由新进程 bootstrap 读回。

Purpose: 在最佳上下文先证明 command wiring、永久 schema 与磁盘重开架构。
Output: 0001 schema、StateStore、窄 commands、composition wiring 与两进程集成测试。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0002-tauri-rust-react.md
@docs/adr/0006-sqlite-application-state.md
@.planning/phases/01-trusted-local-state-contract/01-17-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-PATTERNS.md
</context>

## Artifacts this phase produces

- `src-tauri/src/state/mod.rs`：初始 StateStore、schema metadata 与 settings repository
- `src-tauri/src/state/migrations/0001_initial.sql`：批准后的永久 schema version 1
- `src-tauri/src/commands.rs`：update_app_settings 与 bootstrap_state commands
- `src-tauri/src/lib.rs`：生产 invoke handler 与状态初始化 wiring
- `src-tauri/tests/state_command_restart.rs`：同一 test executable 的两子进程 Tauri IPC tracer

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 贯通 Tauri command 的两进程设置重开路径</name>
  <precondition>01-17-SUMMARY.md 同时记录 freeze-approved、approve-schema-version-1 与 approve-db-backup-contract。</precondition>
  <files>src-tauri/src/state/mod.rs, src-tauri/src/state/migrations/0001_initial.sql, src-tauri/src/commands.rs, src-tauri/src/lib.rs, src-tauri/tests/state_command_restart.rs</files>
  <behavior>
    - write 子进程经 invoke handler 写 theme=dark 与固定 run ID。
    - read 子进程重新构建应用并经 bootstrap_state 读回。
    - 集成测试源码不导入或直接调用 StateStore open/write/read。
  </behavior>
  <action>先写失败的 integration test；测试主进程用 current_exe 启动两个带精确 test filter 的子进程，测试专用 root 只通过测试进程环境消费。按批准合同创建六张 STRICT 表、application_id、user_version、database UUID、schema fingerprint 与 migration ledger；生产 build_app 注册两个窄 command。commands 使用 typed DTO、prepared SQL 与公开脱敏错误。禁止 test root 进入 production CLI 或 release package。</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test state_command_restart -- --nocapture</automated></verify>
  <done>真实 command、SQLite 与 OS 进程边界形成可运行生产 tracer。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri command → SQLite | WebView 意图跨入 Rust 权威状态和永久磁盘 schema。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-40 | Tampering | command input | high | mitigate | typed DTO、DB CHECK/FK 与 prepared SQL。 |
| T-01-41 | Elevation of Privilege | test root | high | mitigate | 仅 integration-test 子进程消费，生产 CLI 无路径参数。 |
| T-01-42 | Tampering | permanent schema | high | mitigate | one-way approval、application ID、ledger/checksum/fingerprint。 |
</threat_model>

<verification>
- state_command_restart 返回 0。
- 测试源码静态检查无 StateStore 直接调用。
</verification>

<success_criteria>
- 真正的生产 tracer 已端到端贯通并成为后续扩展骨架。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-18-SUMMARY.md`
</output>
