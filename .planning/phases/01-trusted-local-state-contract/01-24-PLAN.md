---
phase: 01-trusted-local-state-contract
plan: 24
type: execute
wave: 15
depends_on: [01-17, 01-20, 01-23]
files_modified:
  - src-tauri/src/state/backup.rs
  - src-tauri/src/state/migrations/mod.rs
  - src-tauri/src/state/mod.rs
  - src-tauri/tests/migration_failure.rs
  - src-tauri/tests/backup_restore.rs
  - src-tauri/tests/state_concurrency.rs
autonomous: true
requirements:
  - STATE-03
  - STATE-04
must_haves:
  truths:
    - "迁移前使用 SQLite Online Backup API 创建 snapshot，并由统一 validator 校验 identity。"
    - "只保留最近三份 verified backups；同一未变 source/target 的重复失败复用。"
    - "全部 pending migrations 在一个 BEGIN IMMEDIATE 中执行；并发进程不能交错 migrate/backup。"
  artifacts:
    - path: "src-tauri/src/state/backup.rs"
      provides: "BackupIdentity、Online Backup、reuse 与三份 retention"
    - path: "src-tauri/src/state/migrations/mod.rs"
      provides: "全 pending 单一 transaction 与 policy-constrained transforms"
    - path: "src-tauri/src/state/mod.rs"
      provides: "coordinator→backup→migration 固定 wiring"
    - path: "src-tauri/tests/migration_failure.rs"
      provides: "最后一步故障与全版本 rollback"
    - path: "src-tauri/tests/backup_restore.rs"
      provides: "snapshot identity、retention、reuse、replacement rejection"
    - path: "src-tauri/tests/state_concurrency.rs"
      provides: "两个进程同时 migrate/backup 的序列化与 non-mutation"
  key_links:
    - from: "src-tauri/src/state/backup.rs"
      to: "src-tauri/src/state/validation.rs"
      via: "snapshot 完成后按 ExpectedSnapshotIdentity 只读验证"
      pattern: "ExpectedSnapshotIdentity"
---

<objective>
实现批准后的 verified SQLite backup、三份 retention、全 pending 单事务 rollback 与并发 migrate/backup 协调。

Purpose: 交付 STATE-04 并覆盖两个进程同时 open/migrate/backup。
Output: BackupManager、migration transaction、故障测试与并发扩展。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0006-sqlite-application-state.md
@.planning/phases/01-trusted-local-state-contract/01-17-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-23-SUMMARY.md
</context>

## Artifacts this phase produces

- `src-tauri/src/state/backup.rs`：BackupIdentity、Online Backup、reuse 与三份 retention
- `src-tauri/src/state/migrations/mod.rs`：全 pending 单一 transaction 与 policy-constrained transforms
- `src-tauri/src/state/mod.rs`：coordinator→backup→migration 固定 wiring
- `src-tauri/tests/migration_failure.rs`：最后一步故障与全版本 rollback
- `src-tauri/tests/backup_restore.rs`：snapshot identity、retention、reuse、replacement rejection
- `src-tauri/tests/state_concurrency.rs`：两个进程同时 migrate/backup 的序列化与 non-mutation

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 verified BackupIdentity、reuse 与三份 retention</name>
  <precondition>01-17-SUMMARY.md 记录 approve-db-backup-contract。</precondition>
  <files>src-tauri/src/state/backup.rs, src-tauri/tests/backup_restore.rs</files>
  <behavior>
    - Online Backup snapshot 独立只读 open 并通过完整 validator。
    - 只有 verified backup 参与排序/裁剪，排序不依赖 mtime。
    - 同 source database UUID/snapshot digest/from-to/fingerprint 可复用。
  </behavior>
  <action>使用 rusqlite Online Backup 写临时 snapshot，关闭连接后验证并原子发布 sidecar/DB。测试覆盖第四份裁剪、重复失败复用、sidecar/文件替换、digest/identity 错配和未验证文件不参与 retention。</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test backup_restore</automated></verify>
  <done>backup 可打开、可验身份、只留三份且不被重复失败挤占。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现全 pending rollback 与并发 migrate/backup 协调</name>
  <files>src-tauri/src/state/migrations/mod.rs, src-tauri/src/state/mod.rs, src-tauri/tests/migration_failure.rs, src-tauri/tests/state_concurrency.rs</files>
  <behavior>
    - validate→coordinator→backup→BEGIN IMMEDIATE 顺序固定。
    - 最后 migration 故障后 schema/rows/ledger/metadata 全部保持起始版本。
    - 一个进程暂停在 migrate 或 backup 时，第二进程 open/migrate/backup 返回 StateBusy 且不生成额外 backup。
  </behavior>
  <action>持有 StateCoordinator 覆盖 backup 与整个 migration；事务内重读 user_version，执行所有 pending SQL/constrained transforms、ledger/fingerprint/user_version、FK/quick_check，最后 commit。扩展两进程测试覆盖 concurrent open/migrate/backup，验证 release 后第二进程可正常打开 current schema。</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test migration_failure --test backup_restore --test state_concurrency</automated></verify>
  <done>迁移失败完整回滚，并发 migrate/backup 不交错。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 旧数据库 → backup/migration | 迁移、备份和并发进程共同影响唯一用户数据库。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-54 | Tampering | backup replacement | high | mitigate | snapshot digest、sidecar identity 与统一 validator。 |
| T-01-55 | Tampering | migration transaction | critical | mitigate | 单一 BEGIN IMMEDIATE、policy lint 与最后一步故障测试。 |
| T-01-56 | Denial of Service | concurrent migrate/backup | high | mitigate | StateCoordinator 与两进程故障矩阵。 |
</threat_model>

<verification>
- backup_restore、migration_failure、state_concurrency 返回 0。
</verification>

<success_criteria>
- STATE-04 的 snapshot/retention/reuse/rollback/concurrency 全部自动证明。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-24-SUMMARY.md`
</output>
