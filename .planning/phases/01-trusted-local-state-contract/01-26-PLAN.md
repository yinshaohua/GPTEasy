---
phase: 01-trusted-local-state-contract
plan: 26
type: execute
wave: 17
depends_on: [01-13, 01-25]
files_modified:
  - .github/workflows/phase1-windows-evidence.yml
  - tests/fixtures/contracts/packaging/windows-x64/manifest.json
  - tests/fixtures/contracts/packaging/windows-arm64/manifest.json
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04
  - STATE-05
user_setup:
  - service: GitHub Actions Windows 原生签名 runners
    why: 重跑 Windows x64/ARM64 最终 installed full-state/recovery matrix
    dashboard_config:
      - task: 保持 ephemeral runner 与 Authenticode secrets 可用
        location: GitHub repository Actions settings
must_haves:
  truths:
    - "两个 Windows job 使用不同 state run ID 与 recovery run ID。"
    - "seed→新进程 verify→prepare higher→新进程 refuse→restore→新进程 verify 的顺序完整执行。"
    - "只有统一 finally 执行 recovery cleanup、state cleanup、app cleanup、账户/profile cleanup，且 cleanup 结果进入 attestation。"
  artifacts:
    - path: ".github/workflows/phase1-windows-evidence.yml"
      provides: "Windows 最终 full-state/recovery/lifecycle workflow wiring"
    - path: "tests/fixtures/contracts/packaging/windows-x64/manifest.json"
      provides: "Windows x64 最终 full-state/recovery evidence"
    - path: "tests/fixtures/contracts/packaging/windows-arm64/manifest.json"
      provides: "Windows ARM64 最终 full-state/recovery evidence"
  key_links:
    - from: ".github/workflows/phase1-windows-evidence.yml"
      to: "src-tauri/src/lib.rs"
      via: "installed app 调用 phase1-state-smoke 与 phase1-recovery-smoke 固定 modes"
      pattern: "recovery_run_id"
---

<objective>
把 full-state 与 recovery smoke 接入 Windows x64/ARM64 installed workflow，并刷新最终 attested packaging evidence。

Purpose: 证明签名安装版真实执行完整状态、higher-schema、backup/quarantine restore 与统一 cleanup。
Output: 更新的 Windows workflow 与两份最终 packaging manifests。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trusted-local-state-contract/01-13-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-25-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-VALIDATION.md
</context>

## Artifacts this phase produces

- `.github/workflows/phase1-windows-evidence.yml`：Windows 最终 full-state/recovery/lifecycle workflow wiring
- `tests/fixtures/contracts/packaging/windows-x64/manifest.json`：Windows x64 最终 full-state/recovery evidence
- `tests/fixtures/contracts/packaging/windows-arm64/manifest.json`：Windows ARM64 最终 full-state/recovery evidence

<tasks>

<task type="auto">
  <name>Task 1: 接线 Windows installed full-state/recovery 与统一 finally</name>
  <files>.github/workflows/phase1-windows-evidence.yml</files>
  <action>在 x64/ARM64 jobs 中为同一 job 生成不同 opaque state/recovery run ID；签名安装后依次用独立进程执行 state seed、state verify、recovery prepare、recovery refuse、recovery restore、recovery verify。任何验证前不得调用 cleanup。统一 finally 固定顺序为 recovery cleanup、state cleanup、停止/卸载 app、删除 per-job OS 用户/profile 或 baseline restore，随后才写 cleanup_attested evidence 并上传/attest。workflow 继续运行 host-vs-CLI canary parity 与 WSL contract。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/test-windows-package-verifier.ps1 -CaseSet Workflow; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope PackagingSelfTest -Target Local -Mode Strict</automated></verify>
  <done>workflow 静态/fixture predicate 证明 modes、run IDs 与 cleanup 顺序完整。</done>
</task>

<task type="auto">
  <name>Task 2: 重跑双架构并刷新最终 Windows packaging evidence</name>
  <precondition>Windows x64/ARM64 ephemeral runners、签名资源与 01-06 gh preflight 仍可用。</precondition>
  <files>tests/fixtures/contracts/packaging/windows-x64/manifest.json, tests/fixtures/contracts/packaging/windows-arm64/manifest.json</files>
  <action>调度同一 commit 的两个 native jobs，等待 unified finally 成功后下载并验证 artifact/attestation；仅导入 full_state_restart_round_trip、higher_schema_non_mutating_refusal、compatible_backup_restore、newer_database_quarantined、distinct_run_ids、cleanup_attested、provenance_verified 全部为 true 的 manifests。verify 阶段不写 manifests。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope PackagingSelfTest -Target WindowsX64 -Mode Strict; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope PackagingSelfTest -Target WindowsArm64 -Mode Strict</automated></verify>
  <done>Windows 双架构最终 signed installed evidence 与实际完整测试范围一致。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installed Windows app → run-scoped DB/recovery/profile | 最终签名应用连续执行状态与恢复场景，cleanup 次序决定是否破坏证据或用户状态。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-61 | Tampering | premature cleanup | critical | mitigate | verify/restore 分离与 unified finally ordering contract。 |
| T-01-62 | Spoofing | final Windows manifests | high | mitigate | native signed jobs、实时 attestation 与实际 predicate 字段。 |
</threat_model>

<verification>
- PackagingSelfTest WindowsX64/WindowsArm64 Strict 返回 0。
</verification>

<success_criteria>
- Windows 最终 evidence 覆盖完整状态和恢复且不提前清理。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-26-SUMMARY.md`
</output>
