---
phase: 01-trusted-local-state-contract
plan: 26
type: execute
wave: 18
depends_on: [01-25]
files_modified:
  - tests/fixtures/contracts/codex/windows-x64/manifest.json
  - tests/fixtures/contracts/host/windows-x64/manifest.json
  - tests/fixtures/contracts/wsl2/windows-x64/manifest.json
  - tests/fixtures/contracts/packaging/windows-x64/manifest.json
  - tests/fixtures/contracts/codex/windows-arm64/manifest.json
  - tests/fixtures/contracts/host/windows-arm64/manifest.json
  - tests/fixtures/contracts/wsl2/windows-arm64/manifest.json
  - tests/fixtures/contracts/packaging/windows-arm64/manifest.json
autonomous: false
requirements: [STATE-01, STATE-02, STATE-03, STATE-04, STATE-05]
user_setup:
  - service: GitHub Actions Windows 原生签名环境
    why: 生成真实 Windows x64/ARM64 Authenticode installed evidence
    env_vars:
      - name: WINDOWS_AUTHENTICODE_PFX_BASE64
        source: 正式发布证书的离线保管位置；配置为 GitHub Actions secret
      - name: WINDOWS_AUTHENTICODE_PFX_PASSWORD
        source: 正式发布证书密码的离线保管位置；配置为 GitHub Actions secret
    dashboard_config:
      - task: 提供原生 x64/ARM64 runner、exact Codex 0.146.1、allowlisted 正式宿主和代表性 WSL2
        location: GitHub repository Actions runner/settings
must_haves:
  truths:
    - "Windows x64 与 ARM64 分别产生同 commit、可取回、可验签且 Authenticode Valid 的 native evidence。"
    - "两架构均执行 CLI/正式宿主 parity、WSL passive probe、完整状态重开、higher refusal、backup/quarantine restore 与 lifecycle cleanup。"
    - "没有正式 PFX 时计划在 precondition 停止；绝不创建自签名证书、占位 secret 或 fake pass manifest。"
  artifacts:
    - path: "tests/fixtures/contracts/codex/windows-x64/manifest.json"
      provides: "Windows x64 Codex 0.146.1 contract evidence"
    - path: "tests/fixtures/contracts/host/windows-x64/manifest.json"
      provides: "Windows x64 正式宿主 parity evidence"
    - path: "tests/fixtures/contracts/wsl2/windows-x64/manifest.json"
      provides: "Windows x64 WSL2 passive evidence"
    - path: "tests/fixtures/contracts/packaging/windows-x64/manifest.json"
      provides: "Windows x64 signed full-state/recovery package evidence"
    - path: "tests/fixtures/contracts/codex/windows-arm64/manifest.json"
      provides: "Windows ARM64 Codex 0.146.1 contract evidence"
    - path: "tests/fixtures/contracts/host/windows-arm64/manifest.json"
      provides: "Windows ARM64 正式宿主 parity evidence"
    - path: "tests/fixtures/contracts/wsl2/windows-arm64/manifest.json"
      provides: "Windows ARM64 WSL2 passive evidence"
    - path: "tests/fixtures/contracts/packaging/windows-arm64/manifest.json"
      provides: "Windows ARM64 signed full-state/recovery package evidence"
  key_links:
    - from: ".github/workflows/phase1-windows-evidence.yml"
      to: "scripts/contracts/verify-evidence-provenance.ps1"
      via: "按数值 run/job/artifact ID 重新下载、attestation 验签并导入固定目标 manifests"
      pattern: "provenance_verified"
  assumptions:
    - "FLAGGED_UNVERIFIED [STATE-01]: specless edge probe 为 unclassified；两个目标的 installed full-state restart assertions 为平台 backstop。"
    - "FLAGGED_UNVERIFIED [STATE-02]: specless edge probe 为 unclassified；一次性当前用户 profile 与 canary-clean evidence 为平台 backstop。"
    - "FLAGGED_UNVERIFIED [STATE-03]: specless edge probe 为 unclassified；signed installed app 的 current-schema open/migration assertions 为平台 backstop。"
    - "FLAGGED_UNVERIFIED [STATE-04]: specless edge probe 为 unclassified；双架构 compatible backup/rollback assertions 为平台 backstop。"
    - "FLAGGED_UNVERIFIED [STATE-05]: specless edge probe 为 unclassified；双架构 non-mutating refusal/quarantine restore assertions 为平台 backstop。"
  prohibitions:
    - "FLAGGED_UNVERIFIED: 不得生成或使用自签名证书、占位 PFX、伪 attestation、unsigned/错架构工件或 strict fixture。"
---

<objective>
获取 Windows x64/ARM64 真实 Authenticode installed contract、状态与恢复证据。

Purpose: 在所有非签名功能完成后，用正式平台身份和原生架构证明 Windows 最终行为。
Output: 八份由当前 commit 的真实工件、run/job/artifact 与 attestation 支持的 manifests。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trusted-local-state-contract/01-06-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-13-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-25-SUMMARY.md
@.codex/skills/spike-findings-gpteasy/references/install-and-update.md
</context>

## Artifacts this phase produces

- `tests/fixtures/contracts/codex/windows-x64/manifest.json`：Windows x64 Codex contract evidence
- `tests/fixtures/contracts/host/windows-x64/manifest.json`：Windows x64 正式宿主 parity evidence
- `tests/fixtures/contracts/wsl2/windows-x64/manifest.json`：Windows x64 WSL2 passive evidence
- `tests/fixtures/contracts/packaging/windows-x64/manifest.json`：Windows x64 signed full-state/recovery evidence
- `tests/fixtures/contracts/codex/windows-arm64/manifest.json`：Windows ARM64 Codex contract evidence
- `tests/fixtures/contracts/host/windows-arm64/manifest.json`：Windows ARM64 正式宿主 parity evidence
- `tests/fixtures/contracts/wsl2/windows-arm64/manifest.json`：Windows ARM64 WSL2 passive evidence
- `tests/fixtures/contracts/packaging/windows-arm64/manifest.json`：Windows ARM64 signed full-state/recovery evidence

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: 核验 Windows 正式凭据与原生 runner 前置条件</name>
  <precondition>01-25 的 Windows workflow 接线合同已通过，且尚未调度任何远程 evidence job。</precondition>
  <action>只核验 GitHub Actions secret 名称、正式 Authenticode PFX 的离线可用性、原生 x64/ARM64 runner 标签（windows-2025、windows-11-arm）及 allowlisted 正式宿主/Codex 0.146.1/代表性 WSL2 的可用性。不得读取、打印或记录 secret 值，不得创建自签名、占位或 synthetic 材料；任一前置条件缺失时停止并保持后续证据任务 fail-closed。</action>
  <what-built>01-25 已完成 Windows full-state/recovery/lifecycle workflow 的本地接线，正式签名运行仍等待真实环境。</what-built>
  <instructions>由凭据保管人确认正式 PFX 可用，由仓库管理员确认两个 Actions secret 名称与 windows-2025/windows-11-arm 原生 runner 可用，由证据负责人确认 allowlisted 正式宿主、Codex 0.146.1 与代表性 WSL2 可调度；全程不得展示或复制凭据值。</instructions>
  <how-to-verify>
    1. 使用只读 GitHub CLI 查询 Actions secrets，确认两个名称均存在。
    2. 由凭据保管人确认正式 PFX 与密码可用于 Authenticode，且不向本地输出值。
    3. 确认 windows-2025 与 windows-11-arm 原生 runner、正式宿主、Codex 0.146.1 和代表性 WSL2 可调度。
  </how-to-verify>
  <verify><automated>$required = @('WINDOWS_AUTHENTICODE_PFX_BASE64','WINDOWS_AUTHENTICODE_PFX_PASSWORD'); $configured = @(gh secret list --repo yinshaohua/GPTEasy --app actions --json name --jq '.[].name'); if ($LASTEXITCODE -ne 0) { exit 3 }; $missing = @($required | Where-Object { $_ -notin $configured }); if ($missing.Count -gt 0) { Write-Error ('缺少 Actions secret 名称: ' + ($missing -join ', ')); exit 3 }</automated><human-check>确认正式 PFX、原生双架构 runner 与 allowlisted 正式宿主均可用；不提供或打印凭据值。</human-check></verify>
  <verification>两个 secret 名称存在，凭据保管人确认正式 PFX 可用，原生双架构 runner 与正式宿主/Codex/WSL2 前置条件均被确认；没有任何凭据值、占位、自签名或 synthetic 材料进入输出。</verification>
  <done>仅在 secret 名称、正式凭据可用性、原生 runner 与正式宿主前置条件全部确认后，才允许远程调度 Windows evidence。</done>
  <resume-signal>输入 windows-evidence-prerequisites-ready，或描述任一缺失前置条件。</resume-signal>
</task>

<task type="auto">
  <name>Task 2: 获取并导入 Windows x64 正式证据</name>
  <read_first>
    @.github/workflows/phase1-windows-evidence.yml
    @scripts/contracts/preflight-gh-evidence.ps1
    @scripts/contracts/verify-evidence-provenance.ps1
    @scripts/contracts/verify-windows-package.ps1
    @.planning/phases/01-trusted-local-state-contract/01-25-SUMMARY.md
  </read_first>
  <precondition>Task 1 已返回 windows-evidence-prerequisites-ready；正式 Windows Authenticode PFX 已存在于离线保管位置，两个 secret 名称已配置，原生 x64 runner/正式宿主/Codex 0.146.1/代表性 WSL2 均可用。</precondition>
  <files>tests/fixtures/contracts/codex/windows-x64/manifest.json, tests/fixtures/contracts/host/windows-x64/manifest.json, tests/fixtures/contracts/wsl2/windows-x64/manifest.json, tests/fixtures/contracts/packaging/windows-x64/manifest.json</files>
  <action>只读核验 secret 名称存在后，以 gh 调度当前 main commit 的 WindowsX64 job。等待 job 与 unified finally 全绿，按数值 run/job/artifact ID 下载到新临时目录；重跑 gh preflight，对 evidence bundle 与实际 installer 分别执行 attestation verify，重算 commit/digest/PE arch。仅在 Authenticode Valid、current-user、host parity、WSL passive、full-state、migration/backup、higher refusal、quarantine restore、distinct IDs 和 cleanup_attested 全部为 true 时导入四份 manifest；失败/blocked 不写任何 pass 文件。</action>
  <acceptance_criteria>四份 x64 manifest 由同一真实 run、job、commit、artifact 与签名 attestation 支持，且不含 secret/raw config/DB。</acceptance_criteria>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope ContractSelfTest -Target WindowsX64 -Mode Strict; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope PackagingSelfTest -Target WindowsX64 -Mode Strict</automated></verify>
  <done>Windows x64 正式签名 installed 全范围证据 strict pass。</done>
</task>

<task type="auto">
  <name>Task 3: 获取并导入 Windows ARM64 正式证据</name>
  <read_first>
    @.github/workflows/phase1-windows-evidence.yml
    @scripts/contracts/verify-evidence-provenance.ps1
    @scripts/contracts/verify-windows-package.ps1
    @.planning/phases/01-trusted-local-state-contract/01-25-SUMMARY.md
  </read_first>
  <precondition>Task 1 已返回 windows-evidence-prerequisites-ready；同一正式 PFX secrets 可用，且原生 Windows ARM64 runner、ARM64 正式宿主、Codex 0.146.1 与代表性 WSL2 可用。</precondition>
  <files>tests/fixtures/contracts/codex/windows-arm64/manifest.json, tests/fixtures/contracts/host/windows-arm64/manifest.json, tests/fixtures/contracts/wsl2/windows-arm64/manifest.json, tests/fixtures/contracts/packaging/windows-arm64/manifest.json</files>
  <action>独立调度 WindowsArm64 job并执行与 x64 相同的重新取回、验签、digest、canary、状态/恢复和 lifecycle gates。要求 runner/PE/installed process 均为 ARM64；禁止交叉构建、自报架构或复用 x64 evidence。所有 strict assertions 全绿后才写固定四份 manifests。</action>
  <acceptance_criteria>ARM64 evidence 独立、原生、正式签名、可取回且覆盖完整状态/恢复；没有跨架构替代。</acceptance_criteria>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope ContractSelfTest -Target WindowsArm64 -Mode Strict; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope PackagingSelfTest -Target WindowsArm64 -Mode Strict</automated></verify>
  <done>Windows ARM64 正式签名 installed 全范围证据 strict pass。</done>
</task>

</tasks>

<threat_model>
Security level: ASVS L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Windows signing secrets/runner → signed installer | 外部凭据和原生 runner 决定真实平台身份。 |
| Actions artifacts → committed manifests | 远程结果只有独立取回/验签后可进入最终门。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-66 | Spoofing | Windows architecture/signature | critical | mitigate | 原生 runner、PE arch、Authenticode 与 attestation 交叉验证。 |
| T-01-67 | Tampering | evidence import | high | mitigate | 新临时下载、run/job/commit/digest/lifecycle 全门禁。 |
| T-01-68 | Information Disclosure | PFX/profile evidence | critical | mitigate | secrets 只在 CI secret store；allowlist output/canary scan。 |
</threat_model>

<verification>
- WindowsX64/WindowsArm64 的 ContractSelfTest 与 PackagingSelfTest Strict 均返回 0。
</verification>

<success_criteria>
- Windows 双架构正式 Authenticode evidence 真实覆盖 STATE-01..05。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-26-SUMMARY.md`
</output>
