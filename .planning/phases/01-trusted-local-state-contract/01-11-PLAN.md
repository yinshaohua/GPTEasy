---
phase: 01-trusted-local-state-contract
plan: 11
type: execute
wave: 3
depends_on: [01-04]
files_modified:
  - scripts/contracts/probe-codex.ps1
  - scripts/contracts/probe-windows-host.ps1
  - scripts/contracts/probe-wsl2.ps1
  - scripts/contracts/test-windows-contract-probes.ps1
  - scripts/contracts/test-wsl2-probe.ps1
autonomous: true
requirements:
  - STATE-02
  - STATE-03
  - STATE-05
must_haves:
  truths:
    - "Windows official CLI 与正式宿主 bundled Codex 在同一一次性用户默认配置根读取同一 canary。"
    - "两者的 config root、origin/provider digest、credential carrier 与 shared user layer 必须一致。"
    - "WSL2 被动探针不进入发行版，重复显示名固定 command_target_resolvable=false。"
  artifacts:
    - path: "scripts/contracts/probe-codex.ps1"
      provides: "Windows Codex 0.146.1 app-server/config-read probe"
    - path: "scripts/contracts/probe-windows-host.ps1"
      provides: "正式宿主 bundled Codex 身份与 host-vs-CLI parity probe"
    - path: "scripts/contracts/probe-wsl2.ps1"
      provides: "无副作用 WSL2 host contract probe"
    - path: "scripts/contracts/test-windows-contract-probes.ps1"
      provides: "CLI/host canary 一致与错配负例"
    - path: "scripts/contracts/test-wsl2-probe.ps1"
      provides: "运行集合、重名和禁止进入发行版负例"
  key_links:
    - from: "scripts/contracts/probe-windows-host.ps1"
      to: "scripts/contracts/probe-codex.ps1"
      via: "同一 disposable-user canary 的 app-server initialize/initialized/config-read 摘要比较"
      pattern: "shared_user_layer"
---

<objective>
实现 Windows Codex/正式宿主共享配置 canary parity 与无副作用 WSL2 contract probes。

Purpose: 冻结 Windows x64/ARM64 外部配置事实，并让 host 与 CLI 不一致直接阻断。
Output: 三个生产 probe 与两份 fixture self-test。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
@.codex/skills/spike-findings-gpteasy/references/codex-provider-compatibility.md
@.codex/skills/spike-findings-gpteasy/references/wsl-linux-environments.md
</context>

## Artifacts this phase produces

- `scripts/contracts/probe-codex.ps1`：Windows Codex 0.146.1 app-server/config-read probe
- `scripts/contracts/probe-windows-host.ps1`：正式宿主 bundled Codex 身份与 host-vs-CLI parity probe
- `scripts/contracts/probe-wsl2.ps1`：无副作用 WSL2 host contract probe
- `scripts/contracts/test-windows-contract-probes.ps1`：CLI/host canary 一致与错配负例
- `scripts/contracts/test-wsl2-probe.ps1`：运行集合、重名和禁止进入发行版负例

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 Windows official CLI 与正式宿主 canary parity</name>
  <files>scripts/contracts/probe-codex.ps1, scripts/contracts/probe-windows-host.ps1, scripts/contracts/test-windows-contract-probes.ps1</files>
  <behavior>
    - 精确 0.146.1、binary/schema digest 与 app-server 三步协议必须通过。
    - CLI 与 bundled Codex 从同一默认用户层读到相同固定 canary。
    - config root、origin/provider digest、credential carrier 或 shared-user-layer 任一不一致即失败。
  </behavior>
  <action>在调用方提供的一次性 OS 用户中写入固定非敏感 user-layer canary，分别启动官方 CLI 与允许清单定位的正式宿主 bundled Codex app-server；执行 initialize、initialized、config/read(includeLayers=true)，只输出版本、二进制/schema 摘要、配置根类别、provider/model/origin 摘要、env_key/direct bearer/missing carrier 布尔值。禁止保存配置正文、raw response 或完整命令行。自测通过注入 fixture 覆盖 host 缺失、版本错配、不同根、origin/provider digest 与 credential carrier 错配。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/test-windows-contract-probes.ps1</automated></verify>
  <done>Windows host-vs-CLI parity 是可执行生产 predicate。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现无副作用 WSL2 host contract</name>
  <files>scripts/contracts/probe-wsl2.ps1, scripts/contracts/test-wsl2-probe.ps1</files>
  <behavior>
    - running_before 与 running_after 集合相同。
    - 进入发行版命令计数恒为 0。
    - 重复 DistributionName 不能解析 command target。
  </behavior>
  <action>只调用 wsl version/list/list-running 与只读 HKCU Lxss，按 UTF-16 读取；输出 registration GUID、display name、DefaultUid、运行集合与 command_target_resolvable。任何 -d/--distribution guest command 都由测试检测并拒绝。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope ContractSelfTest -Target Wsl2 -Mode Strict</automated></verify>
  <done>WSL2 probe 不启动发行版且重名 fail-closed。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Windows 用户配置/宿主/WSL → evidence | 外部二进制和系统枚举可能泄漏配置或错误归因。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-22 | Spoofing | official CLI/bundled host | high | mitigate | 精确版本、binary digest、正式 package identity 与 canary parity。 |
| T-01-23 | Information Disclosure | config-read evidence | high | mitigate | allowlist 摘要，禁止 raw config/response/command line。 |
| T-01-24 | Tampering | WSL probe | high | mitigate | 只读命令、前后运行集合与 guest-command 计数。 |
</threat_model>

<verification>
- Windows fixture parity 与 WSL2 self-test 均返回 0。
</verification>

<success_criteria>
- Windows 两架构工作流可运行同一 host-vs-CLI predicate。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-11-SUMMARY.md`
</output>
