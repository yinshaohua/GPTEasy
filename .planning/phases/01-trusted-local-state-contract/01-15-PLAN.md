---
phase: 01-trusted-local-state-contract
plan: 15
type: execute
wave: 7
depends_on: [01-07, 01-10, 01-11, 01-12, 01-13, 01-14]
files_modified:
  - tests/fixtures/contracts/runner-cli-matrix.json
  - scripts/contracts/run-phase1-contracts.ps1
  - scripts/contracts/test-run-phase1-cli.ps1
  - tests/fixtures/contracts/phase1-plan-audit-lock.json
autonomous: true
requirements: [STATE-01, STATE-02, STATE-03, STATE-04, STATE-05]
must_haves:
  truths:
    - "Freeze Local Strict 只冻结本地路径、进程、Codex/宿主/WSL 探针与安装工作流合同，不要求平台签名凭据或签名工件。"
    - "Freeze 输出明确列出 Windows Authenticode、macOS Developer ID/公证和四目标正式工件仍为 deferred，并且 release_ready=false。"
    - "PhaseComplete 继续要求四目标真实签名/公证、attestation、架构、当前用户安装、full-state/recovery 与 lifecycle 证据；unsigned、fixture 或占位证据不能通过。"
  artifacts:
    - path: "tests/fixtures/contracts/runner-cli-matrix.json"
      provides: "非签名 Freeze 与 fail-closed PhaseComplete 的唯一 dispatch 合同"
    - path: "scripts/contracts/run-phase1-contracts.ps1"
      provides: "可执行非签名合同冻结及最终发布证据隔离"
    - path: "scripts/contracts/test-run-phase1-cli.ps1"
      provides: "Freeze/PhaseComplete 正反例与所有计划消费者扫描"
    - path: "tests/fixtures/contracts/phase1-plan-audit-lock.json"
      provides: "修订后 28 份计划及来源输入的当前摘要锁"
  key_links:
    - from: "scripts/contracts/run-phase1-contracts.ps1"
      to: "tests/fixtures/contracts/runner-cli-matrix.json"
      via: "Freeze 与 PhaseComplete 均由 matrix dispatch，且使用不同 prerequisite 集合"
      pattern: "freeze-local|phase-complete-local"
  assumptions:
    - "FLAGGED_UNVERIFIED [STATE-01]: specless edge probe 为 unclassified；完整跨进程状态边界分配给 01-17、01-18、01-19、01-25、01-26、01-27、01-28。"
    - "FLAGGED_UNVERIFIED [STATE-02]: specless edge probe 为 unclassified；当前用户本地与无默认上传边界分配给 01-18、01-19、01-25、01-28。"
    - "FLAGGED_UNVERIFIED [STATE-03]: specless edge probe 为 unclassified；永久迁移与历史样本边界分配给 01-20、01-21、01-22、01-28。"
    - "FLAGGED_UNVERIFIED [STATE-04]: specless edge probe 为 unclassified；备份、retention 与全 pending rollback 分配给 01-22、01-23、01-24、01-28。"
    - "FLAGGED_UNVERIFIED [STATE-05]: specless edge probe 为 unclassified；higher-schema 拒写、quarantine 与恢复分配给 01-22、01-24、01-28。"
    - "FLAGGED_UNVERIFIED [assumption-delta]: detector=false；供应商目录和受管环境在来源中已为复数且仍为必需/用户选择，不触发 singular→plural、required→optional 或 derived→chosen。"
  prohibitions:
    - "FLAGGED_UNVERIFIED: 不得创建占位 secret、自签名证书、伪 attestation、伪 package evidence，或把 test-only fixture 当成正式平台证据。"
    - "FLAGGED_UNVERIFIED: 不得把 unsigned、未公证或非目标架构工件解释为 PhaseComplete 的正式发布证明。"
---

<objective>
建立诚实的非签名合同冻结门，让 SQLite schema/backup 单向决策不再被尚未具备的发布签名凭据阻塞。

Purpose: 使用 01-01～01-14 已执行的 Codex/host/WSL/path/process/install-workflow 合同证据推进本地状态实现，同时把平台签名与公证严格留到 01-26～01-28。
Output: 分离后的 Freeze/PhaseComplete runner 合同、正反例与更新后的只读摘要锁。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trusted-local-state-contract/COVERAGE.md
@.planning/phases/01-trusted-local-state-contract/01-07-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-10-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-11-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-12-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-13-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-14-SUMMARY.md
</context>

## Artifacts this phase produces

- `tests/fixtures/contracts/runner-cli-matrix.json`：非签名 Freeze 与 fail-closed PhaseComplete 的唯一 dispatch 合同
- `scripts/contracts/run-phase1-contracts.ps1`：可执行非签名合同冻结及最终发布证据隔离
- `scripts/contracts/test-run-phase1-cli.ps1`：Freeze/PhaseComplete 正反例与所有计划消费者扫描
- `tests/fixtures/contracts/phase1-plan-audit-lock.json`：修订后 28 份计划及来源输入的当前摘要锁

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 将 Freeze 收紧为诚实的非签名合同门</name>
  <read_first>
    @tests/fixtures/contracts/runner-cli-matrix.json
    @scripts/contracts/run-phase1-contracts.ps1
    @scripts/contracts/test-run-phase1-cli.ps1
    @.planning/phases/01-trusted-local-state-contract/COVERAGE.md
    @.planning/phases/01-trusted-local-state-contract/01-10-SUMMARY.md
    @.planning/phases/01-trusted-local-state-contract/01-11-SUMMARY.md
    @.planning/phases/01-trusted-local-state-contract/01-12-SUMMARY.md
    @.planning/phases/01-trusted-local-state-contract/01-13-SUMMARY.md
    @.planning/phases/01-trusted-local-state-contract/01-14-SUMMARY.md
  </read_first>
  <files>tests/fixtures/contracts/runner-cli-matrix.json, scripts/contracts/run-phase1-contracts.ps1, scripts/contracts/test-run-phase1-cli.ps1</files>
  <behavior>
    - Freeze Local Strict 在正式签名 secrets/manifests 均不存在时仍可运行，但必须执行 RunnerSelfTest、ProvenanceSelfTest、path_smoke、Windows/WSL probes、macOS Wave 0/workflow contract 与 package predicate 正反例。
    - Freeze 输出 freeze_kind=non_signing_contract、release_ready=false、deferred_evidence 包含 Windows x64/ARM64 Authenticode 与 macOS Intel/Apple Silicon Developer ID/notary。
    - 任一非签名检查失败时 Freeze 非零；签名缺失不能被标成通过，只能出现在 deferred_evidence。
    - PhaseComplete 必须执行本地状态/迁移/备份/恢复套件及四目标 ContractSelfTest/PackagingSelfTest Strict，不能因 Freeze 通过而放行。
  </behavior>
  <action>重构 matrix 的 freeze-local 与 phase-complete-local dispatch。Freeze 复用已经存在的生产 verifier/self-test 和 01-10 真实 path smoke，不读取或制造正式平台 manifest；结果新增机器可读的非签名冻结类别、release_ready=false 与精确 deferred 列表。PhaseComplete 单独聚合 01-25 后存在的本地 Cargo/迁移/恢复检查以及 01-26/01-27 的四目标真实 strict predicates。扩展测试覆盖：无签名凭据时 Freeze 正控制、缺任一非签名合同失败、把 fixture/unsigned 标为正式证据失败、Freeze 结果不能喂给 PhaseComplete、PhaseComplete 在正式证据缺失时返回 3。API coverage detector 继续读取既有 COVERAGE.md 的 no-external-API 结论，不创建外部 API surface。不得创建或读取任何秘密值。</action>
  <acceptance_criteria>Freeze 可以仅凭现有非签名合同证据严格通过且诚实记录 deferred；PhaseComplete 仍在任何正式平台证据缺失时 fail-closed。</acceptance_criteria>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope RunnerSelfTest -Target Local -Mode Strict; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/test-run-phase1-cli.ps1 -ScanPlans .planning/phases/01-trusted-local-state-contract; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope Freeze -Target Local -Mode Strict</automated></verify>
  <done>非签名 Freeze 与最终 PhaseComplete 使用互不混淆的证据集合，签名延期不会阻塞 01-16，但也不会削弱 01-28。</done>
</task>

<task type="auto">
  <name>Task 2: 刷新修订后计划摘要锁并证明审计只读</name>
  <read_first>
    @scripts/contracts/audit-phase1-plan-source.ps1
    @scripts/contracts/test-phase1-plan-source-audit.ps1
    @tests/fixtures/contracts/phase1-plan-audit-lock.json
    @.planning/phases/01-trusted-local-state-contract/01-SOURCE-AUDIT.md
  </read_first>
  <files>tests/fixtures/contracts/phase1-plan-audit-lock.json</files>
  <action>在 01-15～01-28 的修订计划已经位于磁盘后，先运行 source-audit 负例自测，再用 audit-phase1-plan-source.ps1 的显式 -UpdateLock 路径只更新摘要锁；随后以 -ReadOnly 重跑，确认 28 个计划、需求、依赖波次、task tags、runner invocation、PATTERNS 每个 concrete path 的精确所有权、VALIDATION 每个计划的职责与自动命令、SOURCE-AUDIT 的 requirement/capability 计划映射以及当前 digests 一致。不得改写 ROADMAP、REQUIREMENTS、PATTERNS、VALIDATION、SOURCE-AUDIT 或任何 PLAN.md。</action>
  <acceptance_criteria>摘要锁只反映本次已审阅的计划修订，普通 Freeze/PhaseComplete 运行保持只读。</acceptance_criteria>
  <verify><automated>powershell -NoProfile -File scripts/contracts/test-phase1-plan-source-audit.ps1; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/audit-phase1-plan-source.ps1 -PhaseDir .planning/phases/01-trusted-local-state-contract -ReadOnly</automated></verify>
  <done>修订后来源与拓扑可机器重算，且正常门禁不写规划文件。</done>
</task>

</tasks>

<threat_model>
Security level: ASVS L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 已执行合同证据 → 单向设计门 | 非签名证据只足以批准本地磁盘合同，不能冒充发布身份。 |
| 最终平台证据 → PhaseComplete | 真实签名、架构、安装与 provenance 才能决定阶段完成。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-34 | Spoofing | Freeze result | high | mitigate | 机器可读 freeze_kind/release_ready/deferred_evidence 与正反测试。 |
| T-01-35 | Elevation of Privilege | PhaseComplete promotion | critical | mitigate | PhaseComplete 独立执行四目标 Strict predicates，拒绝 Freeze/fixture/unsigned 提权。 |
| T-01-36 | Tampering | plan audit lock | high | mitigate | 显式 UpdateLock、只读重算与临时副本负例。 |
</threat_model>

<verification>
- RunnerSelfTest、plan consumer scan 与 Freeze Local Strict 返回 0。
- PhaseComplete 在四目标正式证据尚缺时稳定返回 3，而不是 0。
- source audit 更新后以 ReadOnly 返回 0。
</verification>

<success_criteria>
- schema/backup 决策拥有足够、明确且不依赖签名的合同证据。
- 正式签名/公证没有被伪造、替代或提前宣称完成。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-15-SUMMARY.md`
</output>
