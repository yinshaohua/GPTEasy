---
phase: 01-trusted-local-state-contract
plan: 15
type: execute
wave: 7
depends_on: [01-06, 01-13]
files_modified:
  - tests/fixtures/contracts/codex/windows-x64/manifest.json
  - tests/fixtures/contracts/host/windows-x64/manifest.json
  - tests/fixtures/contracts/wsl2/windows-x64/manifest.json
  - tests/fixtures/contracts/packaging/windows-x64/manifest.json
  - tests/fixtures/contracts/codex/windows-arm64/manifest.json
  - tests/fixtures/contracts/host/windows-arm64/manifest.json
  - tests/fixtures/contracts/wsl2/windows-arm64/manifest.json
  - tests/fixtures/contracts/packaging/windows-arm64/manifest.json
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04
  - STATE-05
user_setup:
  - service: GitHub Actions Windows 原生签名 runners
    why: 提供 Windows x64/ARM64、正式 Authenticode、Codex/宿主与代表性 WSL2
    dashboard_config:
      - task: 配置 ephemeral runner labels 与 signing secrets
        location: GitHub repository Actions settings；秘密不得写入仓库或对话
must_haves:
  truths:
    - "Windows x64 与 ARM64 分别有 native、signed、同 commit、可取回且 attested 的 evidence。"
    - "两架构均证明 official CLI 与正式宿主 bundled Codex 共享一次性用户配置 canary。"
    - "runner/job/SID/profile lifecycle cleanup 通过，marker-only 不能通过。"
  artifacts:
    - path: "tests/fixtures/contracts/codex/windows-x64/manifest.json"
      provides: "Windows x64 Codex 0.146.1 contract evidence"
    - path: "tests/fixtures/contracts/host/windows-x64/manifest.json"
      provides: "Windows x64 正式宿主与 CLI parity evidence"
    - path: "tests/fixtures/contracts/wsl2/windows-x64/manifest.json"
      provides: "Windows x64 WSL2 passive contract evidence"
    - path: "tests/fixtures/contracts/packaging/windows-x64/manifest.json"
      provides: "Windows x64 signed current-user package/lifecycle evidence"
    - path: "tests/fixtures/contracts/codex/windows-arm64/manifest.json"
      provides: "Windows ARM64 Codex 0.146.1 contract evidence"
    - path: "tests/fixtures/contracts/host/windows-arm64/manifest.json"
      provides: "Windows ARM64 正式宿主与 CLI parity evidence"
    - path: "tests/fixtures/contracts/wsl2/windows-arm64/manifest.json"
      provides: "Windows ARM64 WSL2 passive contract evidence"
    - path: "tests/fixtures/contracts/packaging/windows-arm64/manifest.json"
      provides: "Windows ARM64 signed current-user package/lifecycle evidence"
  key_links:
    - from: "Windows workflow artifacts"
      to: "scripts/contracts/verify-evidence-provenance.ps1"
      via: "按 run/job/artifact ID 下载、验签并导入对应目标 manifest"
      pattern: "provenance_verified"
---

<objective>
获取 Windows x64 与 ARM64 独立 attested contract/package freeze evidence。

Purpose: 把两架构的 host-vs-CLI parity、WSL、签名和生命周期结论分开验证。
Output: 八份经主工作区重新取回和验签的 Windows manifests。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trusted-local-state-contract/01-06-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-13-SUMMARY.md
</context>

## Artifacts this phase produces

- `tests/fixtures/contracts/codex/windows-x64/manifest.json`：Windows x64 Codex 0.146.1 contract evidence
- `tests/fixtures/contracts/host/windows-x64/manifest.json`：Windows x64 正式宿主与 CLI parity evidence
- `tests/fixtures/contracts/wsl2/windows-x64/manifest.json`：Windows x64 WSL2 passive contract evidence
- `tests/fixtures/contracts/packaging/windows-x64/manifest.json`：Windows x64 signed current-user package/lifecycle evidence
- `tests/fixtures/contracts/codex/windows-arm64/manifest.json`：Windows ARM64 Codex 0.146.1 contract evidence
- `tests/fixtures/contracts/host/windows-arm64/manifest.json`：Windows ARM64 正式宿主与 CLI parity evidence
- `tests/fixtures/contracts/wsl2/windows-arm64/manifest.json`：Windows ARM64 WSL2 passive contract evidence
- `tests/fixtures/contracts/packaging/windows-arm64/manifest.json`：Windows ARM64 signed current-user package/lifecycle evidence

<tasks>

<task type="auto">
  <name>Task 1: 获取并导入 Windows x64 freeze evidence</name>
  <precondition>01-06 gh preflight 已 approved，x64 ephemeral runner、签名身份、正式宿主和代表性 WSL2 可用。</precondition>
  <files>tests/fixtures/contracts/codex/windows-x64/manifest.json, tests/fixtures/contracts/host/windows-x64/manifest.json, tests/fixtures/contracts/wsl2/windows-x64/manifest.json, tests/fixtures/contracts/packaging/windows-x64/manifest.json</files>
  <action>通过 gh 调度 phase1-windows-evidence 的 WindowsX64 job，等待成功后按数值 run/job/artifact ID 下载到临时目录；先运行 gh preflight，再验证 evidence bundle/installer attestation、commit 与 digest。仅把 schema-valid、canary-clean、host-vs-CLI parity=true、lifecycle cleanup_attested=true 的四份 manifest 写入固定路径。资源缺失返回 blocked，不创建 pass fixture。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope ContractSelfTest -Target WindowsX64 -Mode Strict; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope PackagingSelfTest -Target WindowsX64 -Mode Strict</automated></verify>
  <done>Windows x64 contract 与 signed package evidence 均由当前 commit 的 attestation 支持。</done>
</task>

<task type="auto">
  <name>Task 2: 获取并导入 Windows ARM64 freeze evidence</name>
  <precondition>01-06 gh preflight 已 approved，ARM64 ephemeral runner、签名身份、正式宿主和代表性 WSL2 可用。</precondition>
  <files>tests/fixtures/contracts/codex/windows-arm64/manifest.json, tests/fixtures/contracts/host/windows-arm64/manifest.json, tests/fixtures/contracts/wsl2/windows-arm64/manifest.json, tests/fixtures/contracts/packaging/windows-arm64/manifest.json</files>
  <action>按与 x64 相同的独立取回/验签流程调度 WindowsArm64；禁止使用交叉构建、x64 结果或自报架构替代。导入前要求 PE arch、Authenticode、CLI/host canary parity、WSL passive contract 与 account cleanup 全部通过。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope ContractSelfTest -Target WindowsArm64 -Mode Strict; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope PackagingSelfTest -Target WindowsArm64 -Mode Strict</automated></verify>
  <done>Windows ARM64 独立 native evidence 可取回、可验签且无替代证据。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Windows Actions → committed fixtures | 远程签名/宿主/WSL 证据进入冻结输入。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-34 | Spoofing | Windows target architecture | high | mitigate | 每架构独立 native runner、PE 检查与 attestation。 |
| T-01-35 | Tampering | evidence import | high | mitigate | 临时下载、schema/provenance/canary/lifecycle 全门禁。 |
</threat_model>

<verification>
- 两个目标的 ContractSelfTest 与 PackagingSelfTest Strict 均返回 0。
</verification>

<success_criteria>
- Windows freeze evidence 不共享或猜测跨架构事实。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-15-SUMMARY.md`
</output>
