---
phase: 01-trusted-local-state-contract
plan: 19
type: execute
wave: 10
depends_on: [01-18]
files_modified:
  - src-tauri/src/domain/mod.rs
  - src-tauri/src/state/mod.rs
  - src-tauri/src/state/repositories.rs
  - src-tauri/src/commands.rs
  - src-tauri/tests/state_persistence.rs
autonomous: true
requirements:
  - STATE-01
  - STATE-02
must_haves:
  truths:
    - "两个供应商、明文假 Key、验证记录、native+WSL 环境、不同 current provider 与设置在重开后逐字段一致。"
    - "供应商/环境 identity 使用不可变 UUID，显示名不是主键。"
    - "公开 bootstrap DTO 与错误不包含 API Key。"
  artifacts:
    - path: "src-tauri/src/domain/mod.rs"
      provides: "provider/verification/environment/settings 领域类型与不变量"
    - path: "src-tauri/src/state/mod.rs"
      provides: "完整 StateStore 事务与内部 snapshot"
    - path: "src-tauri/src/state/repositories.rs"
      provides: "参数化 typed repositories"
    - path: "src-tauri/src/commands.rs"
      provides: "完整状态写入与脱敏 bootstrap commands"
    - path: "src-tauri/tests/state_persistence.rs"
      provides: "两个 OS 进程的完整状态 round-trip 深比较"
  key_links:
    - from: "src-tauri/src/commands.rs"
      to: "src-tauri/src/state/repositories.rs"
      via: "typed DTO 进入 Rust repository，公开 snapshot 只返回脱敏投影"
      pattern: "StateSnapshot"
---

<objective>
把 tracer 扩展为完整供应商、验证、环境当前供应商与设置的权威持久状态。

Purpose: 完整交付 STATE-01 的全字段重开语义。
Output: 领域类型、typed repositories、commands 与两进程深比较测试。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CONTEXT.md
@docs/adr/0001-plaintext-provider-credentials.md
@docs/adr/0005-immutable-provider-identity.md
@docs/adr/0006-sqlite-application-state.md
@.planning/phases/01-trusted-local-state-contract/01-18-SUMMARY.md
</context>

## Artifacts this phase produces

- `src-tauri/src/domain/mod.rs`：provider/verification/environment/settings 领域类型与不变量
- `src-tauri/src/state/mod.rs`：完整 StateStore 事务与内部 snapshot
- `src-tauri/src/state/repositories.rs`：参数化 typed repositories
- `src-tauri/src/commands.rs`：完整状态写入与脱敏 bootstrap commands
- `src-tauri/tests/state_persistence.rs`：两个 OS 进程的完整状态 round-trip 深比较

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 扩展完整权威状态与跨进程深比较</name>
  <files>src-tauri/src/domain/mod.rs, src-tauri/src/state/mod.rs, src-tauri/src/state/repositories.rs, src-tauri/src/commands.rs, src-tauri/tests/state_persistence.rs</files>
  <behavior>
    - provider UUID、API Key、verification、environment current provider 与 settings 重开后一致。
    - native 与 WSL environment 独立关联不同 provider。
    - public bootstrap snapshot、Debug、panic 与错误不泄漏 Key。
  </behavior>
  <action>先扩写红测，再实现领域约束和参数化 repositories。测试用两个固定 UUID provider/假 Key、验证记录、native/WSL environment、不同 current provider 与 locale/theme 等设置；write 子进程提交后退出，read 子进程重开并通过 commands 深比较内部测试摘要。API Key 只存在 internal model/SQLite，公开 DTO 返回 identity/status/计数与不可逆摘要。</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test state_persistence</automated></verify>
  <done>STATE-01 全字段 round-trip 通过，公开边界脱敏。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 领域 DTO → SQLite repositories | 明文凭据与关联状态进入本地权威数据库。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-43 | Information Disclosure | API Key | high | mitigate | 仅 internal model/SQLite；DTO/error/test output canary。 |
| T-01-44 | Tampering | environment/provider relation | high | mitigate | immutable UUID、UNIQUE identity 与 FK。 |
</threat_model>

<verification>
- state_persistence 返回 0。
</verification>

<success_criteria>
- 供应商目录、验证状态、环境选择和设置重开保持。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-19-SUMMARY.md`
</output>
