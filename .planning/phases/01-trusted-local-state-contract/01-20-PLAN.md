---
phase: 01-trusted-local-state-contract
plan: 20
type: execute
wave: 12
depends_on: [01-19]
files_modified:
  - src-tauri/src/state/migrations/mod.rs
  - src-tauri/src/state/mod.rs
  - src-tauri/src/bin/generate_v001_fixture.rs
  - src-tauri/tests/fixture_generation.rs
  - tests/fixtures/databases/v001/state.sqlite3
  - tests/fixtures/databases/manifest.json
autonomous: true
requirements: [STATE-03]
must_haves:
  truths:
    - "v001 fixture 在任何 migration_matrix 验证前已存在，且 application_id/user_version/schema/data digest 可验证。"
    - "生成器使用固定假数据、固定时间与固定 SQLite 配置，默认拒绝覆盖 committed fixture 或 manifest。"
    - "生产 StateStore 与 fixture generator 使用同一个 append-only migration registry。"
  artifacts:
    - path: "src-tauri/src/state/migrations/mod.rs"
      provides: "连续版本、名称、checksum 与 schema fingerprint 的 append-only registry"
    - path: "src-tauri/src/state/mod.rs"
      provides: "StateStore 对 migration registry 的 wiring"
    - path: "src-tauri/src/bin/generate_v001_fixture.rs"
      provides: "确定性 create-once fixture generator"
    - path: "src-tauri/tests/fixture_generation.rs"
      provides: "确定性、no-overwrite 与 fixed-data tests"
    - path: "tests/fixtures/databases/v001/state.sqlite3"
      provides: "永久 schema version 1 SQLite fixture"
    - path: "tests/fixtures/databases/manifest.json"
      provides: "fixture path、digest、application ID、user_version 与假数据声明"
  key_links:
    - from: "src-tauri/src/bin/generate_v001_fixture.rs"
      to: "src-tauri/src/state/migrations/mod.rs"
      via: "生成器从 MIGRATIONS 初始化正式 v001，而非复制测试 DDL"
      pattern: "MIGRATIONS"
  assumptions:
    - "FLAGGED_UNVERIFIED [STATE-03]: specless edge probe 为 unclassified；确定性 create-once fixture 与单一 registry 是历史升级矩阵的显式 backstop。"
  prohibitions:
    - "FLAGGED_UNVERIFIED: 不得在测试 verify 阶段重生成或覆盖 committed 历史 fixture。"
    - "FLAGGED_UNVERIFIED: 不得编辑已发布 migration 内容；后续版本只能追加。"
---

<objective>
创建 append-only migration registry 与确定性、create-once 的 v001 历史数据库 fixture。

Purpose: 先建立真实历史输入，再让后续迁移矩阵消费，保证 create-before-verify。
Output: registry、fixture generator/tests、v001 DB 与 manifest。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0006-sqlite-application-state.md
@.planning/phases/01-trusted-local-state-contract/01-19-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-PATTERNS.md
</context>

## Artifacts this phase produces

- `src-tauri/src/state/migrations/mod.rs`：连续版本、名称、checksum 与 schema fingerprint 的 append-only registry
- `src-tauri/src/state/mod.rs`：StateStore 对 migration registry 的 wiring
- `src-tauri/src/bin/generate_v001_fixture.rs`：确定性 create-once fixture generator
- `src-tauri/tests/fixture_generation.rs`：确定性、no-overwrite 与 fixed-data tests
- `tests/fixtures/databases/v001/state.sqlite3`：永久 schema version 1 SQLite fixture
- `tests/fixtures/databases/manifest.json`：fixture path、digest、application ID、user_version 与假数据声明

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 生成确定性 create-once v001 fixture</name>
  <read_first>
    @src-tauri/src/state/migrations/0001_initial.sql
    @src-tauri/src/state/mod.rs
    @src-tauri/tests/state_persistence.rs
    @docs/adr/0006-sqlite-application-state.md
  </read_first>
  <reversibility rating="one-way">committed v001 fixture 与 registry checksum 会成为未来所有迁移和 release-tag 漂移检查的永久基线。</reversibility>
  <files>src-tauri/src/state/migrations/mod.rs, src-tauri/src/bin/generate_v001_fixture.rs, src-tauri/tests/fixture_generation.rs, tests/fixtures/databases/v001/state.sqlite3, tests/fixtures/databases/manifest.json</files>
  <behavior>
    - 两个 TempDir 生成结果的 logical schema/data digest 一致。
    - 已存在 DB 或 manifest 时 generator 默认非零且不修改文件。
    - committed fixture 只含固定假凭据、固定 UUID 与固定 UTC 时间。
  </behavior>
  <action>先创建 append-only registry、generator 和红测，再仅在 committed 目标缺失时显式 initialize 一次 v001 DB/manifest。registry 行包含 version/name/sql/checksum/schema fingerprint；generator 固定 page/journal 等影响文件稳定性的 SQLite 设置、时间、UUID 和假数据。manifest 记录实际 file SHA、logical digest、application_id、user_version、schema fingerprint 和 fake-data 声明。测试后续只生成到 TempDir 比较，绝不覆盖仓库 fixture。</action>
  <acceptance_criteria>committed v001 可只读打开且身份/摘要与 manifest 一致；重复生成被拒绝；不同临时目录逻辑结果一致。</acceptance_criteria>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test fixture_generation</automated></verify>
  <done>01-21 的 migration_matrix 在启动前拥有真实、永久、可验证的历史输入。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把 StateStore 接到 append-only registry</name>
  <read_first>
    @src-tauri/src/state/mod.rs
    @src-tauri/src/state/migrations/mod.rs
    @src-tauri/tests/state_command_restart.rs
    @src-tauri/tests/state_persistence.rs
  </read_first>
  <files>src-tauri/src/state/mod.rs</files>
  <action>删除 tracer 中分散的 migration version/name/checksum/fingerprint 常量，统一从 migrations::MIGRATIONS 读取；启动时验证版本从 1 连续、名称唯一、checksum 与内嵌 SQL 一致、current fingerprint 与最后版本一致。保持 command restart/full-state 生产路径与 fixture generator 消费同一 registry。</action>
  <acceptance_criteria>生产 StateStore、0001 与 generator 不再有第二份 migration metadata，现有状态测试无回归。</acceptance_criteria>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test state_command_restart --test state_persistence --test fixture_generation</automated></verify>
  <done>永久迁移身份只有一个事实源。</done>
</task>

</tasks>

<threat_model>
Security level: ASVS L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration source → committed binary fixture | registry 与 generator 决定永久历史数据库身份。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-49 | Tampering | historical fixture | high | mitigate | create-once/no-overwrite、固定 generator 与 manifest digest。 |
| T-01-50 | Information Disclosure | fixture data | medium | mitigate | 只使用固定假 Key，并在 manifest 明确声明。 |
</threat_model>

<verification>
- fixture_generation 与既有 tracer/full-state tests 返回 0。
</verification>

<success_criteria>
- STATE-03 的历史输入先于历史矩阵创建并永久锁定。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-20-SUMMARY.md`
</output>
