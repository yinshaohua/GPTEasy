---
phase: 01-trusted-local-state-contract
plan: 20
type: execute
wave: 11
depends_on: [01-19]
files_modified:
  - src-tauri/src/lib.rs
  - src-tauri/tests/local_only_boundary.rs
  - src-tauri/tests/installed_state_smoke.rs
  - src-tauri/src/state/coordination.rs
  - src-tauri/src/state/mod.rs
  - src-tauri/tests/state_concurrency.rs
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04
  - STATE-05
must_haves:
  truths:
    - "headless state smoke 使用 seed、verify、cleanup 三个独立模式；verify 不删除状态。"
    - "状态与恢复 workflow 使用不同 run ID，最终 cleanup 只在平台 workflow 的统一 finally 执行。"
    - "StateCoordinator 使用 OS 级 File lock；进程崩溃后锁由 OS 释放，metadata 不能自证 disposable。"
  artifacts:
    - path: "src-tauri/src/lib.rs"
      provides: "phase1-state-smoke seed/verify/cleanup 固定入口"
    - path: "src-tauri/tests/local_only_boundary.rs"
      provides: "当前用户路径、依赖/capability/DTO 与无账户/上传边界"
    - path: "src-tauri/tests/installed_state_smoke.rs"
      provides: "seed/verify 无清理与显式 cleanup 两进程测试"
    - path: "src-tauri/src/state/coordination.rs"
      provides: "std File exclusive lock 与 owner metadata 的跨进程协调"
    - path: "src-tauri/src/state/mod.rs"
      provides: "StateStore open 对 coordinator 的 wiring"
    - path: "src-tauri/tests/state_concurrency.rs"
      provides: "并发 open、busy、崩溃释放与 non-mutation 两进程测试"
  key_links:
    - from: "src-tauri/src/state/mod.rs"
      to: "src-tauri/src/state/coordination.rs"
      via: "任何 Create/Ready/Migrate/Backup/Restore 写路径先持有 exclusive File lock"
      pattern: "StateCoordinator"
---

<objective>
提供 truthful headless full-state smoke、local-only 边界与跨进程状态协调合同。

Purpose: 让 installed workflows 可分阶段验证而不提前清理，并防止两个进程并发迁移/恢复。
Output: 状态 smoke modes、local-only tests、OS file lock coordinator 与并发测试。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0007-local-only-product.md
@.planning/phases/01-trusted-local-state-contract/01-19-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
</context>

## Artifacts this phase produces

- `src-tauri/src/lib.rs`：phase1-state-smoke seed/verify/cleanup 固定入口
- `src-tauri/tests/local_only_boundary.rs`：当前用户路径、依赖/capability/DTO 与无账户/上传边界
- `src-tauri/tests/installed_state_smoke.rs`：seed/verify 无清理与显式 cleanup 两进程测试
- `src-tauri/src/state/coordination.rs`：std File exclusive lock 与 owner metadata 的跨进程协调
- `src-tauri/src/state/mod.rs`：StateStore open 对 coordinator 的 wiring
- `src-tauri/tests/state_concurrency.rs`：并发 open、busy、崩溃释放与 non-mutation 两进程测试

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现不提前清理的 full-state installed smoke</name>
  <files>src-tauri/src/lib.rs, src-tauri/tests/local_only_boundary.rs, src-tauri/tests/installed_state_smoke.rs</files>
  <behavior>
    - seed 写固定假全量状态，verify 在新进程深比较但保留状态。
    - cleanup 只删除匹配 run ID 的固定 run-scoped 状态。
    - state smoke 与 recovery smoke 必须使用不同 run ID。
  </behavior>
  <action>增加 phase1-state-smoke seed、verify、cleanup 三个 mode，只接受 opaque run ID。seed/verify 输出 provider/verification/native+WSL/current provider/settings 计数与 expected digest；verify 不删除数据库或 backup。cleanup 校验 run ID 与 state root 后才删除 run-scoped 文件。local-only test 检查 app_local_data_dir、schema/Cargo/package/capability/commands，证明无产品账户、云同步、默认上传和凭据输出。</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test installed_state_smoke --test local_only_boundary</automated></verify>
  <done>full-state smoke 可在恢复场景之前保留数据库，清理由单独模式负责。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立 OS 释放的跨进程 StateCoordinator</name>
  <files>src-tauri/src/state/coordination.rs, src-tauri/src/state/mod.rs, src-tauri/tests/state_concurrency.rs</files>
  <behavior>
    - 第一个进程持有 std::fs::File exclusive lock 时第二个写 open 返回 StateBusy 且不改文件。
    - kill 第一个进程后 OS 释放锁，第二个进程可取得锁并覆盖旧 owner metadata。
    - owner metadata 只含 PID/start token/run ID 摘要，不能独立证明 disposable runner。
  </behavior>
  <action>使用 Rust 1.97 稳定 File::try_lock/lock 维护 app_local_data_dir 下固定 state.lock；取得 OS lock 后才写 owner metadata，Drop/unwind 正常释放，进程终止由 OS 释放。禁止仅按 lock 文件存在判断占用或删除未验证路径。两进程测试覆盖同时 open、busy timeout、kill holder、再次 open 与 DB/WAL/backup 非修改。</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test state_concurrency</automated></verify>
  <done>跨进程协调不是应用 marker 自报，并具备 crash-release 行为。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 多个 GPTEasy 进程 → 单一状态根 | 并发 open/migrate/backup/restore 可能相互破坏数据库与恢复工件。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-45 | Denial of Service | concurrent state access | high | mitigate | OS exclusive File lock、busy timeout 与两进程测试。 |
| T-01-46 | Tampering | smoke cleanup | high | mitigate | opaque run ID、verify 不清理、统一 finally 才 cleanup。 |
| T-01-47 | Spoofing | lock metadata | medium | mitigate | metadata 仅诊断，是否占用由 OS lock 决定。 |
</threat_model>

<verification>
- installed_state_smoke、local_only_boundary、state_concurrency 返回 0。
</verification>

<success_criteria>
- installed state 可供后续 recovery smoke 使用且并发写被序列化。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-20-SUMMARY.md`
</output>
