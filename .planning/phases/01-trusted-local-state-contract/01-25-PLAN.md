---
phase: 01-trusted-local-state-contract
plan: 25
type: execute
wave: 16
depends_on: [01-24]
files_modified:
  - src-tauri/src/state/recovery.rs
  - src-tauri/src/state/preflight.rs
  - src-tauri/src/commands.rs
  - src-tauri/src/lib.rs
  - src-tauri/tests/higher_schema_refusal.rs
  - src-tauri/tests/recovery_validation.rs
  - src-tauri/tests/installed_recovery_smoke.rs
  - src-tauri/tests/state_concurrency.rs
autonomous: true
requirements:
  - STATE-04
  - STATE-05
must_haves:
  truths:
    - "higher schema 与任何 DB contract failure 在 RW 前进入 recovery-only，DB/WAL/SHM/backup/quarantine 不变。"
    - "restore 只接受后端枚举 opaque ID，先生成 verified quarantine，再原子恢复并重跑 preflight。"
    - "recovery smoke 使用独立 run ID，prepare/refuse/restore/verify/cleanup 分离；最终 cleanup 只由 workflow finally 调用。"
  artifacts:
    - path: "src-tauri/src/state/recovery.rs"
      provides: "compatible backup listing、quarantine 与 atomic restore"
    - path: "src-tauri/src/state/preflight.rs"
      provides: "higher/invalid database recovery-only decision"
    - path: "src-tauri/src/commands.rs"
      provides: "list/restore narrow commands 与写 command 拒绝"
    - path: "src-tauri/src/lib.rs"
      provides: "phase1-recovery-smoke prepare/refuse/restore/verify/cleanup wiring"
    - path: "src-tauri/tests/higher_schema_refusal.rs"
      provides: "DB/WAL/SHM/backup/quarantine non-mutation tests"
    - path: "src-tauri/tests/recovery_validation.rs"
      provides: "replaced/truncated/incompatible backup 与 quarantine tests"
    - path: "src-tauri/tests/installed_recovery_smoke.rs"
      provides: "五模式、多进程、独立 run ID 与 cleanup ordering tests"
    - path: "src-tauri/tests/state_concurrency.rs"
      provides: "两个进程同时 restore/migrate/backup 的协调测试"
  key_links:
    - from: "src-tauri/src/state/recovery.rs"
      to: "src-tauri/src/state/validation.rs"
      via: "restore 前后统一 validator；quarantine 先验证再替换"
      pattern: "validate_database_contract"
---

<objective>
实现 higher-schema non-mutating refusal、opaque backup restore、verified quarantine 与 installed headless recovery smoke。

Purpose: 交付 STATE-05，并把 full-state/recovery 真实入口接到最终平台 workflows。
Output: recovery service/commands、拒写/恢复/并发 tests 与固定 recovery smoke modes。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0006-sqlite-application-state.md
@.planning/phases/01-trusted-local-state-contract/01-24-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
</context>

## Artifacts this phase produces

- `src-tauri/src/state/recovery.rs`：compatible backup listing、quarantine 与 atomic restore
- `src-tauri/src/state/preflight.rs`：higher/invalid database recovery-only decision
- `src-tauri/src/commands.rs`：list/restore narrow commands 与写 command 拒绝
- `src-tauri/src/lib.rs`：phase1-recovery-smoke prepare/refuse/restore/verify/cleanup wiring
- `src-tauri/tests/higher_schema_refusal.rs`：DB/WAL/SHM/backup/quarantine non-mutation tests
- `src-tauri/tests/recovery_validation.rs`：replaced/truncated/incompatible backup 与 quarantine tests
- `src-tauri/tests/installed_recovery_smoke.rs`：五模式、多进程、独立 run ID 与 cleanup ordering tests
- `src-tauri/tests/state_concurrency.rs`：两个进程同时 restore/migrate/backup 的协调测试

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 higher-schema 与 contract failure 的 non-mutating recovery-only</name>
  <files>src-tauri/src/state/preflight.rs, src-tauri/src/commands.rs, src-tauri/src/lib.rs, src-tauri/tests/higher_schema_refusal.rs</files>
  <behavior>
    - higher/application/integrity/ledger/fingerprint/identity failure 不进入 RW。
    - 普通写 commands 稳定拒绝。
    - 调用前后 DB/WAL/SHM/backup/quarantine hash、长度、mtime 不变。
  </behavior>
  <action>existing DB 第一连接只读；失败返回类型化 BootstrapState/公开错误，普通写 command 稳定拒绝。本任务只注册 bootstrap 与写拒绝行为；Task 2 同时创建 recovery service 及 list/restore commands，保证每次验证只消费已存在实现。测试记录所有相关文件并证明无写副作用。</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test higher_schema_refusal</automated></verify>
  <done>旧应用遇到未来或损坏 DB 时无任何写副作用。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 opaque restore、verified quarantine 与跨进程协调</name>
  <files>src-tauri/src/state/recovery.rs, src-tauri/src/commands.rs, src-tauri/tests/recovery_validation.rs, src-tauri/tests/state_concurrency.rs</files>
  <behavior>
    - unknown/arbitrary path、replaced/truncated/incompatible backup 被拒绝。
    - quarantine 创建/验证失败时保留原 DB。
    - restore 持有 StateCoordinator；并发 migrate/backup/restore 不能交错。
  </behavior>
  <action>list_compatible_backups 只返回 opaque ID。restore canonicalize backup root、验证 sidecar/file/full DB contract；取得 coordinator 后先用 Online Backup 保存当前 newer DB 为 quarantine并验证，再同目录临时文件原子替换，最后重跑 preflight。扩展两进程矩阵覆盖 restore 与 migrate/backup 竞争。</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test recovery_validation --test state_concurrency</automated></verify>
  <done>只有 verified backup 可恢复，唯一较新副本先被可靠隔离。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 提供固定 installed recovery smoke 与统一 cleanup 顺序</name>
  <files>src-tauri/src/lib.rs, src-tauri/tests/installed_recovery_smoke.rs</files>
  <behavior>
    - prepare 创建 compatible backup 后构造固定 higher-schema DB；refuse 在新进程验证 non-mutation。
    - restore 与 verify 分别由新进程执行，verify 后状态仍存在。
    - cleanup 是唯一删除入口，且 recovery run ID 与 state run ID 不同。
  </behavior>
  <action>增加 phase1-recovery-smoke prepare/refuse/restore/verify/cleanup，只接受 opaque recovery run ID。prepare 使用编译期固定假数据和 approved backup contract；restore 只使用后端枚举 ID。所有模式输出 allowlist 摘要，不在 verify/refuse/restore 中清理；tests 断言平台 workflow 必须在统一 finally 按 recovery cleanup→state cleanup→account/profile cleanup 顺序调用。</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test installed_recovery_smoke --test higher_schema_refusal --test recovery_validation</automated></verify>
  <done>最终平台工作流拥有固定、可组合、不会提前清理的 full-state/recovery 入口。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recovery command → 当前/备份数据库 | 降级恢复会替换唯一用户数据库并处理更高版本副本。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-57 | Tampering | higher-schema open | critical | mitigate | read-only full preflight 与 non-mutation tests。 |
| T-01-58 | Tampering | backup ID/path | high | mitigate | opaque ID、canonical root 与 identity validation。 |
| T-01-59 | Tampering | quarantine/restore | critical | mitigate | verified quarantine before atomic replace；失败保留原 DB。 |
| T-01-60 | Denial of Service | concurrent restore | high | mitigate | StateCoordinator 与两进程 restore matrix。 |
</threat_model>

<verification>
- higher_schema_refusal、recovery_validation、installed_recovery_smoke、state_concurrency 返回 0。
</verification>

<success_criteria>
- STATE-05 拒写/恢复及最终 headless recovery 入口全部通过。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-25-SUMMARY.md`
</output>
