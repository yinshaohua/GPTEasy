---
phase: 01-trusted-local-state-contract
plan: 25
type: execute
wave: 17
depends_on: [01-13, 01-14, 01-24]
files_modified:
  - .github/workflows/phase1-windows-evidence.yml
  - scripts/contracts/test-windows-package-verifier.ps1
  - .github/workflows/phase1-macos-evidence.yml
  - scripts/contracts/test-macos-wave0-contract.ps1
autonomous: true
requirements: [STATE-01, STATE-02, STATE-03, STATE-04, STATE-05]
must_haves:
  truths:
    - "Windows 与 macOS 工作流均接入 state seed/verify 和 recovery prepare/refuse/restore/verify，且使用不同 opaque run IDs。"
    - "所有 smoke 在 cleanup 前完成；统一 finally 才执行 recovery cleanup、state cleanup、app cleanup 与 account/profile cleanup。"
    - "本计划只验证工作流与生产 predicate 接线，不要求、读取或制造签名 secrets，也不生成正式平台证据。"
  artifacts:
    - path: ".github/workflows/phase1-windows-evidence.yml"
      provides: "Windows 双架构最终 full-state/recovery/lifecycle workflow wiring"
    - path: "scripts/contracts/test-windows-package-verifier.ps1"
      provides: "Windows 六步 smoke、distinct IDs 与 unified finally 合同测试"
    - path: ".github/workflows/phase1-macos-evidence.yml"
      provides: "macOS Wave0/full-state/recovery/lifecycle workflow wiring"
    - path: "scripts/contracts/test-macos-wave0-contract.ps1"
      provides: "macOS 六步 smoke、distinct IDs、Wave 0 与 unified finally 合同测试"
  key_links:
    - from: ".github/workflows/phase1-windows-evidence.yml"
      to: "src-tauri/src/lib.rs"
      via: "installed binary 调用 phase1-state-smoke 与 phase1-recovery-smoke 固定 modes"
      pattern: "phase1-state-smoke|phase1-recovery-smoke"
    - from: ".github/workflows/phase1-macos-evidence.yml"
      to: ".github/workflows/phase1-macos-wave0.yml"
      via: "native jobs 继续 needs wave0"
      pattern: "needs: wave0"
  assumptions:
    - "FLAGGED_UNVERIFIED [STATE-01]: specless edge probe 为 unclassified；双工作流必须执行完整状态新进程 verify。"
    - "FLAGGED_UNVERIFIED [STATE-02]: specless edge probe 为 unclassified；工作流 evidence 只输出 allowlist/digest，不上传 DB/Key。"
    - "FLAGGED_UNVERIFIED [STATE-03]: specless edge probe 为 unclassified；installed app 启动必须实际消费 current schema/migration path。"
    - "FLAGGED_UNVERIFIED [STATE-04]: specless edge probe 为 unclassified；工作流必须实际执行 compatible backup restore。"
    - "FLAGGED_UNVERIFIED [STATE-05]: specless edge probe 为 unclassified；工作流必须执行 higher refusal、quarantine restore 与新进程 verify。"
  prohibitions:
    - "FLAGGED_UNVERIFIED: 不得在本计划创建自签名证书、占位 secrets、正式 manifest 或伪远程 run。"
    - "FLAGGED_UNVERIFIED: 不得在 verify/refuse/restore 前清理 state/recovery 工件。"
---

<objective>
把完整状态与恢复 smoke 接入 Windows/macOS 最终工作流，并在不具备签名凭据时完成所有可执行接线验证。

Purpose: 让非签名实现工作安全推进到尽头，使 01-26/01-27 只负责真实平台运行与证据导入。
Output: 两个平台 workflow 与其生产 predicate 合同测试。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trusted-local-state-contract/01-13-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-14-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-24-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-VALIDATION.md
</context>

## Artifacts this phase produces

- `.github/workflows/phase1-windows-evidence.yml`：Windows 双架构最终 full-state/recovery/lifecycle workflow wiring
- `scripts/contracts/test-windows-package-verifier.ps1`：Windows 六步 smoke、distinct IDs 与 unified finally 合同测试
- `.github/workflows/phase1-macos-evidence.yml`：macOS Wave0/full-state/recovery/lifecycle workflow wiring
- `scripts/contracts/test-macos-wave0-contract.ps1`：macOS 六步 smoke、distinct IDs、Wave 0 与 unified finally 合同测试

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 接线 Windows installed full-state/recovery 与统一 finally</name>
  <read_first>
    @.github/workflows/phase1-windows-evidence.yml
    @scripts/contracts/test-windows-package-verifier.ps1
    @scripts/contracts/verify-windows-package.ps1
    @src-tauri/src/lib.rs
    @src-tauri/tests/installed_state_smoke.rs
    @src-tauri/tests/installed_recovery_smoke.rs
  </read_first>
  <files>.github/workflows/phase1-windows-evidence.yml, scripts/contracts/test-windows-package-verifier.ps1</files>
  <behavior>
    - x64/ARM64 jobs 各生成不同 state/recovery run ID。
    - installed binary 顺序执行 state seed→新进程 verify→recovery prepare→新进程 refuse→restore→新进程 verify。
    - unified finally 才执行 recovery cleanup→state cleanup→app cleanup→account/profile cleanup。
  </behavior>
  <action>先扩展 Workflow CaseSet 红测，再修改现有 Windows evidence workflow。保留 exact checkout、host-vs-CLI parity、WSL probe、Authenticode/current-user package 与 lifecycle seam；在 signed install 后插入六步真实进程序列，逐步解析 allowlist JSON 并关联 distinct IDs/digests。finally 必须无条件运行且仅在所有 cleanup 均成功后生成 cleanup_attested。当前执行只运行静态/fixture contract，不调度 workflow、不读取 PFX secrets、不生成 manifests。</action>
  <acceptance_criteria>Windows workflow 的六步顺序、不同 IDs、无提前清理和 finally 顺序均由生产 predicate 测试证明。</acceptance_criteria>
  <verify><automated>powershell -NoProfile -File scripts/contracts/test-windows-package-verifier.ps1 -CaseSet Workflow</automated></verify>
  <done>01-26 只需提供真实 runners/PFX 并运行既定 workflow。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 接线 macOS installed full-state/recovery、Wave 0 与统一 finally</name>
  <read_first>
    @.github/workflows/phase1-macos-evidence.yml
    @.github/workflows/phase1-macos-wave0.yml
    @scripts/contracts/test-macos-wave0-contract.ps1
    @scripts/contracts/run-macos.zsh
    @src-tauri/src/lib.rs
    @src-tauri/tests/installed_recovery_smoke.rs
  </read_first>
  <files>.github/workflows/phase1-macos-evidence.yml, scripts/contracts/test-macos-wave0-contract.ps1</files>
  <behavior>
    - Intel/Apple Silicon jobs 保留 needs wave0，并使用不同 state/recovery run ID。
    - Developer ID/notary/Gatekeeper/current-user seams 后执行与 Windows 相同六步 installed app 序列。
    - unified finally 完成两个 run cleanup、app cleanup、uid/home cleanup 后才允许 evidence upload/attestation。
  </behavior>
  <action>扩展 PowerShell dependency contract，读取 workflow 与嵌入 zsh，先红测 Wave 0、六步顺序、distinct IDs、cleanup→verify→upload→attest 关系。修改 existing macOS workflow，保持 ~/Applications、codesign/notary/Gatekeeper、host parity 和 lifecycle guard；只在所有 smoke 与 cleanup 事实齐全后产生 strict candidate。当前执行不调用 Apple secrets、不调度原生 job、不创建 manifests。</action>
  <acceptance_criteria>macOS workflow 的 Wave 0、双架构、六步 smoke、不同 IDs 与统一 finally 均由本地可执行 contract 证明。</acceptance_criteria>
  <verify><automated>powershell -NoProfile -File scripts/contracts/test-macos-wave0-contract.ps1 -Scripts scripts/contracts/assert-macos-job-lifecycle.zsh,scripts/contracts/run-macos.zsh,scripts/contracts/test-macos-package-verifier.zsh -Workflow .github/workflows/phase1-macos-wave0.yml -EvidenceWorkflow .github/workflows/phase1-macos-evidence.yml; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope PackagingSelfTest -Target Local -Mode Strict</automated></verify>
  <done>01-27 只需提供真实 macOS runners/Developer ID/notary 并运行既定 workflow。</done>
</task>

</tasks>

<threat_model>
Security level: ASVS L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow contract → future signed run | 本地接线必须完整，但不能自称真实平台已经运行。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-63 | Tampering | premature cleanup | critical | mitigate | 六步模式与 unified finally ordering contract tests。 |
| T-01-64 | Spoofing | local workflow validation | high | mitigate | 本计划不生成 formal manifests，结果固定 non-release。 |
| T-01-65 | Information Disclosure | CI evidence | high | mitigate | allowlist 摘要，禁止 DB/Key/raw response 上传。 |
</threat_model>

<verification>
- Windows Workflow CaseSet、macOS Wave 0/evidence contract 与 PackagingSelfTest Local 返回 0。
- 工作树未新增任何 secret、certificate 或正式 manifest。
</verification>

<success_criteria>
- 所有非签名功能与最终 workflow 接线可在缺少发布凭据时执行到 01-25。
- 真实签名证据仍只由 01-26/01-27 生成。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-25-SUMMARY.md`
</output>
