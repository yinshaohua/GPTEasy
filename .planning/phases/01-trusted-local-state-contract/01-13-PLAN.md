---
phase: 01-trusted-local-state-contract
plan: 13
type: execute
wave: 6
depends_on: [01-04, 01-06, 01-10, 01-11]
files_modified:
  - scripts/contracts/assert-windows-job-lifecycle.ps1
  - scripts/contracts/verify-windows-package.ps1
  - scripts/contracts/test-windows-package-verifier.ps1
  - tests/fixtures/contracts/packaging/windows-positive-control.json
  - .github/workflows/phase1-windows-evidence.yml
autonomous: true
requirements:
  - STATE-01
  - STATE-02
  - STATE-03
  - STATE-04
  - STATE-05
must_haves:
  truths:
    - "package predicate 同时拒绝 unsigned、架构错、非 current-user、缺生命周期证据，并接受 test-only 正控制但标记 strict_gate_eligible=false。"
    - "每个 job 创建独立 OS 用户并记录 runner/job/account identity；finally 删除账户/profile 或验证完整 baseline restore。"
    - "Windows x64/ARM64 workflow 运行 CLI/host canary parity、WSL probe、签名 package 与 provenance attestation。"
  artifacts:
    - path: "scripts/contracts/assert-windows-job-lifecycle.ps1"
      provides: "runner/job/SID/profile 创建、基线与销毁/恢复证明"
    - path: "scripts/contracts/verify-windows-package.ps1"
      provides: "Authenticode/PE arch/current-user/lifecycle/path predicate"
    - path: "scripts/contracts/test-windows-package-verifier.ps1"
      provides: "生产 predicate 正控制、负例和 success aggregation"
    - path: "tests/fixtures/contracts/packaging/windows-positive-control.json"
      provides: "test-only 有效签名/架构/current-user/profile metadata 正控制"
    - path: ".github/workflows/phase1-windows-evidence.yml"
      provides: "Windows x64/ARM64 contract/package attested workflow"
  key_links:
    - from: ".github/workflows/phase1-windows-evidence.yml"
      to: "scripts/contracts/assert-windows-job-lifecycle.ps1"
      via: "创建专用 OS 用户并在 finally 验证 SID/profile 销毁或 baseline 恢复"
      pattern: "cleanup_attested"
---

<objective>
建立 Windows 双架构 package predicate、一次性 OS 用户生命周期证据与 attested workflow。

Purpose: 用真实账户/runner 生命周期而不是应用 marker 证明 disposable 状态，并补齐正控制。
Output: Windows lifecycle guard、package verifier、自测 fixture 与 evidence workflow。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trusted-local-state-contract/01-04-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-06-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-10-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-11-SUMMARY.md
@.codex/skills/spike-findings-gpteasy/references/install-and-update.md
</context>

## Artifacts this phase produces

- `scripts/contracts/assert-windows-job-lifecycle.ps1`：runner/job/SID/profile 创建、基线与销毁/恢复证明
- `scripts/contracts/verify-windows-package.ps1`：Authenticode/PE arch/current-user/lifecycle/path predicate
- `scripts/contracts/test-windows-package-verifier.ps1`：生产 predicate 正控制、负例和 success aggregation
- `tests/fixtures/contracts/packaging/windows-positive-control.json`：test-only 有效签名/架构/current-user/profile metadata 正控制
- `.github/workflows/phase1-windows-evidence.yml`：Windows x64/ARM64 contract/package attested workflow

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 Windows 生命周期与 package 生产 predicate</name>
  <files>scripts/contracts/assert-windows-job-lifecycle.ps1, scripts/contracts/verify-windows-package.ps1, scripts/contracts/test-windows-package-verifier.ps1, tests/fixtures/contracts/packaging/windows-positive-control.json</files>
  <behavior>
    - test-only 正控制通过同一 predicate，但 strict_gate_eligible=false。
    - unsigned、wrong-arch、system install、marker-only、cleanup 缺失分别失败。
    - native pass 需要 Authenticode Valid、目标 PE arch、LOCALAPPDATA/current-user 与 attested lifecycle。
  </behavior>
  <action>lifecycle guard 绑定 GitHub run/attempt/job、runner name/image/tracking identity 和新建 OS 用户 SID/profile；marker 仅作相关字段，不能证明 disposable。finally 必须先停止 run-scoped app，再删除账户/profile并复核不存在；若环境不能销毁账户，则要求 self-hosted ephemeral runner 与完整 baseline hash restore。package verifier 的生产 predicate 接受实际探针结果或 fixture 注入；fixture_mode 的成功始终 strict_gate_eligible=false。自测覆盖正控制与所有拒绝分支。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/test-windows-package-verifier.ps1 -CaseSet All; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/test-windows-package-verifier.ps1 -CaseSet Workflow</automated></verify>
  <done>拒绝所有 artifact 的实现无法通过，因为正控制和 success aggregation 必须成功。</done>
</task>

<task type="auto">
  <name>Task 2: 创建 Windows 双架构 attested evidence workflow</name>
  <files>.github/workflows/phase1-windows-evidence.yml</files>
  <action>创建 x64/ARM64 原生 jobs：exact commit checkout，创建 per-job OS user，运行 01-11 official CLI/host parity 与 WSL probe，正式 Authenticode build，执行 package/path smoke；在统一 finally 完成 app cleanup、账户/profile 销毁或 baseline restore并写 lifecycle result，之后上传 installer/evidence bundle并生成 provenance attestation。cleanup 未通过时 job 失败且不得生成 strict pass manifest。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope PackagingSelfTest -Target Local -Mode Strict</automated></verify>
  <done>workflow 与 verifier 的本地 contract/positive/negative tests 全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Windows runner/package → 当前用户 profile | 签名工件在 OS 账户内执行并可能触达用户状态。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-28 | Spoofing | Windows package | high | mitigate | PE arch、Authenticode、current-user 与 attestation。 |
| T-01-29 | Tampering | disposable account claim | critical | mitigate | job/runner/SID identity 与 attested delete/restore；marker 不能单独通过。 |
| T-01-30 | Information Disclosure | signing/profile evidence | high | mitigate | secret store、字段 allowlist、无用户绝对路径。 |
</threat_model>

<verification>
- Windows verifier 正控制与负例全部通过。
- workflow 静态合同包含统一 finally cleanup 与 attestation。
</verification>

<success_criteria>
- Windows evidence 可证明 package 行为与一次性账户生命周期。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-13-SUMMARY.md`
</output>
