---
phase: 01-trusted-local-state-contract
plan: 09
type: execute
wave: 4
depends_on: [01-08]
files_modified:
  - src-tauri/Cargo.toml
  - src-tauri/Cargo.lock
  - src-tauri/build.rs
  - src-tauri/tauri.conf.json
  - src-tauri/capabilities/default.json
  - src-tauri/src/main.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements:
  - STATE-01
  - STATE-02
must_haves:
  truths:
    - "Tauri/Rust composition root 可编译，main 只调用 library run。"
    - "capability 只有 core:default，Windows NSIS currentUser 与 macOS 14 contract 明确。"
  artifacts:
    - path: "src-tauri/Cargo.toml"
      provides: "exact Rust/Tauri/SQLite dependencies"
    - path: "src-tauri/Cargo.lock"
      provides: "可重复 Cargo 依赖图"
    - path: "src-tauri/build.rs"
      provides: "tauri_build 入口"
    - path: "src-tauri/tauri.conf.json"
      provides: "bundle identifier/current-user/minimum OS contract"
    - path: "src-tauri/capabilities/default.json"
      provides: "最小 core capability"
    - path: "src-tauri/src/main.rs"
      provides: "薄二进制入口"
    - path: "src-tauri/src/lib.rs"
      provides: "可测试 composition root"
  key_links:
    - from: "src-tauri/src/main.rs"
      to: "src-tauri/src/lib.rs"
      via: "只调用 gpteasy_lib::run()"
      pattern: "gpteasy_lib::run"
---

<objective>
创建最小 Tauri 2/Rust 当前用户应用壳。

Purpose: 为真实 path、contract 与 signed package smoke 提供可编译进程。
Output: Cargo 工程、最小 capability、bundle contract 与薄入口。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0002-tauri-rust-react.md
@docs/adr/0006-sqlite-application-state.md
@.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
@.planning/phases/01-trusted-local-state-contract/01-08-SUMMARY.md
</context>

## Artifacts this phase produces

- `src-tauri/Cargo.toml`：exact Rust/Tauri/SQLite dependencies
- `src-tauri/Cargo.lock`：可重复 Cargo 依赖图
- `src-tauri/build.rs`：tauri_build 入口
- `src-tauri/tauri.conf.json`：bundle identifier/current-user/minimum OS contract
- `src-tauri/capabilities/default.json`：最小 core capability
- `src-tauri/src/main.rs`：薄二进制入口
- `src-tauri/src/lib.rs`：可测试 composition root

<tasks>

<task type="auto">
  <name>Task 1: 锁定 Rust/Tauri 工程与最小 capability</name>
  <reversibility rating="costly">crate、bundle identifier 与依赖图会被后续工件消费。</reversibility>
  <files>src-tauri/Cargo.toml, src-tauri/Cargo.lock, src-tauri/build.rs, src-tauri/capabilities/default.json</files>
  <action>按 RESEARCH exact versions 创建 edition 2021 crate；rusqlite 开启 bundled 与 backup，windows-sys 只在 cfg(windows)，tempfile 仅 dev。capability 只授权 main window 的 core:default；不安装未经 audit 的新 crate。</action>
  <verify><automated>cargo metadata --manifest-path src-tauri/Cargo.toml --locked --no-deps</automated></verify>
  <done>Cargo graph 可锁定解析且无高权限 plugin。</done>
</task>

<task type="auto">
  <name>Task 2: 建立薄入口与当前用户 bundle contract</name>
  <files>src-tauri/tauri.conf.json, src-tauri/src/main.rs, src-tauri/src/lib.rs</files>
  <action>lib 创建最小 Tauri builder/main window；main 只调用 run。配置 com.gpteasy.desktop、frontendDist、严格 CSP、Windows currentUser NSIS、macOS minimum 14.0；不注册业务 command、托盘、账户、网络或 updater。</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --lib; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; npm run tauri -- build --debug --no-bundle</automated></verify>
  <done>Tauri debug 应用可编译并保持最小权限。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebView → Tauri runtime | capability 与 composition root 决定前端能调用的原生能力。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-18 | Elevation of Privilege | Tauri capability | high | mitigate | 仅 core:default，无 fs/shell/http/SQL/updater plugin。 |
| T-01-19 | Tampering | Cargo dependency graph | high | mitigate | RESEARCH audit、exact versions、Cargo.lock 与 locked metadata。 |
</threat_model>

<verification>
- cargo test --lib 与 no-bundle build 返回 0。
</verification>

<success_criteria>
- 最小 Tauri/Rust 应用壳可用于平台 smoke。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-09-SUMMARY.md`
</output>
