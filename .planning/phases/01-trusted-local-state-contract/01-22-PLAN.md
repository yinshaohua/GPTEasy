---
phase: 01-trusted-local-state-contract
plan: 22
type: execute
wave: 13
depends_on: [01-21]
files_modified:
  - src-tauri/tests/migration_matrix.rs
  - tests/fixtures/databases/history-lock.json
  - scripts/contracts/verify-migration-history.ps1
  - scripts/contracts/verify-migration-policy.ps1
  - tests/fixtures/migrations/forbidden-migration-cases.json
autonomous: true
requirements:
  - STATE-03
must_haves:
  truths:
    - "migration_matrix 从 manifest 枚举已存在 fixture，复制到 TempDir 后升级，仓库原件只读。"
    - "history lock 固定 SQL、fixture 与 schema fingerprint，并与 release tags 交叉比较。"
    - "VACUUM、ATTACH、DETACH、PRAGMA journal_mode 及 Rust data transform 外部文件/进程副作用由负例 lint 拒绝。"
  artifacts:
    - path: "src-tauri/tests/migration_matrix.rs"
      provides: "manifest-driven 全历史顺序迁移矩阵"
    - path: "tests/fixtures/databases/history-lock.json"
      provides: "migration SQL/fixture/schema fingerprint 不可变 lock"
    - path: "scripts/contracts/verify-migration-history.ps1"
      provides: "工作树与 release tag 只读漂移 gate"
    - path: "scripts/contracts/verify-migration-policy.ps1"
      provides: "SQL token 与 Rust transform capability 禁止项 lint"
    - path: "tests/fixtures/migrations/forbidden-migration-cases.json"
      provides: "四类 SQL 与外部文件/进程 side-effect 负例"
  key_links:
    - from: "tests/fixtures/databases/manifest.json"
      to: "src-tauri/tests/migration_matrix.rs"
      via: "先验证 hash 再复制每个 fixture"
      pattern: "fixtures"
---

<objective>
建立 manifest-driven historical migration matrix、history lock/tag drift gate 与迁移禁止项 lint。

Purpose: 证明全部历史升级，并禁止破坏单事务回滚的 SQL/data-transform 副作用。
Output: 迁移矩阵、不可变 lock、只读 drift verifier、policy verifier 与负例 fixture。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0006-sqlite-application-state.md
@.planning/phases/01-trusted-local-state-contract/01-21-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
</context>

## Artifacts this phase produces

- `src-tauri/tests/migration_matrix.rs`：manifest-driven 全历史顺序迁移矩阵
- `tests/fixtures/databases/history-lock.json`：migration SQL/fixture/schema fingerprint 不可变 lock
- `scripts/contracts/verify-migration-history.ps1`：工作树与 release tag 只读漂移 gate
- `scripts/contracts/verify-migration-policy.ps1`：SQL token 与 Rust transform capability 禁止项 lint
- `tests/fixtures/migrations/forbidden-migration-cases.json`：四类 SQL 与外部文件/进程 side-effect 负例

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 建立 manifest-driven historical migration matrix</name>
  <files>src-tauri/tests/migration_matrix.rs</files>
  <behavior>
    - 不在 Rust 源码手写 fixture 版本列表。
    - 每个 fixture 先 hash/application_id/user_version 校验，再复制到 TempDir。
    - test-only schema versions 2 and 3 顺序后 ledger/checksum/fingerprint/FK/quick_check 一致。
  </behavior>
  <action>消费 01-21 已创建的 manifest 和 v001 DB；所有测试只操作 TempDir 副本。用 test-only 后续 migrations 验证跨多版本顺序，禁止在 verify 中调用 generator 或覆盖 committed fixture。</action>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test migration_matrix</automated></verify>
  <done>所有 committed history fixture 均通过同一升级矩阵。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 固定 history lock 与 release-tag drift gate</name>
  <files>tests/fixtures/databases/history-lock.json, scripts/contracts/verify-migration-history.ps1</files>
  <action>history-lock 记录 0001 SQL SHA、fixture SHA、application ID、user_version 与 schema fingerprint。verifier 重算当前文件并枚举已合并 v* release tags；tag 中存在对应历史时比较摘要，任何修改失败。没有 tag 时仍要求本地 lock 非空完整。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/verify-migration-history.ps1</automated></verify>
  <done>migration/fixture 历史不能被静默改写。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 禁止非事务 migration SQL 与 data-transform 外部副作用</name>
  <files>scripts/contracts/verify-migration-policy.ps1, tests/fixtures/migrations/forbidden-migration-cases.json</files>
  <behavior>
    - SQL 词法扫描在注释/字符串外拒绝 VACUUM、ATTACH、DETACH 与 PRAGMA journal_mode。
    - Rust migration transforms 只能接收 Transaction，registry/migrations 模块不得引用 fs、File、OpenOptions、Path、Command 或网络 API。
    - 正常 DDL/DML 与 constrained transform 正控制通过。
  </behavior>
  <action>实现只读 policy verifier，扫描全部 migration SQL 与 migrations 模块；负例 JSON 同时包含大小写、注释绕过、字符串字面量正控制和 Rust 外部文件/进程 snippet。SelfTest 必须把负例复制到临时目录并走同一生产 lexer/capability predicate。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/verify-migration-policy.ps1 -SelfTest; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; powershell -NoProfile -File scripts/contracts/verify-migration-policy.ps1 -RepositoryRoot .</automated></verify>
  <done>研究列出的非事务与外部 side-effect 迁移均由机器 lint 阻断。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration source → SQLite transaction/外部系统 | 迁移脚本若执行非事务 SQL 或文件/进程副作用会破坏完整回滚。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-50 | Tampering | migration history | high | mitigate | history lock、fixture hash 与 tag drift gate。 |
| T-01-51 | Tampering | non-transactional migration | critical | mitigate | SQL lexer 与 Rust capability lint/负例。 |
</threat_model>

<verification>
- migration_matrix、history verifier、policy self-test/real scan 返回 0。
</verification>

<success_criteria>
- STATE-03 历史链和迁移安全约束均可执行验证。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-22-SUMMARY.md`
</output>
