---
phase: 01-trusted-local-state-contract
plan: 22
type: execute
wave: 14
depends_on: [01-21]
files_modified:
  - src-tauri/src/state/validation.rs
  - src-tauri/src/state/preflight.rs
  - src-tauri/src/state/mod.rs
  - src-tauri/tests/db_contract_validation.rs
  - src-tauri/tests/ready_preflight.rs
autonomous: true
requirements: [STATE-03, STATE-04, STATE-05]
must_haves:
  truths:
    - "现有 DB 的所有合同检查在 SQLITE_OPEN_READ_ONLY connection 完成。"
    - "统一 validator 检查 application_id、user_version、quick_check、foreign_key_check、ledger/checksum、schema fingerprint、database identity 与可选 snapshot identity。"
    - "任何失败都进入 recovery-only；验证前后 DB/WAL/SHM/backup/quarantine 的存在性、hash、长度和 mtime 不变。"
  artifacts:
    - path: "src-tauri/src/state/validation.rs"
      provides: "统一只读 DB contract validator"
    - path: "src-tauri/src/state/preflight.rs"
      provides: "任何 RW open 前的 PreflightDecision"
    - path: "src-tauri/src/state/mod.rs"
      provides: "StateStore Ready/Migrate 对 validator 的 wiring"
    - path: "src-tauri/tests/db_contract_validation.rs"
      provides: "截断、ledger/FK/schema/identity 负例与 non-mutation"
    - path: "src-tauri/tests/ready_preflight.rs"
      provides: "Create/Ready/Migrate/RecoveryOnly 分支 gate tests"
  key_links:
    - from: "src-tauri/src/state/preflight.rs"
      to: "src-tauri/src/state/validation.rs"
      via: "existing DB 第一连接只读运行完整合同"
      pattern: "validate_database_contract"
  assumptions:
    - "FLAGGED_UNVERIFIED [STATE-03]: specless edge probe 为 unclassified；ledger/checksum/fingerprint 与 compiled registry 比较作为显式 backstop。"
    - "FLAGGED_UNVERIFIED [STATE-04]: specless edge probe 为 unclassified；同一 validator 将在 01-23 校验 migration source 与 backup snapshot。"
    - "FLAGGED_UNVERIFIED [STATE-05]: specless edge probe 为 unclassified；higher/invalid DB 的 read-only recovery-only 分支作为显式 backstop。"
  prohibitions:
    - "FLAGGED_UNVERIFIED: existing DB 在完整合同通过前不得打开 RW、设置 WAL、写 ledger 或创建 backup。"
---

<objective>
实现 Ready、Migrate source、backup 与 recovery candidate 共用的只读数据库合同 validator。

Purpose: 阻止只凭 application_id/user_version 打开损坏、伪造、漂移或未来数据库。
Output: validator、preflight wiring 与 non-mutating 正反测试。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/adr/0006-sqlite-application-state.md
@.planning/phases/01-trusted-local-state-contract/01-21-SUMMARY.md
@.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
</context>

## Artifacts this phase produces

- `src-tauri/src/state/validation.rs`：统一只读 DB contract validator
- `src-tauri/src/state/preflight.rs`：任何 RW open 前的 PreflightDecision
- `src-tauri/src/state/mod.rs`：StateStore Ready/Migrate 对 validator 的 wiring
- `src-tauri/tests/db_contract_validation.rs`：截断、ledger/FK/schema/identity 负例与 non-mutation
- `src-tauri/tests/ready_preflight.rs`：Create/Ready/Migrate/RecoveryOnly 分支 gate tests

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义统一只读 DB contract validator</name>
  <read_first>
    @src-tauri/src/state/migrations/mod.rs
    @tests/fixtures/databases/manifest.json
    @tests/fixtures/databases/history-lock.json
    @src-tauri/src/state/mod.rs
    @docs/adr/0006-sqlite-application-state.md
  </read_first>
  <files>src-tauri/src/state/validation.rs, src-tauri/tests/db_contract_validation.rs</files>
  <behavior>
    - integrity/ledger/schema/database identity 任一不一致返回类型化错误。
    - ExpectedSnapshotIdentity 同时比较 sidecar 声明与实际 snapshot digest。
    - 验证前后 DB/WAL/SHM/backup/quarantine 全部元数据不变。
  </behavior>
  <action>用 SQLITE_OPEN_READ_ONLY 打开，按 user_version 从 compiled registry 查找期望 rows/checksum/fingerprint；执行 quick_check、foreign_key_check，并验证 schema_migrations、state_metadata database UUID/application identity/schema fingerprint。可选 ExpectedSnapshotIdentity 还验证 sidecar、source database UUID/from-to/backup UUID 与实际 SHA。红测构造截断、伪 ledger、FK 损坏、schema SQL/user_version 不一致、identity 缺失、sidecar 替换和 snapshot 替换。</action>
  <acceptance_criteria>所有损坏/伪造/替换负例稳定失败且相关文件完全不变；正式 v001/current DB 通过。</acceptance_criteria>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test db_contract_validation</automated></verify>
  <done>数据库是否可信由一个只读生产 predicate 决定。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把完整 validator 接入 Ready/Migrate preflight</name>
  <read_first>
    @src-tauri/src/state/validation.rs
    @src-tauri/src/state/coordination.rs
    @src-tauri/src/state/mod.rs
    @src-tauri/tests/state_concurrency.rs
  </read_first>
  <files>src-tauri/src/state/preflight.rs, src-tauri/src/state/mod.rs, src-tauri/tests/ready_preflight.rs</files>
  <behavior>
    - existing DB 第一 connection 必为 read-only。
    - 只有完整合同通过才取得 StateCoordinator 并进入 RW/WAL。
    - higher/invalid/failure 返回 RecoveryOnly，普通写 command 不可用。
  </behavior>
  <action>实现 PreflightDecision::Create/Ready/Migrate/RecoveryOnly；Ready/Migrate 均先消费统一 validator，再取得 coordinator，随后重新确认版本并打开 RW。compatibility 前禁止 journal_mode、ledger、backup、quarantine 或任何 sidecar 写入。测试以文件快照证明每个失败分支 non-mutating，并覆盖 validation 后到 lock/RW 前版本变化的 fail-closed 路径。</action>
  <acceptance_criteria>Ready/Migrate 只有通过完整只读合同才能进入写路径；所有失败和 higher schema 返回类型化 RecoveryOnly。</acceptance_criteria>
  <verify><automated>cargo test --manifest-path src-tauri/Cargo.toml --test ready_preflight --test db_contract_validation</automated></verify>
  <done>所有现有 DB 的 RW 入口由统一 validator 与 coordinator 保护。</done>
</task>

</tasks>

<threat_model>
Security level: ASVS L1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 现有 SQLite 文件 → RW connection | 损坏、伪造或未来数据库若进入写连接会造成不可逆篡改。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-53 | Tampering | forged ledger/schema | critical | mitigate | compiled checksum/fingerprint 与 read-only validator。 |
| T-01-54 | Denial of Service | truncated DB | high | mitigate | 类型化 RecoveryOnly，不 reset 或 RW。 |
| T-01-55 | Tampering | TOCTOU after validation | high | mitigate | coordinator 后重读版本/identity 再进入 RW。 |
</threat_model>

<verification>
- db_contract_validation 与 ready_preflight 返回 0。
</verification>

<success_criteria>
- 所有 DB 消费路径共享同一完整只读合同。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-22-SUMMARY.md`
</output>
