---
phase: 01-trusted-local-state-contract
plan: 04
type: execute
wave: 2
depends_on: [01-03]
files_modified:
  - tests/fixtures/contracts/schema/contract-manifest.schema.json
  - tests/fixtures/contracts/schema/provenance.schema.json
  - tests/fixtures/contracts/provenance-negative-cases.json
  - scripts/contracts/validate-contract-evidence.ps1
  - scripts/contracts/verify-evidence-provenance.ps1
  - scripts/contracts/test-evidence-provenance.ps1
autonomous: true
requirements:
  - STATE-02
  - STATE-03
  - STATE-04
  - STATE-05
must_haves:
  truths:
    - "manifest 自报 hash、runner 标签或 verified 布尔值不能单独授予通过。"
    - "主工作区按 run/job/artifact ID 重新取回并验证 attestation、commit 与 digest。"
    - "敏感字段、非原生 verdict、过期/不可取回/错配 evidence 全部 fail-closed。"
  artifacts:
    - path: "tests/fixtures/contracts/schema/contract-manifest.schema.json"
      provides: "外部 contract/package manifest allowlist schema"
    - path: "tests/fixtures/contracts/schema/provenance.schema.json"
      provides: "run/job/commit/artifact/attestation 必填 schema"
    - path: "tests/fixtures/contracts/provenance-negative-cases.json"
      provides: "自报、陈旧、错 job/commit/digest 与敏感字段负例"
    - path: "scripts/contracts/validate-contract-evidence.ps1"
      provides: "schema、evidence level、allowlist 与 canary 校验"
    - path: "scripts/contracts/verify-evidence-provenance.ps1"
      provides: "临时下载、GitHub attestation 验签与 digest 交叉验证"
    - path: "scripts/contracts/test-evidence-provenance.ps1"
      provides: "生产 validator/verifier 正反自测"
  key_links:
    - from: "scripts/contracts/verify-evidence-provenance.ps1"
      to: "tests/fixtures/contracts/schema/provenance.schema.json"
      via: "验证字段后重新查询与下载，不信任本地同名文件"
      pattern: "gh attestation verify"
---

<objective>
建立外部 evidence schema、脱敏校验和可独立验证的 provenance 核心。

Purpose: 阻止自报、陈旧或错配 artifact 关闭 freeze/final gate。
Output: 两份 schema、负例 fixture、validator 与 attestation verifier。
</objective>

<execution_context>
@C:/Users/yinsh/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/yinsh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-trusted-local-state-contract/01-RESEARCH.md
@.planning/phases/01-trusted-local-state-contract/01-PATTERNS.md
@C:/Users/yinsh/.codex/gsd-core/references/security-asvs-levels.md
</context>

## Artifacts this phase produces

- `tests/fixtures/contracts/schema/contract-manifest.schema.json`：外部 contract/package manifest allowlist schema
- `tests/fixtures/contracts/schema/provenance.schema.json`：run/job/commit/artifact/attestation 必填 schema
- `tests/fixtures/contracts/provenance-negative-cases.json`：自报、陈旧、错 job/commit/digest 与敏感字段负例
- `scripts/contracts/validate-contract-evidence.ps1`：schema、evidence level、allowlist 与 canary 校验
- `scripts/contracts/verify-evidence-provenance.ps1`：临时下载、GitHub attestation 验签与 digest 交叉验证
- `scripts/contracts/test-evidence-provenance.ps1`：生产 validator/verifier 正反自测

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 固定 manifest/provenance schema 与脱敏 validator</name>
  <files>tests/fixtures/contracts/schema/contract-manifest.schema.json, tests/fixtures/contracts/schema/provenance.schema.json, tests/fixtures/contracts/provenance-negative-cases.json, scripts/contracts/validate-contract-evidence.ps1</files>
  <behavior>
    - provenance 要求 immutable workflow ref、run attempt、numeric job/artifact ID、40-hex commit 与 sha256 digest。
    - raw config、raw app-server response、完整命令行、token 与数据库内容被拒绝。
    - blocked、partial、synthetic 或 cleanup 未证明的 evidence 不具备 strict eligibility。
  </behavior>
  <action>创建严格 JSON Schema 和 validator；manifest 只允许摘要、布尔判据、origin 类型、计数、redactions、runner/account lifecycle 与 provenance 引用。负例 fixture 覆盖缺字段、错 digest 格式、non-native、sensitive field、仅 marker 的 disposable 声明和 strict_gate_eligible 自报。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/validate-contract-evidence.ps1 -SelfTest</automated></verify>
  <done>结构、证据等级和敏感边界均 fail-closed。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现重新取回与签名 provenance 验证</name>
  <files>scripts/contracts/verify-evidence-provenance.ps1, scripts/contracts/test-evidence-provenance.ps1</files>
  <behavior>
    - run、attempt、job、workflow、commit、artifact 任一不匹配即失败。
    - artifact 与 attestation subject digest 同时匹配下载文件才通过。
    - auth/网络/过期属于 blocked，不回退本地文件。
  </behavior>
  <action>verifier 只在 01-06 的 gh preflight 通过后执行真实网络路径；每次下载到新临时目录，查询 run/jobs/artifacts 并对 evidence bundle 与被测工件分别运行 gh attestation verify --repo yinshaohua/GPTEasy，重算 SHA-256 后交叉匹配。self-test 使用注入的 gh transcript/fixture 覆盖正控制、自报、错 commit/job/digest、缺 attestation 与不可取回，不需要真实网络。</action>
  <verify><automated>powershell -NoProfile -File scripts/contracts/run-phase1-contracts.ps1 -Scope ProvenanceSelfTest -Target Local -Mode Strict</automated></verify>
  <done>通过结论来自独立取回与验签；不可用只产生 blocked。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 外部 CI → 主工作区 gate | 不受本地控制的 runner 结果决定磁盘合同和阶段完成。 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Spoofing | external manifest | high | mitigate | run/job/artifact 查询、attestation 与 commit 交叉验证。 |
| T-01-08 | Tampering | downloaded artifact | high | mitigate | 新临时目录、重算 digest、subject digest 对齐。 |
| T-01-09 | Information Disclosure | evidence bundle | high | mitigate | schema allowlist 与多 needle canary。 |
</threat_model>

<verification>
- ProvenanceSelfTest 返回 0。
- 负例不能被 AllowBlocked 转换为 strict pass。
</verification>

<success_criteria>
- 外部证据可独立追溯且脱敏。
</success_criteria>

<output>
执行完成后创建 `.planning/phases/01-trusted-local-state-contract/01-04-SUMMARY.md`
</output>
