# 当前供应商使用保存并应用语义

修改供应商的服务地址、API Key 或默认模型后必须重新验证，验证成功本身不提交。未被当前用户 Codex 环境使用的供应商由用户明确“保存”；正在使用的供应商只能由用户明确“保存并应用”，通过可恢复操作同时替换 SQLite 供应商记录和 Codex 配置。每个供应商只保留当前已验证版本；保存并应用期间，已验证且已确认的目标快照可以连同明文 API Key 临时保存在未完成操作中，但不属于草稿或历史版本。任何验证、写入或状态提交失败都保留完整旧供应商和旧配置。仅修改显示名称不影响服务能力，可以直接保存而不重写 Codex 配置。
