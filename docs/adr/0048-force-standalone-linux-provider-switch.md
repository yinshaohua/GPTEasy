---
status: accepted
---

# 独立 Linux 切换优先生成可用配置

独立 Linux 导出脚本把用户明确选择供应商视为强制设置授权：每次先保存完整恢复点，再用所选供应商的最小 GPTEasy 配置整体替换旧 `config.toml`，不解析、拼接或因旧供应商字段、损坏管理边界和未知 schema 拒绝。独立脚本没有可靠的通用 TOML 解析器，而保留任意旧内容可能使重复键或损坏语法阻止新配置生效；因此切换以新供应商可用为优先，旧配置只通过 `gpteasy restore` 恢复。文件身份异常、Codex 版本不兼容、锁竞争和操作期间的并发修改仍失败关闭。
