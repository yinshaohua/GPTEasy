# 采用直接配置模式，不运行本地 API 代理网关

GPTEasy 选择直接修改宿主应用、WSL2 和 Linux 用户级 Codex 配置来切换供应商，而不是让 Codex 始终连接一个由 GPTEasy 运行的本地 API 网关。首版只管理当前操作系统用户的默认 Codex 配置，不管理机器级公共配置，也不提供自定义配置路径。每次修改前备份原配置，并通过临时文件和原子替换避免部分写入。这样 GPTEasy 不必持续驻留在请求链路中，导出的 Linux function 可以脱离 GPTEasy 长期使用；代价是已经运行的 Codex 需要重启才能读取切换后的配置。
