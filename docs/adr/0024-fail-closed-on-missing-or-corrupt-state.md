# 已有安装的 SQLite 状态缺失或损坏时停止写入

SQLite 保存供应商凭据和恢复所需的应用状态，已有安装不能在数据库缺失、损坏、迁移失败或版本过高时静默创建空库。GPTEasy 进入恢复错误页，保留原数据库或损坏副本，并只允许自动选择按时间倒序排列的最新有效 SQLite Backup API 一致备份恢复；恢复数据库后只读协调 Codex 磁盘状态，不直接覆盖其配置；不提供备份浏览器，所有备份无效时继续阻止普通操作。只有全新安装且不存在既有 GPTEasy 数据时才初始化空库。这样避免用户以为供应商目录仍然存在而继续切换。
