# 用户确认后重启 ChatGPT/Codex 桌面版

GPTEasy 在用户明确确认后可以关闭并重新激活可靠识别的 ChatGPT/Codex 桌面版，以便配置切换立即被新桌面进程读取；这取代 ADR-0020 的“永不控制消费者”决定。正常关闭失败时，只有再次说明任务中断风险并获得确认后，才能强制结束已核实的 OpenAI 官方包进程；Codex CLI 因无法恢复终端、工作目录和会话而始终只提示用户手动重启。该能力必须动态发现应用包身份，并通过真实 Windows 桌面程序 UAT，不能只依赖进程名或 fixture 证据。
