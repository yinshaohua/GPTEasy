# 外部配置只能在明确确认后安全接管

没有有效 GPTEasy 供应商 ID 的当前用户 Codex 配置一律视为外部配置，不按名称、地址或模型自动认领。用户首次切换供应商时，GPTEasy 展示将替换的供应商相关字段并取得明确确认，然后创建完整备份，通过结构化 TOML 编辑移除冲突字段、保留其他设置并建立唯一管理区块；后续切换只替换该区块。TOML 损坏、管理区块重复、字段归属不明或无法安全迁移时拒绝写入，不自动修复，也不要求主要用户手工清理后才能正常使用。
