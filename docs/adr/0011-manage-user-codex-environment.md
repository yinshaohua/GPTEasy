# 管理当前用户 Codex 环境而非桌面宿主应用

GPTEasy 的核心受管对象是当前 Windows 用户默认的 `~/.codex` 配置环境，桌面 Codex 和 Codex CLI 只是读取该环境的消费者。产品能力不依赖某个桌面应用存在，只有 CLI、两者同时存在或暂时都不存在时仍可管理供应商；消费者检测仅用于验证兼容性和判断重启状态。这样避免把产品绑定到易变化的桌面应用身份，同时准确反映共享用户级配置的实验事实。
