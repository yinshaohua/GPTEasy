# API Key 明文保存在 SQLite

GPTEasy 重建后仍由 Rust 后端把供应商 API Key 明文保存在当前用户的 SQLite 数据库中，不使用 Windows Credential Manager，也不要求用户每次切换时重新输入。该选择减少凭据存储与数据库之间的双资源协调，使持久化、迁移和恢复路径更简单；代价是数据库及其备份必须按敏感文件保护，API Key 只能在用户明确操作时完整显示或复制，并且不得进入日志、普通错误、诊断导出或非必要进程参数。
