# 只持久化一个未完成配置操作

SQLite、Codex 配置和凭据载体不能共享同一个原子事务，因此每次配置变更在写磁盘前先持久化至多一个未完成操作，记录操作类型、阶段、目标身份、旧/新指纹、备份引用，以及恢复所需的已验证目标快照；目标快照可以包含明文 API Key，但它已经由用户确认，不是未验证草稿。所有配置变更串行执行。成功或恢复完成后删除该记录，不保留已完成操作或供应商版本历史；启动时若磁盘匹配旧值则恢复旧状态，匹配新值则完成提交，两者都不匹配时停止并进入管理冲突。这样保留崩溃恢复能力，同时避免引入持久诊断日志和操作历史模型。
